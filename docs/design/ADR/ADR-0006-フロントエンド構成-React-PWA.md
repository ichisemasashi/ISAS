# ADR-0006：フロントエンド構成＝React＋TypeScript＋PWA（オフラインファースト）

| 項目 | 内容 |
|---|---|
| ステータス | **提案中**（v1。起票直後。これから敵対的レビュー） |
| 由来 | 要裁定（要求仕様7章のたたき台「Web（React等）＋PWA」を確定する） |
| 関連 | 要求仕様 [5.1.1 オフライン／4.11 UX共通(F-D01〜13)／5.1 UX要求／F-08a 位置ログ／F-73/F-74](../../農業営農支援システム_要求仕様書.md)、[ADR-0007 オフライン同期]、[ADR-0010 地図(MapLibre)]、[ADR-0015 国際化]、[ADR-0016 UXデザインシステム]、[ADR-0009 認証]、[データモデル設計書](../データモデル設計書.md) |

---

## 1. 背景・課題（Context）

フロントエンドは、本システムの最大の技術リスク（現場のオフライン利用）とUX目標（KSASより使いやすく）を同時に満たす必要がある。満たすべき要件：

- **現場＝モバイルでオフライン利用**（要求仕様5.1.1）：圏外の圃場で作業日誌・農薬使用基準チェック・在庫出庫・打刻・位置ログを行い、復帰時に同期。オフライン中も**事前警告**（農薬使用基準 F-41）が要る。
- **UX目標**（5.1／4.11）：作業日誌入力30秒、タスク成功率90%、SUS 75。片手・手袋・屋外日光下でも使える。
- **2つの利用文脈**：**現場（スマホ・オフライン・素早い入力）**と**オフィス（PC・大画面・計画/分析/管理）**。
- **地図/GIS**（4.1／ADR-0010）：圃場ポリゴン描画・オフラインタイル。
- **多言語・多地域**（ADR-0015）：全世界の言語、圃場ローカル暦日、通貨/単位。
- **配信＝MITのOSS**（1.3）：運用者が自前ホスト可能な構成。
- **大量データ**：圃場数万・位置ログ多数を端末側に持ち得る（同期＝ADR-0007）。

これらを、**単一コードベースで保守可能**かつ**現場のオフライン要件を破綻させない**形にする。

## 2. 決定（Decision）

**React＋TypeScript の単一SPAを、PWA（Service Worker）としてオフラインファーストで構築する。** モバイル（現場）とデスクトップ（オフィス）を**レスポンシブな単一コードベース**で提供し、必要な端末機能が満たせない場合に限り**同一コードを薄い native ラッパ（Capacitor）で包む**フォールバックを用意する。

- **言語/型**：**TypeScript**（型安全・大規模保守）。
- **UIライブラリ**：**React**（要求仕様たたき台・エコシステム・PWA/地図/i18nライブラリの充実）。具体のコンポーネント体系は **ADR-0016（UXデザインシステム）** で確定。
- **レンダリング＝クライアントサイドSPA**：オフライン起動が必須のため、**アプリシェルを端末にキャッシュして起動をサーバ非依存にする**。SSR/SSGは初期表示に有利だが、**オフライン起動・端末ローカルデータ駆動**の要件と噛み合わないため主軸にしない（マーケ用公開ページ等が必要なら別建て）。
- **PWA**：
  - **Service Worker**（**Workbox** で生成）＝アプリシェル（HTML/JS/CSS）と静的資産の**プリキャッシュ**、実行時キャッシュ戦略（地図タイル等）。
  - **インストール可能**（ホーム画面追加）。オフラインでも起動・入力可能。
  - **更新戦略**：SWの更新は**明示的な更新通知＋リロード**（勝手に画面が変わらない）。破壊的スキーマ変更時のバージョン整合はADR-0007と連携。
- **オフラインファーストのデータ層**：
  - 端末ローカルの一次ストア＝**IndexedDB**（作業日誌・在庫イベント・位置ログ・マスタキャッシュ・**農薬使用基準の事前キャッシュ**）。UIは**常にローカルストアを読む**（オンライン/オフラインでコードパスを分けない）。
  - サーバとの**差分同期は ADR-0007** に委譲。フロントは「ローカルに書く→同期エンジンが送る」を前提に設計（楽観的更新）。冪等キー＝**UUIDv7**（データモデル設計書§5：`event_uuid`）をクライアントで採番し、`event_ts` も `uuidv7_time` 相当をクライアントで算出して送る（スパイクS1で確定した経路）。
- **地図**：**MapLibre GL JS**（ADR-0010）。オフラインタイルは対象国分＋端末は圃場周辺のみ（ADR-0011）。
- **国際化**：**ICU MessageFormat（CLDR）** ベースのi18n（ADR-0015）。日付は「時点=UTC表示変換／暦日=圃場TZ」をフロントでも遵守。
- **状態管理**：サーバ状態は**クエリキャッシュ（TanStack Query 等）**、ローカル永続は IndexedDB、UI状態は軽量ストア。**「サーバ状態・永続ローカル状態・UI状態」を層として分離**する。
- **ビルド/ツール**：**Vite**（高速・PWAプラグイン）。E2E/単体テストの土台も用意（ADR-0021）。
- **アクセシビリティ/現場UX**：大きいタップ標的・高コントラスト・片手操作・オフライン状態の明示（F-D系）。**性能予算**（初期表示・入力レイテンシ）を定義し計測（ADR-0020）。
- **native ラッパのフォールバック（重要）**：**iOS Safari の PWA 制約**（バックグラウンド位置取得が実質不可、ストレージのエビクション、Background Sync API 非対応、プッシュ制約）により、**F-08a の「作業中の継続的な位置ログ」等がブラウザPWAだけでは満たせない可能性**がある。その場合は**同一Reactコードを Capacitor で薄くネイティブ化**し、位置情報・バックグラウンド・ストレージ永続をネイティブ機能で補う。**単一コードベースを維持**したまま端末能力差を吸収する。どこまでをPWAで賄い、どこからラッパが要るかは**端末能力スパイクで確定**する（下記・未解決）。

## 3. 検討した選択肢（Options）

| 観点 | 選択肢 | 評価 |
|---|---|---|
| 配布形態 | **PWA（採用）** ／ フルネイティブ（iOS/Android別） ／ React Native | ◎ PWAは単一コード・自前ホスト（MIT）・即時配信。フルネイティブは現場/オフィス2文脈×2OSで工数肥大。RNは現場のネイティブ機能に強いがWeb（オフィス大画面）と二重管理になりやすい |
| ラッパ | **Capacitor フォールバック（採用）** ／ ラッパなし ／ 最初からRN | ◎ 同一Reactコードを必要時のみネイティブ化。ラッパ無しだとiOS制約でF-08aが破綻。最初からRNはオフィスWebと乖離 |
| レンダリング | **CSR/SPA（採用）** ／ SSR（Next.js等） | ◎ オフライン起動・ローカルデータ駆動にはCSRが自然。SSRはサーバ依存で圏外起動と相性が悪い |
| 言語 | **TypeScript（採用）** ／ JavaScript | ◎ 大規模・多人数・長期保守で型が効く |
| ローカル永続 | **IndexedDB（採用）** ／ localStorage ／ SQLite(wasm) | ◎ IndexedDBは大容量・構造化・非同期。localStorageは容量/同期API不足。SQLite-wasmは有力だがまず標準のIndexedDBで、必要ならADR-0007で再評価 |
| 状態 | **層分離（サーバ/永続/UI）（採用）** ／ 単一グローバルストアに全部 | ◎ オフライン同期を単一ストアに混ぜると複雑化。関心を分離 |

**却下理由**：フルネイティブ2OSは工数と二重保守。純SSRはオフライン起動に不向き。ラッパ無しPWAはiOSの位置ログ制約で現場要件（F-08a）を落とす。

## 4. 影響・帰結（Consequences）

**良い影響**
- 単一コードベースで現場/オフィス両文脈をカバー。自前ホスト（MIT）に適合。
- オフラインファーストの一貫したデータパスで、圏外/圏内のコード分岐を排除しバグ面を縮小。
- スパイクS1で確定した冪等（UUIDv7＋CHECK）とフロントの採番が一致し、同期の芯がぶれない。

**悪い影響・制約**
- **iOS PWA制約**が F-08a（継続位置ログ）等の現場要件を直撃し、Capacitorラッパが実質必須化する可能性（＝「純PWA」では終わらない）。
- **端末ストレージ制約/エビクション**：大量の圃場・タイル・イベントを端末に持つと容量・退避が問題（ADR-0011のタイル戦略・ADR-0007の保持方針と接合）。
- **オフライン同期の複雑性**はADR-0007へ集約されるが、フロントの楽観的更新・競合表示（5.1.1の競合ルール）はUX設計（ADR-0016）と密結合。
- SPA初期ロードのサイズ管理（コード分割・地図/i18nの遅延読込）が性能予算に効く。

## 5. 未解決事項（設計/スパイクで詰める）

- **端末能力スパイク（新規 S6）**：iOS/Android の PWA で **バックグラウンド位置取得・Background Sync・ストレージ永続（persist()）・エビクション閾値** を実測し、**PWAで賄える範囲とCapacitorラッパが要る範囲**を確定する（F-08a/5.1.1に直結）。
- IndexedDB のデータ量・クエリ性能（数万件のローカル圃場/イベント）の実測（ADR-0007のローカルスキーマと一体）。
- 楽観的更新と競合表示（管理者裁定＝5.1.1 M-5）のUXパターン（ADR-0016）。
- SW更新とローカルスキーマ・マイグレーションの整合（ADR-0007）。
- コンポーネント/デザイントークンの具体（ADR-0016）。
- 認証トークンのフロント保持方式（メモリ/Cookie・オフライン時の扱い）（ADR-0009）。
