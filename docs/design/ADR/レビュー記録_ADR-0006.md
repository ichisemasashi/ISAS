# ADR-0006 敵対的レビュー記録票

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0006：フロントエンド構成＝React＋TypeScript＋PWA](ADR-0006-フロントエンド構成-React-PWA.md) |
| レビュー種別 | 敵対的レビュー（この決定のまま実装すると破綻・矛盾・安全/法令リスクになる箇所の摘出） |
| レビュー日 | 2026-07-20 |
| レビュアー | Claude (Opus 4.8) |
| 指摘件数（累計） | 第1回10件（High 3／Medium 4／Low 3）＝**全件対応済**（v2反映）／第2回7件（High 1／Medium 4／Low 2）＝**未処置**。累計17件。 |
| 総括 | React＋PWAの方向は妥当だが、**「PWA中心」という枠組みが、現場の最重要要件（F-08a継続位置ログ・F-41オフライン安全警告）と配布モデル（MIT自前ホスト）の接合部で緊張を生む**。特に(1)Capacitorラッパが実質必須化するとApp Store配布が要り「運用者が自前ホスト」の前提が崩れる（H1）、(2)オフライン安全警告が端末キャッシュの陳腐化で**法令違反の散布を許し得る**（H2）、(3)iOSのストレージ・エビクションで**未同期データが消失**し得る（H3）。いずれも「PWAで賄える範囲」を楽観視した結果で、**端末能力スパイクS6の前に方針の条件分岐を明文化**する必要がある。 |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

---

## サマリ

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A6-H1 | 🟠 High | **MIT自前ホスト ↔ Capacitorネイティブ配布の矛盾**。iOS制約でCapacitorラッパが実質必須化すると、App Store配布・コード署名・開発者アカウントが要り、「運用者が自前ホストで配布」（1.3）が成り立たない。かつラッパ必須なら「PWA中心」の前提自体が崩れる | 対応済 |
| A6-H2 | 🟠 High | **オフライン安全警告の陳腐化リスク**。F-41（農薬使用基準）の事前警告を端末キャッシュで行うが、キャッシュした安全/法令ルールが古いと**違法な散布を許可**し得る。安全criticalなオフラインデータの鮮度・失効・fail-closed方針が未定義（ADR-0015の「安全ラベルはレビュー済みのみ」と接合していない） | 対応済 |
| A6-H3 | 🟠 High | **未同期データの消失リスク**。IndexedDB採用の帰結として、iOSのストレージ・エビクション（容量圧迫/長期未使用）で**未送信の作業記録・在庫・位置ログが消える**。一日オフライン入力後の喪失は現場の致命傷。ADRはエビクションを「悪い影響」に挙げるのみで、データ喪失の重大度・保護策が不足 | 対応済 |
| A6-M1 | 🟡 Medium | **オフラインファーストの過剰一般化**。「UIは常にローカルストアを読む」を全文脈へ適用すると、オフィスの分析/一覧（オンライン・数万件）まで IndexedDB 経由に強制され非現実的。offline-firstの適用範囲を**現場の運用サブセット**に限定すべき | 対応済 |
| A6-M2 | 🟡 Medium | **読取のsource-of-truthが曖昧**。「UIは常にローカルストア（IndexedDB）を読む」と「サーバ状態はクエリキャッシュ（TanStack Query）」が併記され、どちらが真かが未定義。二重キャッシュの整合ルールがない | 対応済 |
| A6-M3 | 🟡 Medium | **継続位置ログの実現性が未定義**。バッテリ消費・データ量（IndexedDB）と、「作業中ON/休憩中OFF」の**状態検出手段**（手動/打刻連動/ジオフェンス）が示されず、F-08aの運用が絵に描いた餅になり得る | 対応済 |
| A6-M4 | 🟡 Medium | **単一バンドルの肥大**。現場スマホがオフィス/分析コードまでDLする。コード分割・遅延読込が「言及」止まりで、性能予算（初期表示）との接合が弱い | 対応済 |
| A6-L1 | 🟢 Low | **React採用の根拠が弱い**。「要求仕様たたき台」への追認が主で、Vue/Svelte等との技術比較（PWA/地図/i18n/オフライン適合）が薄い | 対応済 |
| A6-L2 | 🟢 Low | **対応ブラウザ/OSの下限マトリクスが未定義**（iOS Safari の最低版・Android WebView 等）。制約（Background Sync等）は版依存のため前提の明示が要る | 対応済 |
| A6-L3 | 🟢 Low | **SW更新時の入力中データ保全**が未記載。「更新通知→リロード」でオフライン入力中の未確定データが飛ばないか | 対応済 |

---

## 🟠 High

### A6-H1. MIT自前ホスト ↔ Capacitorネイティブ配布の矛盾
- **該当箇所**：決定「PWA中心＋Capacitorフォールバック」／背景「配信＝MITのOSS・運用者が自前ホスト」（1.3）
- **指摘（攻撃）**：ADRは「PWAは自前ホスト（MIT）に適合」を長所に挙げつつ、iOS制約への対策として**Capacitorラッパ**を用意する。しかし **Capacitorのネイティブアプリ（特にiOS）は App Store 配布・コード署名・Apple Developer 登録（有償）・審査が必須**で、**運用者が自分のサーバに置くだけでは配れない**。もし F-08a（継続位置ログ）等でラッパが**実質必須**なら、「PWAで自前ホスト」という配布の前提が現場アプリで崩れる。つまり**「PWA中心」は条件付き**であり、その条件（どの要件でラッパが要るか）を先に確定しないと、配布戦略・OSSとしての提供形態が宙に浮く。
- **影響**：OSS運用者が現場アプリを配布できない／各運用者が自前で署名・審査・配布を負う運用コスト。プロジェクトの提供形態（1.3）に直結。
- **要修正（案）**：**配布の条件分岐を明記**する：(a) **PWAで賄える機能範囲**（オフィス全般＋現場のフォアグラウンド操作）は自前ホストPWAで提供、(b) **バックグラウンド位置ログ等ネイティブ必須機能**が要る運用者向けに、**公式が署名済みネイティブアプリ（またはビルド手順）を別チャネルで提供**（App Store 公式配布 or MDM/エンタープライズ配布 or セルフビルド）。「PWA中心」を「**PWAを既定、ネイティブは能力が要る場合のオプション**」と正確に言い換え、S6で必須範囲を確定してから配布方式を決める、と未解決に接続。

### A6-H2. オフライン安全警告の陳腐化（違法散布を許すリスク）
- **該当箇所**：決定「農薬使用基準の事前キャッシュ」「オフライン中も事前警告（F-41）」／ADR-0015「安全ラベルはレビュー済みのみ表示」
- **指摘（攻撃）**：F-41の農薬使用基準（登録失効・使用回数・収穫前日数・希釈上限等）を**端末キャッシュ**で事前警告する設計だが、**キャッシュが古いと危険**：登録が取り消された薬剤・改定された基準を**「OK」と誤警告**し、**違法・危険な散布を許可**し得る。安全/法令に関わるデータは、鮮度・失効・オフライン時のfail-close（不確実なら警告を出す/操作を保留）方針が要る。ADR-0015は「安全ラベルはネイティブレビュー済みのみ表示」と保守的に決めたのに、本ADRのオフラインキャッシュはその保守性と接合していない。
- **影響**：法令違反・健康/環境被害・トレーサビリティの信頼失墜。要求仕様の中核（安全な営農支援）を毀損。
- **要修正（案）**：安全criticalなオフラインデータに**鮮度契約**を持たせる：(a) キャッシュに**基準の版・有効期限・最終同期時刻**を持ち、**期限切れ/長期未同期時は「基準が古い可能性」を明示し、判定をfail-safe側**（警告を強める/確定を保留し要オンライン確認）にする。(b) **登録失効・回収は優先同期**（オンライン復帰時に最優先で反映）。(c) オフライン警告は「参考」、**最終確定はオンライン再検証**を要する操作（機微操作）と位置づける。ADR-0015/ADR-0007と接合。

### A6-H3. 未同期データの消失（ストレージ・エビクション）
- **該当箇所**：決定「一次ストア＝IndexedDB」／影響「端末ストレージ制約/エビクション」
- **指摘（攻撃）**：IndexedDBは**ブラウザのストレージ退避（eviction）**の対象で、特に **iOS Safari は未使用・容量圧迫時にデータを消す**（`navigator.storage.persist()` の効きも限定的）。現場が**一日圏外で入力→未同期のまま端末退避が起きると、作業記録・在庫・位置ログが喪失**する。ADRはこれを「悪い影響」に1行挙げるだけで、**「未同期の書込は消えてはいけない」という不変条件**への保護が設計判断に昇格していない。
- **影響**：現場の記録喪失＝法定記録（農薬）・在庫・労務の欠落。オフラインファーストの信頼が根底から崩れる。
- **要修正（案）**：(a) **`persist()` 要求＋インストールPWA/ネイティブで永続性を確保**（S6で各OSの実効性を実測）。(b) **未同期キューは最優先で早期同期**（接続が一瞬でも回復したら送る）＋送信までの**冗長化**（可能なら別ストレージ/エクスポート）。(c) **未同期件数・最終同期時刻をUIで常時可視化**し、長期未同期を警告（F-D系）。(d) エビクション耐性が不足する運用者向けはネイティブ（H1と連動）。データモデルの冪等（UUIDv7）は重複を防ぐが**消失は防げない**点を明記し、ADR-0007の保持責務と接合。

## 🟡 Medium

### A6-M1. オフラインファーストの過剰一般化
- **該当箇所**：決定「UIは常にローカルストアを読む（オンライン/オフラインでコードパスを分けない）」
- **指摘（攻撃）**：現場の素早い入力にはローカルファーストが正しいが、**オフィスの計画・分析・一覧（数万圃場・集計・帳票）**は**オンライン前提・サーバ集計**が自然で、これらを**全てIndexedDB経由**にすると、端末への大量ミラー리ング・集計のクライアント再実装で破綻する。「全文脈でローカルストアを読む」は過剰。
- **要修正（案）**：offline-firstの**適用範囲を「現場の運用データ（日誌・在庫・打刻・位置ログ・安全基準参照）」に限定**し、**オフィスの分析/一覧はオンライン・サーバ集計（通常のクエリ）**と明記。二層（オフライン運用UI／オンライン分析UI）を役割分担し、共有はコンポーネント/デザインに留める。

### A6-M2. 読取の source-of-truth が曖昧
- **該当箇所**：決定「UIは常にローカルストア（IndexedDB）を読む」＋「サーバ状態はクエリキャッシュ（TanStack Query）」
- **指摘（攻撃）**：2つのキャッシュ（IndexedDB と Query キャッシュ）が併記され、**読取の真実源がどちらか未定義**。オフライン運用データはIndexedDBが真、オンライン分析はサーバ/Queryが真、のはずだが、混在時の整合（楽観的更新の反映先・再取得の優先）が決まっていない。
- **要修正（案）**：M1の二層に対応させ、**運用データ＝IndexedDBが真実源（Queryは同期エンジン越しにIndexedDBを更新）**、**分析データ＝サーバが真実源（Queryキャッシュは揮発）**、と**データ種別ごとに真実源を固定**して明記。ADR-0007のローカルスキーマと一体で確定。

### A6-M3. 継続位置ログの実現性（電力・状態検出）
- **該当箇所**：決定「位置ログ…UIは常にローカルへ」／要求 F-08a「作業中ON/休憩中OFF」
- **指摘（攻撃）**：継続的なGPS取得は**バッテリ消費**が大きく、**IndexedDBへの高頻度書込**でデータも嵩む。さらに「作業中/休憩中」を**どう判定してON/OFFするか**（手動トグル/打刻連動/ジオフェンス/加速度）が未定義で、運用として成立しない恐れ。
- **要修正（案）**：**打刻（F-74）連動を既定**（作業開始打刻→位置ログON、休憩/終了→OFF）とし、取得間隔を**適応的**（移動時のみ密、静止時は疎）に。バッテリ/データ量の目安をS6で実測。ネイティブ時はバックグラウンド最適化を利用（H1連動）。

### A6-M4. 単一バンドルの肥大
- **該当箇所**：決定「レスポンシブな単一コードベース」／影響「SPA初期ロードのサイズ管理」
- **指摘（攻撃）**：現場スマホが**オフィス/分析/地図/i18n全部入りバンドル**をDLすると、初期表示・低速回線でUX目標（初期表示SLO）を割る。コード分割は「言及」のみ。
- **要修正（案）**：**ルート/文脈単位のコード分割（現場UI／オフィスUI）**、地図・i18n・分析の**遅延読込**、性能予算（初期JS上限）を定義し計測（ADR-0020）。現場UIは最小構成で起動、と明記。

## 🟢 Low

### A6-L1. React採用の技術的根拠が弱い
- **該当箇所**：決定「UIライブラリ＝React」「要求仕様たたき台・エコシステム」
- **指摘**：採用理由が「たたき台への追認＋エコシステム」に寄り、Vue/Svelte/SolidJS 等との**PWA/オフライン/地図/i18n適合・バンドルサイズ・学習性**の比較が薄い。ADRは代替の却下理由を残す文書。
- **要修正（案）**：主要代替を1–2行ずつ比較（例：エコシステム/採用実績/地図・i18nライブラリの成熟でReactを採る）を追記。

### A6-L2. 対応ブラウザ/OS下限マトリクス未定義
- **該当箇所**：全体
- **指摘**：Background Sync・persist()・Web Push の可否は**OS/ブラウザ版に依存**するのに、サポート下限（iOS Safari最低版・Android・デスクトップ）が未定義で、制約議論の前提が曖昧。
- **要修正（案）**：対応下限マトリクスを定義し、S6の実測対象に含める。

### A6-L3. SW更新時の入力中データ保全
- **該当箇所**：決定「SW更新は明示的な更新通知＋リロード」
- **指摘**：更新リロード時に**オフライン入力中の未確定フォーム/下書き**が失われないか（下書き＝データモデル④はローカル保持だが、UI状態の保全は別）。
- **要修正（案）**：更新適用は**安全なタイミング（未確定入力なし）に限る**か、下書きをIndexedDB（④）へ退避してからリロード、と明記。

---

## 所見（メタ）

- **「PWA中心」の楽観が3つのHighを生んだ**：iOS/端末の現実（配布H1・安全鮮度H2・データ消失H3）は、要求仕様の中核（現場オフライン・安全・法定記録）に直撃する。**端末能力スパイクS6の前に、要件×端末能力の条件分岐（どこまでPWA/どこからネイティブ、安全データのfail-safe、未同期の保護）を先に言語化**すべき。
- **ADR-0007（オフライン同期）との強い接合**：H2/H3/M1/M2は同期・保持の設計と不可分。ADR-0006単独で閉じず、**0006↔0007を接合部でレビュー**するのが有効（P0で得た教訓の再適用）。
- 収束方針：H3件を「条件分岐の明文化＋S6での実測前提」に落とし、M/Lを反映後、第2回で新たな接合部矛盾（特に配布・安全・同期）を確認する。

---

# 第2回（ADR-0006 v2 に対する敵対的再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0006 v2](ADR-0006-フロントエンド構成-React-PWA.md)（第1回10件を反映後） |
| レビュー日 | 2026-07-20 |
| 指摘件数（第2回） | 計7件（High 1／Medium 4／Low 2）。**未処置**（提案提示のみ） |
| 総括 | 第1回の是正は妥当で、High 3→1に低下。ただし**是正が新たな接合部を開いた（振り子）**：(1) H1の「公式が署名済みネイティブアプリを提供」は、**中央publisherを持たない自前ホストOSS**と整合せず、アプリ↔バックエンド結合が未定義（R2-H1）。(2) M1の二層（運用/分析）は**実務で境界が重なる**画面・**共有マスタの居場所**が未整理（R2-M1/M2）。(3) H2のfail-safeは**長期オフライン圃場で運用を殺す**（R2-M3）。(4) **オフライン認証継続**が未定義（R2-M4）。いずれも「配布・データ層・オフライン運用の現実」に踏み込むと出る、局所化した緊張。ADR-0007/0009/0005との接合を明記して閉じる。 |

## サマリ（第2回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| R2-H1 | 🟠 High | H1是正の「公式が署名済みネイティブアプリを別チャネルで提供」は、**中央publisherを持たない自前ホストOSS**と矛盾。ネイティブアプリ↔各運用者の自前バックエンドの**結合（設定可能backend URL／各運用者の自己署名公開）**が未定義で、配布の実体が宙に浮く | 未対応 |
| R2-M1 | 🟡 Medium | 二層（運用/分析）の**境界が実務で重なる**：現場が履歴閲覧、管理者が現場入力、「今日の入力＋サーバ傾向」の混在画面など。境界の判定基準・両方要る画面の扱いが未定義 | 未対応 |
| R2-M2 | 🟡 Medium | **共有マスタ/参照データ**（agro_chemical・作物）が両層で必要なのに、二層モデルに**参照データの居場所と鮮度契約**（H2）が未整理 | 未対応 |
| R2-M3 | 🟡 Medium | H2のfail-safeが**長期オフライン圃場で運用を殺す**：数日圏外の海外/僻地で毎回「基準が古い」警告/保留になり、オフライン運用の意味が消える | 未対応 |
| R2-M4 | 🟡 Medium | **オフライン認証継続が未定義**：トークン失効後も現場は使えねばならないが、ADR-0005の短TTL権限キャッシュ・ADR-0009の短命トークンと衝突。0006がオフライン運用の認証要件を示していない | 未対応 |
| R2-L1 | 🟢 Low | H3の「別ストレージへ冗長化」は**同一端末では端末全体のeviction/紛失に無力**。実質的保護は早期同期＋ネイティブ永続で、冗長化の効果を過大評価 | 未対応 |
| R2-L2 | 🟢 Low | 位置ログ打刻連動でも**打刻忘れで無記録**・iOSバックグラウンド依存は残る。打刻忘れ検知と背景実行のネイティブ依存が未記載 | 未対応 |

## 🟠 High（第2回）

### R2-H1. 「公式署名アプリ」が自前ホストOSSと整合しない（配布の実体未定義）
- **該当箇所**：v2 決定「ネイティブは…公式が署名済みアプリを別チャネルで提供 or セルフビルド or MDM」
- **指摘（攻撃）**：H1の是正で「**公式が署名済みネイティブアプリを提供**」と書いたが、本プロジェクトは**MITのOSSで各運用者が自前ホスト**（1.3）。すなわち**中央の「公式publisher」も、単一のバックエンドURLも存在しない**。ネイティブアプリは通常**特定バックエンドに結びつく**ため、「公式アプリ1本」では各運用者の自前サーバに接続できない。「公式が提供」は**誰が・どのバックエンド向けに・どの鍵で署名して配るのか**が未定義で、配布の実体が宙に浮く（H1を言い換えただけで解けていない）。
- **影響**：ネイティブ必須機能（F-08a背景位置ログ）が要る運用者の配布経路が実装不能。OSS提供形態の根幹。
- **要修正（案）**：ネイティブ配布モデルを**具体化して選択肢を提示**する：
  - **(a) 設定可能backendの汎用アプリ**：公式が「**接続先バックエンドURLを実行時設定できる**汎用クライアント」を1本ストア公開（各運用者はサーバだけ用意）。※ストア審査・ブランド・任意URL接続のセキュリティ（許可リスト/検証）を要検討。
  - **(b) 運用者による自己署名公開**：各運用者が**フォークして自分の開発者アカウントで署名・配布**（MDM/エンタープライズ/ストア）。公式は**ビルド手順とCIテンプレを提供**。※運用者負担大だが自律的。
  - **(c) 併用**：小規模は(a)、大規模/規制の厳しい運用者は(b)。
  - **S6で必須範囲を確定した後にこの配布モデルを決定**し、鍵管理・審査・任意backend接続のセキュリティをADR-0017（セキュリティ）と接合、と明記。

## 🟡 Medium（第2回）

### R2-M1. 二層（運用/分析）の境界の重なり
- **該当箇所**：v2 §2.0 二層表
- **指摘（攻撃）**：「運用UI（offline）／分析UI（online）」は概念として明快だが、**実務の画面は重なる**：現場作業者が過去の作業履歴を見る、管理者が現場で入力する、ダッシュボードが「今日の未同期入力＋サーバ集計傾向」を並べる、等。**画面単位で二分できない**ため、境界の判定基準が要る。
- **要修正（案）**：層は**画面単位でなくデータ種別単位**で決めると明記：**書込みを伴うオフライン運用データ＝IndexedDB層**、**読取専用の集計/大量一覧＝サーバ層**。混在画面は**両ソースを別々に読んで合成**（運用データはIndexedDBから、集計はサーバから）し、オフライン時は集計部分を「オフラインにつき最新不可」と縮退表示する、と規定。

### R2-M2. 共有マスタ/参照データの居場所
- **該当箇所**：v2 §2.0／決定「マスタキャッシュ」「安全基準の鮮度契約」
- **指摘（攻撃）**：agro_chemical（③共有+上書き）・作物マスタ等の**参照データは運用（オフライン安全チェック）でも分析でも要る**。二層モデルは運用/分析を分けたが、**両方が参照する共有データの居場所・鮮度**が未整理。
- **要修正（案）**：参照データを**第3の区分「共有参照キャッシュ」**として明示：**オフライン運用のためIndexedDBに版・鮮度契約つきでキャッシュ（H2の安全基準もここ）**、分析はサーバ参照。参照データの更新は優先同期対象。データモデルの③（agro_chemical_effective）と対応づける。

### R2-M3. 長期オフラインでのfail-safeが運用を殺す
- **該当箇所**：v2 決定「安全criticalデータの鮮度契約…期限切れはfail-safe（警告強化/確定保留）」
- **指摘（攻撃）**：fail-safeは正しいが、**海外/僻地で数日〜数週間圏外**が常態の圃場では、安全基準キャッシュが期限切れになり**毎回の散布で警告/保留**が出て、オフライン運用そのものが機能しなくなる（要求の中核＝圏外運用を否定）。
- **要修正（案）**：**オフライン有効期間を運用者が設定可能**にし、**期間内は通常運用**、**超過時のみ強い警告＋明示的承認（「基準が古い可能性を了解して実施」を記録＝監査）で継続可能**にする。安全と運用継続のバランスを運用者の法域/実態に委ねる（E系と接合）。「保留＝実施不能」を既定にしない。

### R2-M4. オフライン認証継続が未定義
- **該当箇所**：v2 §5「認証トークンのフロント保持（0009）」のみ
- **指摘（攻撃）**：現場は**トークン失効後もオフラインで使えねばならない**（数日圏外）。しかしADR-0005は短TTL権限キャッシュ、ADR-0009は短命トークンを指向し、**オフライン運用と衝突**。0006は「オフライン時どうログイン状態/権限を保つか」の要件を示していない。
- **要修正（案）**：0006の要件として明記：**オフラインは「最後に検証したセッション＋権限スナップショット」で運用継続**（読み書きはローカル、機微操作は不可）、**オンライン復帰時に再認証・再検証**し、失効していれば未同期分の送信のみ許して以降をロック。具体はADR-0009/0005で確定（オフライン猶予期間・スナップショットの署名等）。

## 🟢 Low（第2回）

### R2-L1. 「別ストレージ冗長化」の過大評価
- **該当箇所**：v2 決定 H3対応「(b) 送信までの冗長化（別ストレージ/エクスポート）」
- **指摘**：同一端末内の別ストレージは、**端末全体のeviction・端末紛失/故障には無力**。実質的な保護は**早期同期＋ネイティブ永続**。
- **要修正（案）**：「別ストレージ冗長化」の記述を弱め、**保護の主軸は早期同期・persist()・ネイティブ**と明記。任意エクスポートは補助に留める。

### R2-L2. 打刻連動の打刻忘れ・背景依存
- **該当箇所**：v2 決定「位置ログは打刻連動」
- **指摘**：打刻連動でも**打刻忘れで無記録**、かつON後の継続取得は**iOSバックグラウンド実行に依存**（H1/H3の制約が残る）。
- **要修正（案）**：**打刻忘れ検知**（作業中のはずが位置更新なし→リマインド）を追加し、背景実行はS6/ネイティブ依存であることを明記（過度に「打刻連動で解決」と読ませない）。

## 所見（メタ・第2回）

- **是正が現実の一段深部を開いた**：第1回のH1（配布）・H2（安全鮮度）・M1（二層）の是正が、それぞれ**配布の実体（R2-H1）・長期オフラインの実運用（R2-M3）・二層の境界と共有データ（R2-M1/M2）**という「実装/運用の現実」に踏み込む緊張を開いた。High 3→1で収束方向。
- **0006は単独で閉じない**：R2-H1→0017、R2-M3→E系、R2-M4→0009/0005、参照データ→データモデル③、と**他ADRとの接合の明記で閉じる**性質。P0の教訓（接合部レビュー）を継続適用。
- 収束方針：R2-H1（配布モデルの具体化）を最優先、M1〜M4を各接合先へ接続して是正後、第3回でHigh 0を確認する。
