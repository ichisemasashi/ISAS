# ADR-0002 敵対的レビュー記録票

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002：配備モデル＝1システム＝1DB＝原則1国](ADR-0002-配備モデル-1DB-1国.md) |
| レビュー種別 | 敵対的レビュー（この決定のまま実装すると破綻・矛盾する箇所の摘出） |
| レビュー日 | 2026-07-17 |
| レビュアー | Claude (Opus 4.8) |
| 指摘件数（累計） | 第1〜4回＝計17件（High 1／Medium 8／Low 8）を全件処置（v5）。**第5回＝8件（High 2／Medium 3／Low 3）＝全件処置（v7）**。**第6回＝7件（High 1／Medium 3／Low 3）＝全件処置（v8）**。**第7回＝7件（High 1／Medium 3／Low 3）＝全件処置（v9）**。**第8回＝7件（High 1／Medium 3／Low 3）＝全件処置（v10）**／**第9回＝独立レビュー15件（High 4／Medium 7／Low 4）＝未処置**。累計61件 |
| 総括 | 第1〜4回でHigh 1→0を3回連続維持し **v5でクローズ**した（第1回：レジデンシーの全データ経路化・ADR-0001接合／第2〜3回：中央ID×レジデンシー・1国複数DB×単一DB横断・中央IDのSPOF/任意化／第4回：上限超グループのシャードルーティング）。**その後 v6 で ADR-0008 独立レビュー IND-H1 の波及として A2e-M1（法域ローカル user→shard 索引）を追記したが、この追記は本記録票のどの回でも攻撃されていなかった**。第5回でこれを埋めた結果、**High 2 を含む8件を検出＝ADR-0002 は再オープン**（下記 第5回）。 |
| ⚠️ 再オープン中 | **v6 の A2e-M1 追記に対する第5回レビューで High 2 を検出**（判断そのものではなく「4行の追記が本文全体に行き渡っていない」ことに起因）→ **v7で全件処置**。しかし **v7 に対する第6回レビューで再び High 1（A2g-H1）を検出**——今度は**新旧の齟齬ではなく「v7で同時に足した2つの処置どうしの接合」**（全シャード複製×加入は同期＝分散トランザクション）。**v8で全件処置済**。**しかし第7回レビューで再び High 1（A2h-H1）**：v8 の整理「**索引は性能最適化であって正しさの必須要素ではない**」を突き詰めると**索引テーブル自体が不要**（各シャードの①メンバーシップから導出でき、A1e-M1 の既存の扱いで足る）。それなのに (iii) を採ったことで **ADR-0001（P0土台）に「RLSの防波堤の外」という恒久的な例外を新設させた**（v8→v9→v10 の3版）。**v9で全件処置済**（索引を「導出＋キャッシュ」へ格下げし §3.2 を作り直し、**ADR-0001 の枠を条件付きへ緩めて対象0件にした**＝P0土台の健全性が回復）。**しかし第8回レビューで再び High 1（A2i-H1）**：**導出に切り替えたことで「対象シャード集合を知る手段」を失い、停止シャードが対象集合に現れないため complete が常に真になる**（給与の過少計上が `complete` として確定＝完全性フラグの仕組みが根から無効化）。**v10で全件処置済**。**しかし第9回＝独立レビューで High 4 を検出**（8巡の自己レビューが到達できなかった角度：**FORCE RLS 下で SECURITY DEFINER は①を読めない＝既定方式が実装不能**／**restrictive の AND 合成により横断が単一DBでも成立しない＝base case 未成立**／completeの対象集合固定が可用性のANDを作った／シャード分割移行と監査チェーンの非互換）。**設計者が中核2件を検証し、いずれも正しいことを確認**。**クローズ不可。ADR-0001・データモデル設計書（M3・クローズ済み）の再オープンを要する。** |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

> 以下は各回の詳細記録（第1回6件・第2回4件・第3回4件・第4回3件、いずれも全件対応済）。

> 所見：配備モデル（1DB=1国）の方向性は妥当。ただし**「データが国内に留まる」という核心的な主張が、DB本体だけを見た主張で、バックアップ/レプリカ/ログ/CDN/監視等の“周辺データ経路”を含めていない**（データレジデンシーの穴）。加えて、複数国ユーザーの認証（ADR-0001でクローズしたブートストラップと衝突）・可用性（ADR-0001の1DB前提とスケール）の接合に論点がある。

---

## サマリ

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2-H1 | 🟠 High | 「データが国内に留まる」がDB本体のみを指し、バックアップ/リードレプリカ/ログ/監視/CDN等の周辺データ経路が国外に出る穴 | **対応済**（v2：全データ経路へ拡張） |
| A2-M1 | 🟡 Medium | 複数国ユーザーの認証：1システム=1DBかつRLSブートストラップ（ADR-0001 A1e-M1）が国別に閉じるため、国をまたぐログイン/ID解決の経路が未定義 | **対応済**（v2：中央IDでの二段解決） |
| A2-M2 | 🟡 Medium | 1DB=1国と「5万ユーザー/DB上限・水平スケール」の関係が未整理（1国が上限超なら1国=複数DBが必要で、モデルの前提が崩れる） | **対応済**（v2：1国=複数DB拡張） |
| A2-M3 | 🟡 Medium | システム外DWHの「PIIフリー/仮名化」が推奨止まりで、仮名化の再識別リスク・責務の線引きが弱い（越境の実質的リスク） | **対応済**（v2：既定PIIフリー＋警告） |
| A2-L1 | 🟢 Low | 「原則1国」の例外（隣接小国の同居）を認めると、A案（全国同居）で否定したレジデンシー問題が小規模再発 | **対応済**（v2：同一法域内に限定） |
| A2-L2 | 🟢 Low | 国コード（ISO 3166）と実際の法域（EU/GDPRは国横断、米国は州単位等）の粒度不一致 | **対応済**（v2：法域概念を導入） |

---

## 🟠 High

### A2-H1. 「データが国内に留まる」の範囲がDB本体のみ
- **該当箇所**：決定「データは対象国内に留まる」「1システム＝1DB＝原則1国」
- **指摘（攻撃）**：本ADRの核心的な価値提案は「データが国内に留まりレジデンシーに適合」だが、これは**DB本体（プライマリ）だけを見た主張**。実運用では以下の**周辺データ経路が容易に国外へ出る**：
  - **バックアップ**（オブジェクトストレージのリージョン）／**リードレプリカ・DR**（別リージョン）
  - **ログ・監視・APM・エラートラッキング**（多くはSaaSで海外リージョン）／**メトリクス**
  - **添付ファイル/写真**（オブジェクトストレージ・CDNのエッジ）／**メール・通知**の中継
  - バックエンドの**アプリサーバ配置**・管理アクセス
  これらのどれか一つでも国外に出れば、**「データが国内に留まる」というレジデンシー適合は崩れる**。DBだけ国内でも意味をなさない。
- **影響**：ADRの中核主張（規制適合）が、実装の周辺経路で無効化される。GDPr等で違反になり得る。
- **要修正（案）**：「データが国内に留まる」の**適用範囲を、DB本体だけでなくバックアップ/レプリカ/ログ/監視/添付/CDN/メール等の全データ経路に拡張**して明記する。周辺サービスも国内リージョン/国内完結を要件化し、満たせない場合の扱い（仮名化・国内代替）を運用者責任として定義（ADR-0017/0019/0020と連携）。

## 🟡 Medium

### A2-M1. 複数国ユーザーの認証・ID解決経路
- **該当箇所**：決定「複数国は独立システム」「ユーザーの多重存在」＋ ADR-0001 A1e-M1（ブートストラップは各DB内の特権経路）
- **指摘（攻撃）**：ADR-0001でブートストラップ（ユーザー→テナント解決）は**各DB内の特権経路**に閉じた。しかし本ADRで複数国＝別DBとなると、**あるユーザーがどの国のシステムに属すかを知る「国をまたぐID解決」**が必要になる。これが**どのDBにも閉じない**（どのDBを引くべきか自体が分からない鶏卵）。ADR-0009（認証）に丸投げされているが、1DB=1国モデルとID中央管理の接合が未定義で、複数国ユーザーのログイン導線が破綻しうる。
- **要修正（案）**：国をまたぐID解決（中央のIDディレクトリ or SSOでユーザー→所属国/システムを解決してから各DBのブートストラップへ）の設計方針を、ADR-0009との境界とともに明記する。

### A2-M2. 1DB=1国と規模上限・水平スケールの関係
- **該当箇所**：決定「1システム＝1DB＝原則1国」＋ 要求仕様「1DBあたり5万ユーザー・水平スケール」
- **指摘（攻撃）**：要求仕様は「1DBあたり最大5万ユーザー、全体は増設で水平拡張」。しかし**1国が5万ユーザーを超える**場合、「1DB=1国」だとその国が1DBに収まらない → **1国=複数DBが必要**になり、本ADRの「1国=1DB」前提が崩れる。1国内の複数DB間で、ADR-0001の「横断は単一DB内で完結」も崩れる（同一国の別DBに跨るグループ管理者横断が再び問題化）。
- **要修正（案）**：「1DB=1国」は**上限内の国**に適用し、上限超の国は「1国=複数DB（シャード）＋国内での横断は集約層」で扱う、と拡張規約を明記（ADR-0001のD案ハイブリッドと接続）。

### A2-M3. システム外DWHの仮名化が推奨止まり
- **該当箇所**：決定「システム外集約はPIIフリー/仮名化を推奨」
- **指摘（攻撃）**：国またぎ集計を「システム外DWHで仮名化・集計値のみ」とするが、**「推奨」止まり**で強制力がない。仮名化データも**再識別（少数圃場・希少作物・位置情報の組合せ）**で個人/経営が特定され得るため、越境集約の実質的リスクが残る。責務は運用者としつつも、ソフトが提供するエクスポート機能の既定が緩いと運用者が誤用しやすい。
- **要修正（案）**：エクスポート/集約機能の**既定をPIIフリー**（PII列は既定で除外、含める場合は明示操作＋警告）とし、再識別リスクの注意喚起を機能に組み込む方針を明記（要求仕様のエクスポート第三者データ制約・#13と連携）。

## 🟢 Low

### A2-L1. 「原則1国」の例外がA案の問題を再発させる
- **該当箇所**：決定/未解決「原則1国の例外（隣接小国の同居）」
- **指摘**：極小規模の隣接国を1システムに束ねる例外を認めると、**A案（全国同居）で否定したデータ所在問題が小規模に再発**する。例外の可否は規制次第で、安易に認めると一貫性が崩れる。
- **要修正（案）**：例外は「同一法域（例：EU域内）に限る」等の条件を付し、異なる法域の同居は不可と明記。

### A2-L2. 国コードと法域の粒度不一致
- **該当箇所**：決定「国コード（ISO 3166）で国別プロファイル選択」
- **指摘**：レジデンシーの単位は必ずしも「国」ではない。**EU/GDPRは国横断の法域**、**米国は州・セクタ単位**の規制もある。ISO 3166の国粒度だけでは法域を正しく表現できない場面がある。
- **要修正（案）**：配備単位を「国」ではなく「**法域（jurisdiction）**」と捉え直し、国コードは既定の代理値として扱う、と粒度を明記。

---

## 処置方針（推奨）

1. **A2-H1** は決定に反映必須（「国内に留まる」の範囲を全データ経路へ拡張＝ADRの中核主張を実効化）。
2. **A2-M1〜M3** は接合部の設計方針（国またぎID解決・1国複数DB・DWH既定PIIフリー）を明記。
3. **A2-L1・L2** は例外条件・法域粒度の注記。

> 総括：配備モデル自体は妥当だが、**「データが国内に留まる」を DB 本体だけで主張していた点（A2-H1）が最大の穴**。周辺データ経路まで含めないとレジデンシー適合は名ばかりになる。ADR-0001（ブートストラップ）・要求仕様（5万/DB・水平スケール）との接合（A2-M1/M2）も要明記。これらを反映すれば配備モデルは実効性を持つ。

---

# 第2回レビュー（ADR-0002 v2 に対する再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | ADR-0002 v2（第1回6件を反映した版） |
| レビュー日 | 2026-07-17 |
| 指摘件数 | High 0／Medium 2／Low 2（計4件） |

> 所見：第1回の是正が、**それぞれ新たな緊張を1つずつ生んだ**——中央ID（A2-M1の是正）がレジデンシー（A2-H1）と衝突し、1国=複数DB（A2-M2の是正）がADR-0001の「横断は単一DB内で完結」を国内で破る。いずれも「振り子」で想定内。High相当の破綻はない。

## サマリ（第2回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2b-M1 | 🟡 Medium | 中央IDディレクトリ（A2-M1の是正）が全国ユーザーのPII（誰がどの国か）を1箇所に集約し、A2-H1（データは国内）と衝突 | **対応済**（v3：最小識別子＋フェデレーション） |
| A2b-M2 | 🟡 Medium | 「1国=複数DB＋国内集約層」（A2-M2の是正）が、ADR-0001の「横断は単一DB内で完結」を国内で破り、C-2/M-6同型の問題が再発。集約層の機構が未定義 | **対応済**（v3：グループ単位でシャードを揃える） |
| A2b-L1 | 🟢 Low | 国コード（ISO 3166）と法域が多対多（EUは複数国→1法域、USは1国→複数州）で、「国コードは法域の代理値」では解決しない | **対応済**（v3：法域属性＋マッピング） |
| A2b-L2 | 🟢 Low | 「1システム=1DB=1国」の純粋性が、中央ID・国内集約層という横断コンポーネントの追加で崩れつつある（全体像の明示が必要） | **対応済**（v3：構成要素の全体像を追加） |

## 🟡 Medium（第2回）

### A2b-M1. 中央IDが全国ユーザーPIIを集約しレジデンシーと衝突
- **該当箇所**：決定「国をまたぐID解決は中央のIDディレクトリ/SSO」＋「データは対象国内に留まる（A2-H1）」
- **指摘（攻撃）**：A2-M1の是正で導入した**中央IDディレクトリ**は、その性質上**全国のユーザー（誰が・どの国のシステムに属すか、認証情報）を1箇所に集約**する。これは**まさにADRが避けようとした「国をまたぐPII集約」を、業務データではなくID/認証データで行っている**——A2-H1（データは国内）と正面衝突する。中央IDをどの法域に置くかで、その法域が全ユーザーのPIIを保持する構図になる。
- **要修正（案）**：中央IDが保持する情報を**最小化**する（グローバルに一意な**不透明識別子＋所属システムのルーティング情報のみ**。氏名等のPIIは各国システムに置く）。認証は**フェデレーション**（中央は認証結果の仲介のみ、資格情報本体は各国 or 標準IdP）を検討。中央IDの配置法域とそのレジデンシー扱いを明記する。

### A2b-M2. 1国=複数DBの横断がADR-0001の前提を国内で破る
- **該当箇所**：決定「1国=複数DB（シャード）＋国内集約層で横断」＋ ADR-0001「横断は単一DB内で完結」
- **指摘（攻撃）**：A2-M2の是正で上限超の大国を「1国=複数DB」にすると、**同一国で複数シャードに跨るグループ管理者の横断**が必要になる。ところがADR-0001は「横断は単一DB内で完結」が売りだった（C-2/M-6を回避した理由）。**1国=複数DBはこの前提を国内で破り、シャード横断という同型の問題を再発**させる。「国内集約層で扱う」とあるが、その機構（クエリルータ/フェデレーテッドクエリ/集約サービス）とRLS横断の両立が未定義。
- **要修正（案）**：1国=複数DB時の横断方式（読取専用の集約DB/DWHへ寄せる、フェデレーテッドクエリ、シャードキー設計でグループを同一シャードに寄せる等）を選択肢として提示し、「**大国の分割時はグループ単位でシャードを揃える**（グループが複数シャードに跨らない）」という設計制約を第一候補として明記する。

## 🟢 Low（第2回）

### A2b-L1. 国コードと法域の多対多
- **該当箇所**：決定「配備単位は法域、国コードは代理値」
- **指摘**：国コードと法域は**多対多**。EU（複数国→1つのGDPR法域）、米国（1国→州/セクタ単位の複数法域）。「国コードは法域の代理値」という1:1前提では表現しきれない。
- **要修正（案）**：データモデルに**法域（jurisdiction）属性**を持たせ、国コード→法域のマッピング表で解決する方針を検討事項に追加（ADR-0015 国際化・国別プロファイルと連携）。

### A2b-L2. 純粋な「1DB=1国」から横断コンポーネントへ
- **該当箇所**：決定全体（中央ID・国内集約層の追加）
- **指摘**：v2で中央ID・国内集約層という**国DBに閉じない横断コンポーネント**が加わり、「全部が国内DBの中」という当初の純粋モデルが崩れつつある。どの要素が国内DB内で、どれが横断か、境界が読み手に見えにくい。
- **要修正（案）**：構成要素を「①各国システム（DB内）／②中央ID（横断・最小PII）／③システム外集約DWH（任意・PIIフリー）／④国内集約層（大国シャード時）」に整理した**全体像（簡易構成図/箇条）**をADRに追加する。

## 処置方針（第2回・推奨）

1. **A2b-M1・A2b-M2** は決定の精緻化（中央IDの最小化・配置法域、大国シャード時はグループ単位でシャードを揃える）。
2. **A2b-L1・L2** は法域の多対多・全体像の明示。

> 総括：2回のレビューで High は 1→0。第2回は「第1回の各是正が生んだ新たな緊張」（中央ID×レジデンシー、1国複数DB×単一DB横断）が中心で、いずれも「振り子」の想定内。A2b-M1（中央IDのPII最小化）とA2b-M2（大国シャード時のグループ寄せ）を反映すれば、配備モデルは横断コンポーネントまで含めて整合する。ADR-0001と同様、2〜3回で収束の見込み。

---

# 第3回レビュー（ADR-0002 v3 に対する再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | ADR-0002 v3（第2回4件を反映した版） |
| レビュー日 | 2026-07-17 |
| 指摘件数 | High 0／Medium 2／Low 2（計4件） |

> 所見：v3の是正が、また新たな限界を露呈させた——**中央ID（A2b-M1）は全ログインのSPOFになり、単一国運用（多数派）には不要**（A2c-M2）。**グループ単位シャード（A2b-M2）は単一グループが上限超だと破綻**（A2c-M1）。振り子は続くが振幅は縮小（High 0維持）。A2c-M2（中央IDを任意化）は構成の簡素化に効く重要指摘。

## サマリ（第3回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2c-M1 | 🟡 Medium | グループ単位シャード（A2b-M2）は「1グループ≤1シャード(5万人)」前提。単一グループが上限超だと破綻し、グループ内シャード横断が最大グループで再発 | **対応済**（v4：上限超グループは国内集約層） |
| A2c-M2 | 🟡 Medium | 中央ID（A2b-M1）が全ログインのクリティカルパス＝グローバルSPOF。単一国運用（#13の自社利用等・多数派）には不要で、独立システムの可用性利点を損なう。中央IDは複数国跨ぎ時のみの任意コンポーネントにすべき | **対応済**（v4：任意化＋縮退動作） |
| A2c-L1 | 🟢 Low | フェデレーション認証で、複数国で働く個人が「1認証で複数国システムへ」か「国ごと別資格情報」かのUX/分離期待が未定義（ADR-0009境界） | **対応済**（v4：ADR-0009で定めると明記） |
| A2c-L2 | 🟢 Low | 中央IDの「opaque_id→所属国」自体が全ユーザーの国別在籍メタデータの集約であり、最小化してもゼロPIIではない（残留リスクの明示が必要） | **対応済**（v4：ゼロPIIでない旨を明記） |

## 🟡 Medium（第3回）

### A2c-M1. グループ単位シャードの限界（単一グループが上限超）
- **該当箇所**：決定「シャードキーはグループ単位で設計し、1グループが複数シャードに跨らないようにする」
- **指摘（攻撃）**：この是正は「**1グループ≤1シャード（5万人）**」を暗黙前提にしている。しかし**単一のグループ/連合そのものが5万ユーザーを超える**（巨大な農協連合等）と、そのグループは1シャードに収まらず、**グループ内でシャード跨ぎ横断が必要**になる——結局C-2/M-6が「最大グループ」で再発する。上限超グループの扱いが未定義。
- **要修正（案）**：「単一グループが1DB上限を超える場合は、そのグループ内横断は読取専用の国内集約層で扱う（グループ横断ダッシュボードは集約DB/DWH参照）」と、上限超グループの明示ルールを追加。これはADR-0001の「集計はマテビュー/集計テーブル」と同じ方向で整合する。

### A2c-M2. 中央IDのSPOF化と単一国運用での不要性
- **該当箇所**：決定「国をまたぐID解決は中央のIDディレクトリ/SSO」
- **指摘（攻撃）**：中央IDを**必須**にすると、**全ログインが中央IDに依存＝グローバルSPOF**になり、中央IDが落ちると単一国ユーザーもログインできない。これは「国ごと独立システム」の**可用性上の利点（1国が落ちても他国は動く）を損なう**。さらに、**#13で多数派と見込まれる単一国運用（自社利用等）では、そもそも国をまたがないため中央IDは不要**。共通ケースに不要なグローバル依存を課すのは過剰。
- **要修正（案）**：**中央IDは「ユーザーが複数国システムにまたがる運用」でのみ必要な任意コンポーネント**と位置づける。単一システム運用では中央IDなしで各DB内ブートストラップ（ADR-0001 A1e-M1）のみで完結する。可用性のため中央IDは冗長化するが、**その障害時も既存セッションや単一国ログインは縮退動作**できる設計を推奨、と明記。

## 🟢 Low（第3回）

### A2c-L1. フェデレーション認証の複数国UX
- **該当箇所**：決定「認証はフェデレーション」
- **指摘**：複数国で働く個人が「1回の認証で複数国システムにアクセスできる（SSO）」のか「国ごとに別資格情報」なのか、UX・分離の期待が未定義。ADR-0009との境界。
- **要修正（案）**：期待（SSO or 国別）と、それに伴う分離/UXのトレードオフをADR-0009で定めると明記。

### A2c-L2. 中央IDのメタデータ残留
- **該当箇所**：決定「中央IDは不透明識別子＋ルーティングのみ」
- **指摘**：最小化しても「どのopaque_idがどの国か」は**全ユーザーの国別在籍というメタデータの集約**であり、完全なゼロPIIではない（在籍国自体が機微な場合がある）。
- **要修正（案）**：中央IDの保持情報が「ゼロPIIではなく最小メタデータ」であることと、その残留リスク・配置法域の判断を運用者責任として明示。

## 処置方針（第3回・推奨）

1. **A2c-M1・A2c-M2** は決定の精緻化（上限超グループは集約層、中央IDは任意化・縮退動作）。特にA2c-M2は共通ケースの簡素化に効く。
2. **A2c-L1・L2** は認証UX境界・メタデータ残留の明示。

> 総括：3回のレビューで High は 1→0→0。第3回は「是正の限界」（グループ>シャード、中央ID SPOF）に収れん。A2c-M2（中央IDの任意化）は多数派の単一国運用を軽くする良い簡素化。これらを反映すればADR-0002は**単一国〜多国籍まで一貫した配備モデル**として閉じる。次回で収束の見込み。

---

# 第4回レビュー（ADR-0002 v4 に対する再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | ADR-0002 v4（第3回4件を反映した版） |
| レビュー日 | 2026-07-17 |
| 指摘件数 | High 0／Medium 1／Low 2（計3件） |

> 所見：中央IDの任意化・上限超グループの集約層で骨格は整った。残るのは**極端なテール（単一グループ>5万人のシャード分割時のルーティング）**と記述整合のみ。High相当なし。**本ADRのレビューはこの回で底**と判断し、A2d反映をもってクローズを推奨。

## サマリ（第4回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2d-M1 | 🟡 Medium | 上限超グループをシャード分割する場合、グループ内の org→shard ルーティングマップ（個別操作を正しいシャードへ振る仕組み）が未定義。読取集約は集約層で扱えるが、組織別の個別操作のルーティングが要る（極端なテール） | **対応済**（v5：シャードディレクトリを明記、詳細は設計へ） |
| A2d-L1 | 🟢 Low | A2-M1冒頭「国をまたぐID解決は中央IDで行う」とサブ箇条「任意（単一国では不要）」が既定/例外で不整合気味。冒頭を「複数国跨ぎ時は中央IDで解決」に整える | **対応済**（v5：冒頭を複数国跨ぎ時に整える） |
| A2d-L2 | 🟢 Low | 中央ID障害時の縮退スコープ（複数国の新規ログインは不可、単一国・既存セッションは継続）が明示されていない | **対応済**（v5：縮退スコープを明記） |

## 🟡 Medium（第4回）

### A2d-M1. 上限超グループのシャード内 org→shard ルーティングが未定義
- **該当箇所**：決定「上限超グループはシャード分割し、横断は国内集約層で扱う」
- **指摘（攻撃）**：単一グループが5万人超でシャード分割される場合、**読取の横断集約は集約層で扱える**が、**個別操作（特定組織への作業指示発行・その組織のデータ参照）を正しいシャードへ振り分けるルーティング**が要る。グループ管理者のセッションは、あるorgはシャード1、別orgはシャード2、と操作ごとに宛先シャードが変わる。この**org→shardのルーティングマップ**（どの組織がどのシャードにあるか）が未定義。なおグループ管理者の書込は「特定org＝1シャード」に閉じるため分散トランザクションは不要だが、**宛先解決の仕組み**は必要。
- **影響**：極端なテール（>5万人の単一グループ）だが、該当時にグループ管理者の個別操作が正しいシャードに届かない。
- **要修正（案）**：「シャード分割時は org→shard のルーティングマップ（シャードディレクトリ）を持ち、個別操作は宛先orgのシャードへ、横断集計は集約層へ、と振り分ける」を明記。詳細（マップの管理・再配置）は設計フェーズ（シャーディング設計）へ。**極端なテールのため、設計フェーズ課題として送り本ADRはクローズしてよい**。

## 🟢 Low（第4回）

### A2d-L1. 中央IDの「既定/例外」の記述整合
- **該当箇所**：A2-M1冒頭「国をまたぐID解決は中央のIDディレクトリ/SSOで行う」／サブ箇条A2c-M2「中央IDは任意（単一国では不要）」
- **指摘**：冒頭が「中央IDで行う」と既定のように読め、サブ箇条で「任意」と補正しており、既定か例外かが一読で分かりにくい。
- **要修正（案）**：冒頭を「**複数国にまたがる運用の場合は**中央ID/SSOで解決する（単一国運用では不要）」と整える。

### A2d-L2. 中央ID障害時の縮退スコープの明示
- **該当箇所**：決定「中央ID障害時も既存セッション・単一国ログインは縮退動作」
- **指摘**：縮退の**具体的スコープ**（複数国ユーザーの新規ログインは中央ID復旧まで不可、単一国ユーザー・既存セッションは継続）が明示されていない。
- **要修正（案）**：縮退時に「何ができ何ができないか」を明記する。

## 処置方針（第4回・推奨）

1. **A2d-M1** はルーティングマップの必要性を1文明記し、詳細は設計フェーズ（シャーディング）へ。
2. **A2d-L1・L2** は記述整合・縮退スコープの明示。

> 総括：4回のレビューで High は 1→0→0→0。第4回は極端なテール（>5万グループのルーティング）と記述整合のみで、**ADR-0002は実質収束**。A2dを反映すれば、単一国〜多国籍・上限超グループまで一貫した配備モデルとして閉じる。**A2d反映後はADR-0002をクローズし、ADR-0003（データライフサイクル）へ進むのが妥当**（同一ADRの反復は収穫逓減、残り20本のADRの機会費用）。

---

# 第5回レビュー（ADR-0002 v6・A2e-M1 に対する敵対的レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002 v6](ADR-0002-配備モデル-1DB-1国.md)。特に **§2 の【v6追記】A2e-M1（法域ローカル user→shard 索引）** |
| レビュー日 | 2026-07-26 |
| レビュアー | Claude (Opus 5)／設計者＝査読者（自己レビュー） |
| このレビューの経緯 | **クローズ済みのP0土台ADRに、ADR-0008 独立レビュー IND-H1 の波及として v6 で新しい設計要素（A2e-M1）が追加されたが、本記録票にその項目が1件も無かった**＝**敵対的レビューを一度も通っていない設計要素が土台ADRに載っている**状態だった。ADR-0008 側の記録票には IND-H1 の指摘と是正方針はあるが、**ADR-0002 v6 で新規に起こした本文そのもの**（配置・整合・可用性・機密性・他決定との接合）は攻撃されていない。これを埋めるための回。 |
| 攻撃方針 | 3軸（**要求**／**配備**／**権限**）。加えて ADR-0008 第9回の教訓「**新規に書いた文を最も疑う**」を適用し、**A2e-M1 の4行が既存の A2b-M2／A2c-M1／A2c-M2／§2構成要素一覧／§4帰結／§5未解決事項と整合するか**を1つずつ突き合わせる。 |
| 指摘件数（第5回） | 計8件（**High 2**／Medium 3／Low 3）。**全件対応済**（v7反映） |
| 総括 | **A2e-M1 の判断そのもの（法域ローカル索引が要る／中央IDとは別物）は正しい**——IND-H1 の是正として的確で、ここを疑う理由はない。問題は**「4行で足したこと」の波及が本文全体に行き渡っていない**点にある。具体的には (1) **新しい横断コンポーネントが増えたのに、§2の構成要素一覧（4要素）にも §4帰結にも §5未解決事項にも現れず、可用性の主張が更新されていない**（A2f-H1）。(2) **A2e-M1 が「権威用途は国内集約層へ」と1行で振った先が、A2c-M1 の適用条件外であり、かつ権限モデル未定義**——結果として **F-72（受託者の労務サマリの権威取得）が大国配備で実現経路を持たない**（A2f-H2）。**これは IND-H1 と同型の「中核要求が大国配備で宙に浮く」欠陥が、1段下で再発したもの**。**High 2 につきクローズ状態の再確認は不可（要 v7）。** |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

## サマリ（第5回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2f-H1 | 🟠 High | **user→shard 索引が可用性のクリティカルパスに入ったのに、構成要素一覧（§2・4要素）に不在で、縮退スコープも「単一国運用は横断コンポーネント非依存」という可用性の主張も更新されていない**。ADR-0019 がこの一覧を根拠にHA設計すると、最大規模の配備でログイン可用性を取りこぼす | 対応済（v7反映） |
| A2f-H2 | 🟠 High | **F-72（本人起点横断の権威用途）が大国配備で実現経路を持たない**。A2e-M1 はオンライン多シャードファンアウトを「参考表示に限定」し権威用途を国内集約層へ振ったが、その集約層は (a) A2c-M1 で「単一グループ>5万人の極端なテールのみ」と限定された別物、(b) **権限モデル未定義**、(c) 個人労務データを置くことが ADR-0005（個人データ最小権限）・ADR-0003（①-b PII）・GDPR最小化と未接合。**IND-H1 と同型の欠陥が1段下で再発** | 対応済（v7反映） |
| A2f-M1 | 🟡 Medium | **A2b-M2 本文が「シャード跨ぎ横断を避ける」のまま**で、A2e-M1（本人起点横断は必然的に跨ぐ）と矛盾。A2b-M2 だけを読んだ設計者は「跨ぎは起きない」と誤解する | 対応済（v7反映） |
| A2f-M2 | 🟡 Medium | **索引の整合性モデル（真実源との関係・許容するズレの向き）が未定義**。真実源は各シャードのメンバーシップで索引は派生＝原子更新不可。ズレの向きで安全性が非対称（脱退遅延＝安全／加入遅延＝欠落）なのに、その性質が規定されていない | 対応済（v7反映） |
| A2f-M3 | 🟡 Medium | **索引は「誰がどのグループ群と取引関係にあるか」の関係グラフを、RLSの外側に全ユーザー分集約する**。同一法域＝レジデンシー問題なしは正しいが、**機密性の問題がないことを意味しない**（受託関係は営業秘密）。R6-M1（権限データ中央集約）と同型の縁が「法域内だから」で見逃されている | 対応済（v7反映） |
| A2f-L1 | 🟢 Low | **A2c-M1 の ※注「単一グループが5万人を超える極端なテールにのみ該当」が、A2e-M1 からの参照で事実上無効化されているのに残置**（記述整合） | 対応済（v7反映） |
| A2f-L2 | 🟢 Low | **索引の粒度が文書間で揺れている**：ADR-0002 は `user_id → シャード集合`、ADR-0008 §2.6.1 は `user→{テナント,シャード}`。テナントまで持つかで保持データ量・更新頻度・機密性（A2f-M3）が変わる | 対応済（v7反映） |
| A2f-L3 | 🟢 Low | **v6追記が §2 の箇条書き内に埋め込まれただけで、§4帰結・§5未解決事項に一切反映されていない**（新コンポーネントの運用コスト・索引の配置が挙がっていない） | 対応済（v7反映） |

## 🟠 High（第5回）

### A2f-H1. 索引が可用性のクリティカルパスに入ったのに、構成要素一覧・可用性の主張が未更新
- **該当箇所**：v6 §2【v6追記】A2e-M1「冗長化し、障害時はTTL内の既知解決で縮退」／§2「**構成要素の全体像（A2b-L2）**」の**4要素**（①各国システム／②中央ID〔任意・複数国跨ぎ時のみ〕／③システム外DWH〔任意〕／④国内集約層〔大国シャード時のみ〕）／A2c-M2「中央IDを**必須**にすると全ログインが依存するグローバルSPOFになり…**単一国運用では不要**」
- **指摘（攻撃・配備軸）**：A2e-M1 は**第5の横断コンポーネント**を導入したのに、**「境界を明示する」ことを目的に置かれた §2 の構成要素一覧に載っていない**。一覧は依然「横断コンポーネントは中央ID（複数国のみ）と集約層（極端なテールのみ）」と読める。しかし実際には、**シャード化した単一国配備**では user→shard 索引が**全ユーザーのログイン/横断のクリティカルパス**に入る。
  - A2c-M2 は中央IDについて **縮退スコープを precise に定義**した（単一国ユーザーの新規ログイン・既存セッションは継続可／複数国ユーザーの新規ログインは不可）。**A2e-M1 には同等の精度の縮退定義がない**——「TTL内の既知解決で縮退」だけでは、**コールドなユーザー（当日初回ログイン・新規端末・キャッシュ失効後）はルーティング先が determine できず、単一国運用でもログイン不能**になる。
  - さらに ADR-0002 が繰り返し掲げてきた可用性の売り——「**国ごと独立システム（1国が落ちても他国は動く）／単一国運用はグローバルSPOFを持たない**」——は、**シャード化法域では成立しなくなった**（法域内SPOFが生まれた）。本文にその後退が書かれていない。
- **影響**：**未起票の ADR-0019（インフラ・デプロイ）は、この構成要素一覧を根拠にHA対象を決める**。索引が一覧にない以上、**最大規模の配備（＝シャードが要るほど大きい国）でログイン可用性の設計が丸ごと抜ける**。運用開始後に「最大顧客だけログインできない障害モードがある」と判明する類の欠落。
- **要修正（案）**：
  1. §2 の構成要素一覧に **⑤法域ローカル user→shard 索引（シャード化法域でのみ必要・法域内配置・ルーティングメタデータのみ）** を追加し、①〜⑤の**必要条件（いつ存在するか）**を表で明示する。
  2. A2c-M2 と**同じ精度で縮退スコープを定義**する：索引障害時に (a) キャッシュ済みユーザーはTTL内で継続、(b) **コールドユーザーは全シャードへのフォールバック探索**（コスト大だが可用性優先）か**ログイン不可**か、を**決める**。フォールバック探索を採るなら、それ自体が全シャードへの認証前アクセスになるため権限・レート制限の設計が要る（ADR-0001/0009）。
  3. §4帰結に「**シャード化法域では法域内の横断コンポーネントが増え、単一国運用でも横断コンポーネント非依存ではなくなる**」を明記し、ADR-0019 へのHA要件として申し送る。

### A2f-H2. 大国配備で F-72（本人起点横断の権威用途）の実現経路がない
- **該当箇所**：v6 §2【v6追記】A2e-M1「**本人起点横断の権威用途（給与＝complete のみ確定）は、多シャード時は国内集約層（A2c-M1）に寄せ**、オンラインの多シャードファンアウトは**参考表示に限定**するのが安全」／A2c-M1「**単一のグループ/連合そのものが1DB上限（5万人）を超える**」場合の「読取専用の国内集約層」／同 ※注「これは単一グループが5万人を超える**極端なテールにのみ該当**する」／ADR-0008 §2.6.2 完全性フラグ（権威用途は complete のみ確定）／要求 #18・F-72（受託者の労務サマリ）
- **指摘（攻撃・要求×権限軸）**：A2e-M1 は権威用途の逃げ道を**1行で国内集約層に振った**が、その先が3重に成立していない。
  1. **適用条件が違う**：A2c-M1 の国内集約層は「**単一グループが5万人超**」という極端なテール向けに定義され、※注でそう限定されている。一方 A2e-M1 が扱うのは「**受託者のメンバーシップが複数グループに跨る**」という、シャード化法域では**普通に起きる**ケース（受託の本質＝無関係な複数グループから請け負う、と A2e-M1 自身が書いている）。**普通のケースを、極端なテール向けの機構に振っている**。
  2. **権限モデルが未定義**：国内集約層は A2c-M1 で「読取専用の集約DB/DWH」とだけ書かれ、**RLS・ロール・個人データの最小権限がどうなるかが一切定義されていない**。ADR-0001 は「RLSが最終防波堤」、ADR-0005 は「個人データ（位置ログ・打刻・労務サマリ）は最小権限」「本人起点横断は労務サマリ限定」を決めたが、**集約層はその設計の外**にある。
  3. **個人データを集約層へ複製することの是非が未検討**：給与の権威用途は**個人特定が必須**であり、仮名化できない。つまり **全シャードの個人労務データを、法域内とはいえ1箇所に集約する**ことになる。これは ADR-0003 の①-b（PIIイベント）、ADR-0005 の最小権限、GDPR のデータ最小化と正面から接合が要る論点なのに、検討された形跡がない。**ADR-0014 の「システム外DWH＝PIIフリー/仮名化」原則とも真逆**（別物だが、本文で両者の区別が曖昧なため混同されやすい）。
  結果として、**大国配備における F-72 の権威取得は、オンラインファンアウト（＝参考表示に限定、と A2e-M1 自身が封じた）でも、国内集約層（＝適用条件外・権限未定義）でも、実現経路を持たない**。
- **影響**：**IND-H1 とまったく同型の欠陥**——「中核要求（受託者の労務横断・F-72）が大国配備で宙に浮く」——が、**IND-H1 の是正の中で1段下に再発**している。労務（Phase 3）の詳細設計に入った段階で、配備モデルの再オープンを迫られる。
- **要修正（案）**：**逃げずに、大国配備での F-72 の経路を1つ決める**。選択肢は少なくとも3つあり、ADR-0002 で裁定すべき：
  - **(a) 多シャードファンアウトで complete を成立させる**：ADR-0008 §2.6.2 の完全性フラグを使い、**全対象シャードが応答したときのみ権威値として確定**（partialは参考）。可用性は落ちるが**データを複製しない**のが最大の利点（最小権限・GDPR最小化と整合）。給与確定は月次のバッチ的タイミングであり、**リアルタイム性の要求が低い**ことを踏まえれば現実的。→ **本命候補**。
  - **(b) 国内集約層を正式な構成要素として設計する**：適用条件を A2c-M1 の極端なテールから**シャード化法域一般**へ拡張し、**集約層のRLS・ロール・保持期間・個人データ最小権限・監査を ADR-0001/0005 と同水準で定義**する（実質、第2のデータストアの権限設計）。コストが大きい。
  - **(c) 権威用途そのものをシャード内に閉じる**：給与・法定集計の権威単位を「シャード（グループ）ごと」と定義し、**跨ぎの合算は業務プロセス側（人手・オフライン）で行う**と割り切る。要求 #18/F-72 の解釈変更を伴うため、要求仕様への申し送りが要る。
  いずれを採るにせよ、**A2e-M1 の「集約層に寄せるのが安全」という1行は、根拠が示されていないので撤回**し、選択肢の比較と却下理由を §3 に記録する（ADRの目的＝根拠の記録）。

## 🟡 Medium（第5回）

### A2f-M1. A2b-M2 の「シャード跨ぎ横断を避ける」が A2e-M1 と矛盾
- **該当箇所**：v6 §2 A2b-M2「1国=複数DBは…**シャード跨ぎ横断（C-2/M-6同型）を再発させる。これを避けるため**、シャードキーはグループ単位で設計し、1つのグループが複数シャードに跨らないようにする」／A2e-M1「**本人起点横断は、1国=複数シャードでは必然的にシャード跨ぎになる**」
- **指摘**：A2b-M2 は「跨ぎ横断を**避ける**」と断定しているが、A2e-M1 が「**本人起点横断は必然的に跨ぐ**」と明示的に否定した。**グループ管理者横断については A2b-M2 が成立し、本人起点横断については成立しない**——この区別が A2b-M2 本文に反映されていないため、**A2b-M2 だけを読んだ設計者は「シャード跨ぎ横断は起きない」と誤解**する。IND-H1 が生まれた原因（A2b-M2 を根拠に「単一国内はDB内で解決」と思い込んだ）と同じ構造が、文書上に残ったまま。
- **要修正（案）**：A2b-M2 に **「ただしこれが成立するのは**グループ管理者の横断**であり、**本人起点横断（受託者のメンバーシップは複数グループに跨る）は例外**として A2e-M1 で扱う」** を明記する。断定の**射程を明示**する（ADR-0008 第9回の所見と同じ教訓）。

### A2f-M2. 索引の整合性モデル（真実源・許容するズレの向き）が未定義
- **該当箇所**：v6 §2 A2e-M1「索引の物理・整合（メンバーシップ変更時の更新）はシャーディング設計＋ADR-0008 §2.6.1/§2.6.2 と一体で確定」／ADR-0005（メンバーシップの真実源＝各テナント/シャードのDB）／ADR-0008 IND-L3（キャッシュの両方向即時無効化）
- **指摘**：**丸投げされているのは「物理」だけでよく、「整合性モデル」はここで決めないと後段が決められない**。メンバーシップの真実源は**各シャードのDB**であり、索引はその**派生（二重管理）**。両者は別システムなので**原子的に更新できない**——ズレは必ず起きる。重要なのは**ズレの向きで安全性が非対称**であることが規定されていない点：
  - **加入が索引に反映される前**：そのシャードがファンアウト対象に入らず、**データが見えない（欠落）**。RLSでは防げない種類の不具合。
  - **脱退後も索引に残る**：ファンアウト先には含まれるが、**そのシャードのRLSが拒否するので漏洩しない**（フェイルセーフ側）。
  つまり **索引は「正確な集合」である必要はなく、「取りこぼしのない上位集合（over-approximation）」であれば安全**。この性質を明記すれば設計が一気に単純になる（脱退は遅延可・加入だけ同期）のに、それが書かれていないため、後段は「完全一致の分散整合」という難問を解こうとしてしまう。なお ADR-0008 IND-L3 は**キャッシュ**の無効化を決めたが、**索引本体の更新が非同期なら、キャッシュを無効化しても古い索引を引き直すだけ**で解決しない——この層の区別も未整理。
- **要修正（案）**：A2e-M1 に **「索引はメンバーシップ集合の**上位集合（over-approximation）**であることのみを保証する。漏れ（false negative）は許さず、余り（false positive）は許す（各シャードのRLSが最終防波堤）」** と性質を規定する。そのうえで **加入＝同期的に索引へ追加**（または加入直後の猶予期間は全シャードへフォールバック）、**脱退＝非同期で遅延可**とする。**索引本体の更新とキャッシュ無効化は別層**であることを ADR-0008 §2.6.1 と突き合わせて明記。

### A2f-M3. 索引が「取引関係グラフ」をRLSの外側で全件集約する（機密性）
- **該当箇所**：v6 §2 A2e-M1「**同一法域内**（越境ではない・**レジデンシー問題なし**）」「索引はルーティングメタデータのみ（権限は各シャード＝法域内で解決）」／第8回 R6-M1（中央ディレクトリの権限スナップショット中央集約＝越境）／ADR-0001（RLSが最終防波堤＝多層防御）
- **指摘（攻撃・権限軸）**：**「レジデンシー問題がない」ことは「機密性の問題がない」ことを意味しない**——v6 はこの飛躍の上に安心している。`user_id → メンバーシップを持つシャード集合` は、**「どの受託者がどのグループ群と取引関係にあるか」という関係グラフを、RLSの外側に、全ユーザー分、1箇所に集約したもの**である。ルーティング情報であっても、**関係の存在そのものが情報**であり、受託関係・取引先は営業秘密になり得る（競合が誰と組んでいるか／ある法人が誰を使っているか）。
  - **ADR-0001 の多層防御の外**：この索引は RLS の適用対象外の集約点であり、侵害されれば**全件が一度に漏れる**。ADR-0001 が「入口層が侵害されてもRLSが防ぐ」と設計した構造の、例外になっている。
  - **R6-M1 と同型**：第8回 R6-M1 は「中央ディレクトリが権限スナップショットを集約する」ことを Medium と判定した。今回は**権限そのものは集約していない**が、**メンバーシップの存在**は集約している。「法域内だから」という理由で、**同型の縁が検討されずに通過している**。
- **要修正（案）**：
  - A2e-M1 に **「索引は法域内であってもRLSの外側の集約点であり、`(user, shard)` の関係グラフという機密資産である」** と明記し、**ADR-0017 の脅威モデル対象**として申し送る（最小化・暗号化・アクセス監査・侵害時の影響範囲）。
  - **保持を最小化**：シャード集合のみとし、**テナント粒度は持たない**（A2f-L2 と併せて粒度を決める。テナントまで持つと関係グラフの解像度が上がり機密性が増す）。
  - 索引への**アクセスは入口層のルーティング処理のみ**に限定し、**人手・分析・エクスポートの経路を持たせない**（監査可能に）。

## 🟢 Low（第5回）

### A2f-L1. A2c-M1 の「極端なテールにのみ該当」注記の残置
- **該当箇所**：v6 §2 A2d-M1 末尾「※これは単一グループが5万人を超える**極端なテールにのみ該当**する」／A2e-M1 が A2c-M1 の国内集約層を一般ケースで参照
- **指摘**：A2e-M1 が国内集約層を「多シャード時の権威用途」に使う以上、この注記の限定は事実に反する（A2f-H2 の文書側の表れ）。A2f-H2 の裁定に合わせて注記を修正または削除する必要がある。
- **要修正（案）**：A2f-H2 で採る選択肢に応じて、注記を「（a)を採るなら）国内集約層は極端なテールのみで正しい」／「（b)を採るなら）シャード化法域一般に拡張」と書き換える。

### A2f-L2. 索引の粒度が ADR-0002 と ADR-0008 で揺れている
- **該当箇所**：ADR-0002 v6 A2e-M1「`user_id → メンバーシップを持つ**シャード集合**`」／ADR-0008 v9 §2.6.1「`user→{**テナント**,シャード}` のルーティング情報」
- **指摘**：**シャードだけか、テナントまで持つか**で、保持データ量・メンバーシップ変更時の更新頻度（テナント単位の異動で索引更新が要るか）・機密性（A2f-M3 の関係グラフの解像度）が変わる。2文書で表現が違うまま実装に渡すと、より広い方（テナント粒度）が採用されがち。
- **要修正（案）**：**シャード集合のみ**に統一する（ファンアウト先の決定にはシャードで十分。テナント絞り込みは各シャードのRLSが行う＝最小化とも整合）。ADR-0008 §2.6.1 の表現も揃える。

### A2f-L3. v6追記が §4帰結・§5未解決事項に反映されていない
- **該当箇所**：v6 §2【v6追記】のみ／§4 帰結（新コンポーネントの運用コストへの言及なし）／§5 未解決事項（索引の配置・整合が挙がっていない）
- **指摘**：ADRの構造上、決定を足したら**帰結と未解決事項も更新される**のが運用ルール（§1 テンプレート）。v6 は §2 に4行足しただけで、**「悪い影響・制約」に索引の運用コスト（配置・冗長化・整合・監視）が現れず、§5 にも索引の未決事項がない**。結果として、後続ADR（0019）が §5 を読んでも索引の存在に気づけない（A2f-H1 の一因）。
- **要修正（案）**：§4 に「シャード化法域では法域内横断コンポーネントが増える（運用コスト・可用性設計）」、§5 に「**法域ローカル索引の配置・整合モデル・可用性・脅威モデル**（ADR-0019/0017・シャーディング設計）」を追加。

## 処置方針（第5回・推奨）

1. **A2f-H2 を最初に裁定**する（(a)ファンアウトでcomplete／(b)集約層を正式設計／(c)権威をシャード内に限定）。**推奨は (a)**——データを複製せず ADR-0005 の最小権限・GDPR最小化と整合し、給与確定の低リアルタイム性とも合う。裁定が §3 選択肢表・A2c-M1 の注記（A2f-L1）・ADR-0008 §2.6.2 の記述に連動する。
2. **A2f-H1**：構成要素一覧に⑤を追加、A2c-M2 と同精度の縮退スコープを定義、§4/§5 を更新（A2f-L3 も同時に解消）。
3. **A2f-M2**：索引の性質を「**上位集合（over-approximation）**・漏れ不可/余り可」と規定。これが決まれば実装設計が大幅に単純化する。
4. **A2f-M1／M3／L2**：断定の射程明示（A2b-M2）、機密資産としての申し送り（ADR-0017）、粒度をシャード集合に統一（ADR-0008 §2.6.1 も同期）。
5. 反映後、**第6回で3軸＋新規記述への攻撃を再適用**。ADR-0002 は**土台ADR**であり波及が広いので、ここは急がない。

> **所見（メタ）**：今回の8件は、**A2e-M1 の判断が誤っていたから出たのではなく、「4行の追記が本文全体に行き渡っていない」から出た**。クローズ済みADRへの波及追記は、**「決定を1箇所に足す」だけでは足りず、構成要素一覧・帰結・未解決事項・関連する既存決定の射程まで見直す**必要がある。**クローズ済みADRを更新したら、その版に対して1回レビューを回す**——これを運用ルールに加えるべき（今回、ADR-0001 v7 も同様に自身の記録票へ項目がないが、あちらは ADR-0003 A3b-M1 の要修正案どおりの反映であり、新規に起こした設計を含まない点で今回とは異なる）。

## 処置結果（v7・2026-07-26）

第5回の8件を**全件処置**した。裁定を要した A2f-H2 は **§3.1 で (a)「全シャード応答（complete）で確定」を採用**し、v6の「国内集約層へ寄せる」案は**撤回**（却下した (b)(c) の理由も §3.1 に記録）。**ADR-0008 §2.6.2 が既に complete 前提で書かれていたため、v6 の集約層案のほうが外れ値だった**ことも判明した。

| ID | 反映先 | 処置の要点 |
|---|---|---|
| A2f-H1 | §2 構成要素表（①〜⑤）／A2e-M1 配置／§4帰結 | ⑤ user→shard 索引を**必要条件つきの表**として追加。**独立サービスではなく法域内の全シャードへ複製**する設計に変更しSPOFを回避。A2c-M2 と同精度の縮退スコープを4項目で定義。認証前の全シャード探索は不採用と明記 |
| A2f-H2 | §3.1（新設）／A2e-M1 | **(a) complete で確定**を採用。データを複製しないことを優先し、可用性はリトライ可能な非同期ジョブ＋低リアルタイム性（月次確定）で緩和。SLA内に揃わなければ業務エスカレーション |
| A2f-M1 | A2b-M2 | 「跨ぎ横断を避ける」の**射程をグループ管理者横断に限定**し、本人起点横断は例外と明記（IND-H1 の読み違いの再発防止） |
| A2f-M2 | A2e-M1 | **上位集合保証（漏れ不可・余り可）**を規定。加入＝同期反映／脱退＝遅延可。索引本体とキャッシュの層の違いも明記 |
| A2f-M3 | A2e-M1／§4帰結 | **RLS外側の機密資産（取引関係グラフ）**として ADR-0017 の脅威モデル対象に。粒度最小化・専用ロール・監査で、全シャード複製による露出面増を相殺 |
| A2f-L1 | A2d-M1 ※注 | 国内集約層は極端なテール限定のまま**維持できる**ことを明記（(a)採用により集約層を使わないため）。索引との混同を注意喚起 |
| A2f-L2 | A2e-M1／**ADR-0008 §2.6.1** | 粒度を **`user → シャード集合`** に統一（テナント粒度は持たない）。ADR-0008 側も同時に修正 |
| A2f-L3 | §4帰結／§5未解決事項 | 索引の運用コスト・可用性の性質変化・機密性を帰結に追加。索引の物理設計／complete の実測（S4/S7）／ADR-0008 との粒度整合を未解決事項に追加 |

> **次**：v7 に対する**第6回レビュー**（3軸＋新規記述への攻撃）。特に **§3.1(a) の可用性（対象シャード数が増えたとき complete が揃うか）** と **全シャード複製が A2f-M3 の機密性に与える影響**は、v7で新しく書いた判断なので重点的に攻撃する必要がある。

---

# 第6回レビュー（ADR-0002 v7 に対する敵対的再レビュー・3軸＋新規記述の重点攻撃）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002 v7](ADR-0002-配備モデル-1DB-1国.md)（第5回8件を反映後） |
| レビュー日 | 2026-07-26 |
| レビュアー | Claude (Opus 5)／設計者＝査読者（自己レビュー） |
| 攻撃方針 | 3軸（**要求**／**配備**／**権限**）＋**新規記述を最も疑う**。v7で新しく書いたのは (1) A2b-M2の射程限定、(2) A2e-M1 の粒度・**上位集合保証**・**全シャード複製**・機密資産、(3) §2 構成要素表①〜⑤、(4) **§3.1 の裁定(a)**、(5) §4/§5 の追記。**特に「v7で足した決定どうしを合成したときに何が起きるか」**を突き合わせる（第9回 ADR-0008 で Medium 4件が新規記述そのものの欠陥だった教訓）。 |
| 指摘件数（第6回） | 計7件（**High 1**／Medium 3／Low 3）。**全件対応済**（v8反映） |
| 総括 | 第5回8件の処置は**いずれも指摘の根に届いており、方向は正しい**（特に §3.1 で v6 の集約層案を撤回して (a) を選んだ判断、および上位集合保証の導入は、後段の設計を実際に単純化している）。しかし**v7で足した2つの決定を合成すると、ADR-0002 が一貫して避けてきた原則に反する**——「**索引を全シャードに複製**」（A2f-H1の処置）と「**加入は索引へ同期反映**」（A2f-M2の処置）を素直に繋ぐと、**メンバーシップ付与という日常操作が法域内全シャードへの同期書込＝分散トランザクションになり、書込可用性が全シャードのANDになる**（A2g-H1）。**読取側のSPOFを消した処置が、書込側でSPOFより悪いもの（AND可用性）を作っている。** 各処置は個別に正しく、**接合で壊れている**——第5回の所見「4行の追記が本文全体に行き渡っていない」と同じ構造が、今度は**新旧ではなく新規どうしの間**で起きた。**High 1 につきクローズ不可。** |

## サマリ（第6回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2g-H1 | 🟠 High | **【配備軸・v7新規どうしの接合】「索引は全シャードに複製」×「加入は同期反映」＝メンバーシップ付与のたびに法域内全シャードへの同期書込**。ADR-0002/0008 が一貫して避けてきた**分散トランザクション**が日常操作で発生し、**1シャードでも停止していると加入処理ができない（書込可用性が全シャードのAND）**。A2f-H1 で消した読取SPOFより悪い | 対応済（v8反映） |
| A2g-M1 | 🟡 Medium | **索引の配置裁定（全シャード複製 vs 独立サービス）が §3 に記録されておらず、機密性の比較が欠落**。複製は**侵害面がシャード数倍**（どれか1つの侵害で法域内全ユーザーの関係グラフが漏れる）なのに、「粒度最小化・専用ロール・監査で相殺する」と根拠なく断定。A2f-H2 は §3.1 に選択肢表を作ったのに、同格の裁定である配置は本文中の断定のみ | 対応済（v8反映） |
| A2g-M2 | 🟡 Medium | **§3.1(a) の complete を「同一時点での全シャード同時応答」と読むと、ローリングメンテ下でシャード数が増えるほど構造的に成立しなくなる**。「リトライで吸収」は瞬間障害には効くが、**計画メンテで常に1台落ちている状態**は吸収できない | 対応済（v8反映） |
| A2g-M3 | 🟡 Medium | **【権限軸】索引テーブルが ADR-0001 のテーブル4分類・FORCE RLS・認証ブートストラップ特権経路のどこに属すか未定義**。`tenant_id` を持たず、かつ**認証前に他人の `user_id` で引く**ため、「ユーザー所有（user_idでRLS）」にも収まらない | 対応済（v8反映） |
| A2g-L1 | 🟢 Low | **④国内集約層と⑤索引が両方必要になるケース（単一グループ>5万人）で、本人起点の権威横断をどちらで行うかの優先順位が未定義**。A2d-M1 注記は「混同するな」と書いただけで、重なる場合を決めていない | 対応済（v8反映） |
| A2g-L2 | 🟢 Low | **単一シャード法域→シャード化への移行手順が §5 に不在**。「単一シャードなら索引不要」と決めた以上、**索引の初期構築と、移行中に上位集合保証が成立しない期間**の扱いが要る | 対応済（v8反映） |
| A2g-L3 | 🟢 Low | **(a) の正しさが「partial を確定値に昇格させない運用規律」に依存することを v7 自身が認めている**が、**技術的強制**（確定処理が complete を必須検証する）が決まっていない。規律は破れる | 対応済（v8反映） |

## 🟠 High（第6回）

### A2g-H1. 「全シャード複製」×「加入は同期反映」＝日常操作が分散トランザクションになる
- **該当箇所**：v7 §2 A2e-M1「**【v7】配置＝法域内の全シャードに複製する（A2f-H1）**：…法域内の各シャードDBに同一内容のルーティングテーブルとして複製し、アプリは健全な任意のシャードから引く」／同「**【v7】索引の整合性モデル＝上位集合保証（A2f-M2）**：… **加入＝同期的に索引へ追加**（索引更新が完了するまでメンバーシップ付与を完了扱いにしない）、**脱退＝非同期で遅延可**」／§2 A2d-M1「個別の書込は『特定組織＝1シャード』に閉じるため**分散トランザクションは不要**」／ADR-0008 §2.2 A8-H1「束は**単一シャードで原子受理**でき、**分散トランザクションを避ける**」
- **指摘（攻撃・配備軸／v7新規どうしの接合）**：2つの処置は**個別には正しい**が、**合成すると本ADRの根本原則を破る**。
  - 「索引は**全シャード**に複製」＋「加入は**同期的に**索引へ追加」＝**メンバーシップ付与のたびに、法域内の全シャードDBへ同期書込を行う**。これは**まさに分散トランザクション**であり、A2d-M1 と ADR-0008 A8-H1 が明示的に避けてきたもの。
  - しかも影響は性能だけではない：**1シャードでも停止していると加入処理を完了できない**（同期書込が完了しないため）。つまり**書込可用性が全シャードのAND**になる。A2f-H1 は「索引を独立サービスに置くとSPOFになる」ことを嫌って複製を選んだが、**読取のSPOF（1台落ちたら止まる）を消す代わりに、書込のAND（1台落ちたら止まる、を全台ぶん）を作っている**——**可用性の観点では悪化している**。
  - メンバーシップ付与は**日常操作**である（新しい受託契約・応援作業員の追加・季節雇用）。極端なテールの話ではない。
- **影響**：シャード化法域で、シャード1台のメンテナンス中はユーザーの追加ができない。運用が破綻するか、実装者が黙って「加入も非同期」に倒して**上位集合保証（漏れ不可）を破る**——後者のほうが起きやすく、そのとき**新規加入ユーザーのデータが見えない欠落**が静かに発生する（RLSでは防げない）。
- **要修正（案）**：**上位集合保証の性質をもう一段活かす**。上位集合であればよいなら、**複数コピーの「和」を取ってよい**（和は必ず上位集合になる）。したがって：
  - **加入時の同期書込は「加入先シャード自身のコピー1つ」に限定**する（ローカルトランザクションで、メンバーシップ付与と同一DB・同一トランザクション＝分散トランザクション不要）。
  - **他シャードのコピーへは非同期複製**（遅延可）。
  - **読取は複数コピーから引いて和を取る**（クォーラム読み。健全な k 台から取得し和集合をファンアウト先とする）。加入直後でも**加入先シャードのコピーには必ず載っている**ため、そのコピーが読めれば漏れない。
  - この形なら **書込＝1シャード（AND解消）／読取＝任意のk台（SPOF解消）** となり、A2f-H1 と A2f-M2 の両方の狙いを同時に満たす。
  - なお「読取で和を取る」ことは**余り（false positive）を増やす**が、上位集合保証の定義どおり**余りは安全**（各シャードのRLSが拒否する）。
  - k の値・複製遅延の上限・加入直後の読取先の選び方は **ADR-0019／シャーディング設計**で確定し、**S4/S7 で検証**する。

## 🟡 Medium（第6回）

### A2g-M1. 索引の配置裁定が §3 に記録されておらず、機密性の比較が欠落
- **該当箇所**：v7 §2 A2e-M1「索引を**独立サービスとして1箇所に置くと…SPOF**になり…**全シャードに複製**し…」／同 A2f-M3「（各シャードに複製する分、露出面が増えるトレードオフを、この最小化と監査で**相殺する**）」／§3.1（A2f-H2 の裁定には選択肢表を作った）
- **指摘（攻撃・権限×配備軸）**：
  1. **同格の裁定なのに記録の粒度が違う**：A2f-H2（権威用途）は §3.1 に (a)(b)(c) の比較表と却下理由を残したのに、**A2f-H1 の配置（複製 vs 独立サービス vs クォーラム）は本文中の1文の断定のみ**。ADRの目的は根拠の記録であり、**後から「なぜ複製にしたのか」を辿れない**。
  2. **比較軸が可用性しかない**：独立サービス案は「SPOFになる」という**可用性の一点だけで却下**され、**機密性が比較されていない**。しかし A2f-M3 が自ら認めるとおり索引は機密資産であり、**複製すると侵害面がシャード数 N 倍**になる——**N台のうちどれか1台が侵害されれば、法域内の全ユーザーの取引関係グラフが漏れる**。独立サービスなら守るべき箇所は1つ。**可用性で複製を選び、機密性で複製が不利、という真のトレードオフ**なのに、片側しか評価していない。
  3. **「最小化と監査で相殺する」は根拠のない断定**：粒度最小化（シャード集合のみ）は**漏れる情報の解像度**を下げるが、**漏れる確率が N 倍になること**は下げない。監査は事後検知であって防止ではない。「相殺」と言い切れる論拠がない。
- **影響**：ADR-0017（セキュリティ）が脅威モデルを立てるとき、「索引はN箇所に複製されている＝侵害確率がN倍」という最も重要な事実が、ADR-0002 の帰結からは読み取れない（§4は「露出面が増える」とだけ書き、N倍という規模感がない）。
- **要修正（案）**：**§3.2 として配置の選択肢表を新設**し、**(i) 独立サービス（冗長化）／(ii) 全シャード複製／(iii) 加入先へ同期＋非同期複製＋クォーラム読み（A2g-H1 の案）**を、**可用性・機密性（侵害面）・書込コスト・整合の難しさ**の4軸で比較して裁定を記録する。**A2g-H1 の修正と一体で行う**（(iii) が有力なら結論も変わる）。§4帰結には「**侵害面がシャード数に比例する**」を明記し、ADR-0017 へ定量的に申し送る。

### A2g-M2. complete を「同一時点の同時応答」と読むとローリングメンテ下で成立しない
- **該当箇所**：v7 §3.1「対象シャード集合へファンアウトし、**全応答が揃ったときのみ**権威値として確定」「可用性は**リトライ可能な非同期ジョブ**＋低リアルタイム性（月次確定）で緩和でき、SLA内に揃わなければ業務エスカレーション」／ADR-0008 §2.6.2「完全性（complete＝**全シャード応答**／partial＝一部欠損）」
- **指摘（攻撃・要求軸）**：「リトライで吸収」は**偶発的な瞬間障害**には効くが、**計画メンテナンス**には効かない。シャードは**ローリングで更新される**（同時に全台止めない代わりに、更新期間中は常にどこか1台が落ちている）。シャード数 N が増えるほど**「N台すべてが同時に健全な窓」は狭くなり、更新期間中は構造的に存在しない**。つまり **(a) は「リトライ回数を増やせばいつか揃う」という前提が成り立たない状況**を持つ。
  - さらに本質的な問題として、**そもそも権威用途に「同一時点の同時性」が必要なのかが検討されていない**。労務サマリの権威確定は**締め後の確定済み期間**を対象とする——シャードAを10時、シャードBを14時に読んでも、**対象期間のデータがもう変わらないなら合算は正しい**。必要なのは**時点の同時性ではなく、期間の完全性**である。
- **影響**：大国配備（Nが大きい＝まさに(a)が必要な配備）ほど complete が揃わず、月次の給与確定が**恒常的に業務エスカレーション**になる。(a) が実運用で成立せず、§3.1 の裁定が (c)（権威をシャード内に限定＝中核要求の毀損）へ後退する。
- **要修正（案）**：**complete の定義を「期間の完全性」に明確化**する。
  - 権威確定の complete ＝「**対象期間（締め済み）について、対象シャード集合のすべてから応答を得た**」こととし、**各シャードの取得時点が揃っている必要はない**と明記する。
  - 各シャードの応答に「**どの期間までが締め済み（不変）か**」を付す（ADR-0003 の①-a不変性・0008 の完全性フラグと接合）。締め済み期間のデータは後から変わらないため、**時点をまたいだ合算が安全**であることを根拠づける。
  - **部分取得の蓄積を許す**：ジョブは取得できたシャードから順に確定材料を蓄積し、**全シャード分が揃った時点で権威値を確定**する（毎回やり直さない）。これによりローリングメンテ中でも「1台ずつ順に取る」ことで complete に到達できる。
  - ADR-0008 §2.6.2 の完全性フラグにも「**時点同時性は要求しない／期間完全性で判定する**」旨を波及させる。

### A2g-M3. 索引テーブルが ADR-0001 のテーブル分類・RLS体系に収まっていない
- **該当箇所**：v7 §2 A2e-M1「法域内の各シャードDBに**同一内容のルーティングテーブル**として複製」「アクセスは入口層のルーティング処理**専用ロール**に限定」／ADR-0001（テーブル4分類＝①テナント固有／②システム共有／③共有＋上書き／④ユーザー所有、**FORCE RLS**、非所有者ロール、認証ブートストラップは特権経路 A1e-M1）
- **指摘（攻撃・権限軸）**：索引を「**各シャードDB内のテーブル**」として持つと決めた以上、**ADR-0001 のRLS体系に位置づける必要がある**が、どの分類にも素直に収まらない。
  - **`tenant_id` を持たない**（`user_id → シャード集合`）ので①③ではない。②システム共有に見えるが、システム共有は「全テナントが読んでよいマスタ」であり、**全ユーザーの所属関係を全テナントに開示してよいはずがない**（A2f-M3 が機密資産と認めたばかり）。
  - ④「ユーザー所有（user_id でRLS）」が形は近いが、**入口層はルーティングのために"他人の user_id"で引く**（そのユーザーがどのシャードにいるかを解決するのが目的）。自分の行しか見えないRLSでは**機能しない**。
  - つまり実態は **ADR-0001 A1e-M1 の「認証ブートストラップ＝特権経路」と同じ性質**（RLSを適用せず、専用ロールで、限定された操作のみ）だが、**v7 はそれを「専用ロール」と書いただけで、ADR-0001 の特権経路の枠組みに接続していない**。ADR-0001 は FORCE RLS を全テーブルに効かせる方針なので、**例外テーブルの存在は ADR-0001 側にも記載が要る**（さもなくばポリシーテストで「RLS未設定のテーブルがある」と検出されるか、逆に検出されずに素通りする）。
- **影響**：実装時に索引テーブルのアクセス制御が場当たりになる。最悪、②システム共有として全テナント可読で作られ、**任意のテナント利用者が法域内全ユーザーの所属を引ける**（A2f-M3 の懸念が最短経路で実現する）。
- **要修正（案）**：
  - A2e-M1 に「**索引テーブルは ADR-0001 の4分類の外にある「特権経路専用テーブル」であり、A1e-M1（認証ブートストラップ）と同じ扱いとする**」と明記する：RLSは適用せず（またはデフォルト拒否のポリシーを置き）、**アクセスは入口層の専用ロールのみ**、**アプリの通常ロール・テナント利用者からは一切参照不可**、**参照は監査対象**。
  - **ADR-0001 側にも波及**させる（特権経路テーブルの一覧に索引を加え、ポリシーテストの対象に「特権経路テーブルが通常ロールから読めないこと」を含める）。**この波及自体が、また ADR-0001 のレビューを1回要する**（本プロジェクトの運用ルール）。

## 🟢 Low（第6回）

### A2g-L1. ④集約層と⑤索引が重なるケースの優先順位が未定義
- **該当箇所**：v7 §2 構成要素表（④＝単一グループ>5万人のみ／⑤＝シャード化法域）／A2d-M1 注記「両者を混同しないこと」／A2c-M1「そのグループ内の横断・集計は読取専用の国内集約層で扱う」
- **指摘**：単一グループが5万人を超える配備では **④と⑤が両方存在する**。このとき、そのグループ内のユーザーの**本人起点の権威横断**は、A2c-M1（集約層で扱う）と §3.1(a)（complete ファンアウト）の**どちらで行うのか**が決まっていない。注記は「混同するな」と述べただけで、**重なる場合の優先順位**を与えていない。最大規模の顧客で給与の算出経路が未定義、という状態になる。
- **要修正（案）**：**本人起点横断は常に §3.1(a)（complete ファンアウト）を正**とし、④集約層は**グループ管理者の横断ダッシュボード（参考表示・非権威）専用**と役割を限定する、と明記する（そもそも A2f-H2 で「個人データを集約層へ複製しない」と決めたのだから、この整理が一貫する）。

### A2g-L2. 単一シャード→シャード化への移行手順が未解決事項にない
- **該当箇所**：v7 §2 A2e-M1 縮退スコープ①「**単一シャード法域では索引そのものが不要**」／§5 未解決事項（索引の物理設計はあるが**移行**がない）／A2d-M1「組織の再配置は設計フェーズで詰める」
- **指摘**：法域は成長してシャード化する（規模上限超で分割）ので、**「索引が存在しない状態」から「存在する状態」への移行は必ず一度は起きる**。移行中は (a) 索引の初期構築、(b) データ移動中のメンバーシップの二重所在、(c) 上位集合保証が成立しない期間、が発生するが、§5 に挙がっていない。A2d-M1 の「組織の再配置」は組織単位の移動の話で、索引の初期構築・移行中の一貫性は別論点。
- **要修正（案）**：§5 に「**シャード分割（単一→複数）の移行手順**：索引の初期構築（全メンバーシップからの一括生成）、移行中は**上位集合を意図的に広く取る**（移行元・移行先の両シャードを索引に載せる＝余りは安全）、切替後の縮小」を追加し、シャーディング設計／ADR-0019 で確定する。

### A2g-L3. (a) の正しさが運用規律に依存したままで、技術的強制がない
- **該当箇所**：v7 §3.1 注記「(a) を採るということは、**partial を確定値に昇格させない運用規律**（ADR-0008 §2.7）が権威用途の正しさを担保する、ということである。**ここが崩れると (a) の安全性は失われる**」
- **指摘**：**自己申告として正直だが、対策になっていない**。設計上の唯一の正しさの拠り所を「規律」に置くのは弱い（ADR-0008 §2.7 の「partial は no-store・確定用途不可」も規範であって強制ではない）。**規律は破れる**——急ぎの月次締めで「今回だけ partial で」が通る。
- **要修正（案）**：**技術的に強制する**。権威確定の処理（給与・法定集計の確定API/ジョブ）は、**完全性フラグを必須の入力として受け取り、`complete` 以外を渡されたら実行前に拒否する**（省略不可・既定値なし）。集約結果の型に完全性を埋め込み、**partial の値は確定関数に渡せない**ようにする（型で表現）。ADR-0008 §2.7／実装設計へ申し送る。

## 処置方針（第6回・推奨）

1. **A2g-H1 と A2g-M1 を一体で処置**する（配置の裁定をやり直す）。**(iii) 加入先へ同期＋非同期複製＋クォーラム読み**が、書込のAND・読取のSPOF・侵害面のいずれにも答える有力案。§3.2 として選択肢表を新設し、可用性・機密性・書込コスト・整合の4軸で記録する。
2. **A2g-M2**：complete を「**期間の完全性**（時点同時性は不要）」と定義し直し、部分取得の蓄積を許す。ADR-0008 §2.6.2 へ波及。
3. **A2g-M3**：索引を **ADR-0001 の特権経路専用テーブル**として位置づけ、**ADR-0001 へ波及**（波及後は ADR-0001 も1回レビューを回す）。
4. **A2g-L1/L2/L3**：役割の限定、移行手順の未解決事項化、完全性フラグの技術的強制。
5. 反映後、**第7回で3軸＋新規記述への攻撃を再適用**。

> **所見（メタ・第6回）**：今回の High は、**新旧の齟齬ではなく「v7で同時に足した2つの処置どうし」の接合**から出た。第5回の教訓は「追記を本文全体に行き渡らせる」だったが、**それだけでは足りない**——**同じ版で複数の処置を入れたら、処置どうしを突き合わせる**必要がある。個別の指摘に個別に答えると、答えは正しいのに合成が壊れる（A2f-H1の答えとA2f-M2の答えは、それぞれ単独では最適だった）。**処置の合成を1つの検査項目として、以後のレビューに含める。**
>
> あわせて、**ADR-0002 は v6→v7→（v8）と、ADR-0008 の波及を起点に3版連続で High/Medium が出続けている**。これは ADR-0002 自体の品質というより、**「1DB=1国（0002）× 横断（0005）× シャード化（規模）」という三者の接合が、本プロジェクトで最も難しい領域**であることの表れ（ADR-0008 の R4→R5→R6 で越境の縁が形を変えて出続けたのと同じ構造）。**ここは急がず、収束するまで回す**のが正しい——土台であり、Phase 3（労務）とPhase 4（海外）の両方が乗るため。

## 処置結果（v8・2026-07-26）

第6回の7件を**全件処置**した。中核は **A2g-H1／A2g-M1 の一体処置＝索引の配置と役割の再裁定**である。第6回で推奨した「クォーラム読み」は、**書込1台では R+W>N を満たさず単独では漏れを防げない**ため、そのままでは採らず、より根本的な整理に置き換えた。

**根本整理（§2 A2e-M1【v8】）**：上位集合保証を突き詰めると **最大の上位集合＝法域内の全シャード**であり、**全シャードへのファンアウトは常に安全**（権限のないシャードは各自のRLSが空を返す）。したがって**索引は「ファンアウト先を絞る＝性能最適化」であって正しさの必須要素ではない**。この一段の整理で、指摘の連鎖が解ける。

| ID | 反映先 | 処置の要点 |
|---|---|---|
| A2g-H1 | §2 A2e-M1【v8】／§3.2 | **v7の「加入＝全シャードへ同期」を撤回**。書込は**加入先シャードのローカルTXのみ**（メンバーシップ付与と同一トランザクション＝分散TX不要・他シャードの稼働に非依存）、他コピーへは非同期複製。読取は**用途別**（ログイン＝健全なコピーの和／日常横断＝索引／**権威確定＝索引を使わず全シャードファンアウト**）。**読取のSPOFも書込のANDも消えた**。v7本文の該当記述にも撤回を明記（同じ誤りを2箇所に残さない） |
| A2g-M1 | **§3.2（新設）**／§4帰結 | 配置の選択肢を **(i) 独立サービス／(ii) 全シャード複製＋加入同期〔v7案・撤回〕／(iii) 加入先ローカル同期＋非同期複製〔採用〕／(iv) 索引を持たない**の4案で、**可用性（読/書）・機密性（侵害面）・整合の難しさ**の4軸で比較記録。**機密性は「相殺できる」と言えない**ことを認め、**(iv) を正式な構成オプションとして併置**（機密性最優先／シャード数の少ない運用者が索引なしを選べる）。帰結に「**侵害面はシャード数 N に比例する**」を明記 |
| A2g-M2 | **§3.1.1（新設）**／ADR-0008 §2.6.2 | complete を **「期間の完全性」**と定義（時点同時性は要求しない）。締め済み期間は不変（ADR-0003 ①-a）なので**シャードごとに別時点で取得しても合算は正しい**。**部分取得を蓄積**して揃った時点で確定＝**ローリングメンテ中でも1台ずつ順に取って到達できる**。ADR-0008 へ波及 |
| A2g-M3 | §2 A2e-M1【v8】／**ADR-0001 v8** | 索引を **ADR-0001 の4分類の外＝「特権経路専用テーブル」**と位置づけ、A1e-M1（認証ブートストラップ）と同じ扱い（SECURITY DEFINER関数 or 専用ロールのみ・通常ロールからは一切参照不可・参照は監査）。**ADR-0001 に枠を追記し、ポリシーテスト項目と一覧化の未解決事項も追加** |
| A2g-L1 | A2d-M1 注記／構成要素表④ | **本人起点横断は常に §3.1(a) を正**、**④国内集約層はグループ管理者の横断ダッシュボード（参考表示・非権威）専用**に役割限定。個人に紐づく権威値を集約層から出す経路は設けない |
| A2g-L2 | §5 | **シャード分割（単一→複数）の移行手順**を未解決事項に追加（索引の一括初期構築／移行中は上位集合を意図的に広く取る／移行中の権威確定は全シャードファンアウトで成立） |
| A2g-L3 | **§3.1.2（新設）**／ADR-0008 §2.6.2 | partial の非昇格を**運用規律から技術的強制へ**。確定処理は完全性を**必須入力**として受け取り `complete` 以外を拒否、**型に完全性を埋め込み partial を確定関数に渡せない**ようにする |

**波及（要レビュー）**：本処置は **ADR-0001 v8**（特権経路専用テーブルの枠）と **ADR-0008**（§2.6.2 の complete 明確化）に及んだ。運用ルールにより、**ADR-0001 は再オープンし、この波及に対する敵対的レビューを1回通してから再クローズ**する。ADR-0008 は未クローズなので v10 の作業に合流させる。

> **次**：v8 に対する**第7回レビュー**。第6回の教訓（**処置どうしの合成を検査する**）を今回自ら適用したが、v8 でも新規記述が多い——特に **(a)「索引は性能最適化」という整理が本当に全用途で成り立つか**（ログインだけは正しさに関わると認めた点の帰結）、**(iv) 索引なし構成を併置したことで生じる2構成の分岐**、**§3.1.1 の「締め済み期間」の判定を誰がどう保証するか**は、いずれも v8 で初めて書いた判断であり重点的に攻撃する必要がある。

---

# 第7回レビュー（ADR-0002 v8 に対する敵対的再レビュー・3軸＋教訓3〜6の適用）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002 v8](ADR-0002-配備モデル-1DB-1国.md)（第6回7件を反映後） |
| レビュー日 | 2026-07-26 |
| レビュアー | Claude (Opus 5)／設計者＝査読者（自己レビュー） |
| 攻撃方針 | 3軸（要求／配備／権限）＋確立した検査項目を全適用：**教訓3（処置どうしの合成）／教訓4（「〜と同じ扱い」の射程）／教訓5（処置で何を失ったか）／教訓6（追記の行き渡り先6箇所）**。v8 で新しく書いたのは §2 A2e-M1【v8】の4項目（性能最適化の整理・配置・特権経路テーブル・撤回の明記）、§3.1.1／§3.1.2／§3.2（新設）、構成要素表④⑤、A2d-M1 注記、§4／§5。 |
| 指摘件数（第7回） | 計7件（**High 1**／Medium 3／Low 3）。**全件対応済**（v9反映） |
| 総括 | v8 の中核整理——**「最大の上位集合＝全シャードなので、全シャードファンアウトは常に安全。ゆえに索引は性能最適化であって正しさの必須要素ではない」**——は正しく、これによって分散トランザクションと読取SPOFを同時に解消できた。**しかしこの整理を最後まで突き詰めていない**：索引が担う情報 `user → シャード集合` は、**各シャードの①メンバーシップテーブルから導出できる**。ログイン時に認証専用ロールが各シャードのメンバーシップを問い合わせて和を取れば足り、それは **A1e-M1（①のテーブルを特権経路で読む）という既存の扱いで済む**。つまり**索引テーブルという新しいオブジェクトは本来不要**であり、それなのに (iii) を採用したことで、**ADR-0001（P0土台）に「RLSの防波堤の外」という恒久的な例外の枠を開けさせ、v8→v9→v10 の3版を費やさせた**（A2h-H1）。しかも §3.2(iv) を選んだ配備では**その枠の対象が0件**になる。**教訓5（処置で何を失ったか）の裏返し＝「処置で何を余計に背負わせたか」**を検査すべきだった。**High 1 につきクローズ不可。** |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

## サマリ（第7回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2h-H1 | 🟠 High | **【配備×権限軸】「索引は性能最適化」の整理を突き詰めると索引テーブル自体が不要**（`user→シャード集合`は各シャードの①メンバーシップから導出でき、A1e-M1 の既存の扱いで足る）。それなのに (iii) を採用し、**ADR-0001（P0土台）に「RLSの防波堤の外」という恒久的な例外の枠を新設させた**（v8→v9→v10 の3版）。§3.2(iv) の配備では枠の対象が0件。かつ **(iv) の定義「IdPに所属を持たせる」は真実源を二重化する誤った定式化** | 対応済（v9反映） |
| A2h-M1 | 🟡 Medium | **§3.1.1「締め済み期間」の判定責任が未定義**。誰が締めるのか／**締めはテナントごとに時期が違い、1テナントの締め遅延で受託者の給与確定が全体ブロック**／**労務サマリが ADR-0003 ①-a（追記型・不変）とは限らない**ため「締め済み＝以後不変」の根拠が弱い | 対応済（v9反映） |
| A2h-M2 | 🟡 Medium | **【権限軸】認証層が法域内の全シャードDBへ接続できるという「接続面の集中」が未評価**。認証専用ロールは A1e-M1 により RLS の外にあるため、ADR-0008 §2.6 の「実データの権限はRLSが最終防波堤」という多層防御が効かない。索引の複製の機密性（A2f-M3）とは別の面 | 対応済（v9反映） |
| A2h-M3 | 🟡 Medium | **§3.2 に (iii)/(iv) の2構成を併置したが、既定と切替条件が定量的でなく、下流ADR（0009/0019/0001）が両方を設計する負担・どちらを必須とするかが未明示**。OSSとして選択させるのは妥当だがコストが2倍になる | 対応済（v9反映） |
| A2h-L1 | 🟢 Low | **§1背景に「大国配備では本人起点横断がシャードを跨ぐ」という前提が未追記**（教訓6①）。課題設定が §2 の中にしかなく、背景だけを読むとこの緊張に気づけない | 対応済（v9反映） |
| A2h-L2 | 🟢 Low | **A2c-M1 本文に④集約層の役割限定（非権威）が届いていない**（教訓6⑥）。A2d-M1 の注記と構成要素表には書いたが、A2c-M1 は依然「グループ内の横断・集計は集約層で扱う」と権威/非権威の区別なく述べている | 対応済（v9反映） |
| A2h-L3 | 🟢 Low | **§3.1.2（partial非昇格を型で強制）は実装設計の規定で、配備モデルADRの範疇を超えている**。ADR-0002 は配備境界を決めるADRであり、確定APIの型設計は ADR-0008／実装の話（ADRの粒度が崩れる） | 対応済（v9反映） |

## 🟠 High（第7回）

### A2h-H1. 「索引は性能最適化」を突き詰めると索引テーブルは不要——それなのにP0土台に恒久的な例外を開けさせた
- **該当箇所**：v8 §2 A2e-M1【v8】「**索引は「正しさの必須要素」ではなく性能最適化である**」「最大の上位集合＝法域内の全シャードであり、**全シャードへファンアウトすることは常に安全**」／用途別表「ログイン／ブートストラップ＝**必須**（ここだけは正しさに関わる）」／【v8】配置「法域内の各シャードDBに同一内容の**ルーティングテーブル**として複製」／【v8】「索引テーブルは ADR-0001 の**特権経路専用テーブル**に位置づける」／§3.2 の4案（(iii)採用・**(iv) 索引を持たない**＝「ログインの所属解決をIdPに委ねる」）／ADR-0001 v8〜v10（**分類外の枠・専用スキーマ `priv`・網羅検査・SECURITY DEFINER関数群・ダンプマスキング**）／ADR-0001 A1e-M1（**①のテーブルを特権経路で読む**——テーブルの分類は①のまま）
- **指摘（攻撃・配備×権限軸／教訓5の裏返し）**：v8 の整理は正しいが、**一歩手前で止まっている**。
  1. **索引が持つ情報は導出可能**：`user_id → メンバーシップを持つシャード集合` は、**各シャードの①メンバーシップテーブルから `user_id` で引けば得られる**（各シャードは自分の分について常に正確）。**索引はその導出結果のキャッシュにすぎない**——v8 自身が「上位集合保証」「和を取れば必ず上位集合」と述べたのは、まさにこの導出の性質である。
  2. **導出なら新しいテーブル分類は要らない**：ログイン時に認証専用ロールが**法域内の各シャードのメンバーシップテーブルを（特権経路で）問い合わせて和を取る**——これは **ADR-0001 A1e-M1 が既に認めた扱い**（「①のテーブルを特権経路〔SECURITY DEFINER／認証専用ロール〕で読む。テーブルの分類は①のまま」）そのものである。**分類外の新しい枠は不要**。しかも v8 自身が「ログインは頻度が低いのでN回参照を許容」と書いており、**性能上の障害もない**（キャッシュも可）。
  3. **それなのに (iii) を採ったことで、ADR-0001 に恒久的な例外を強いた**：ADR-0002 v8 の A2g-M3 波及を起点に、ADR-0001 は **v8→v9→v10 の3版**をかけて「分類外＝特権経路専用テーブル」の枠を作った——**専用スキーマ `priv`／通常ロールへの `USAGE` 不付与／カタログ走査の網羅検査／マイグレーション規約とCI検出／SECURITY DEFINER 関数群の一覧化と `prosecdef` 走査／ダンプマスキング**。ADR-0001 v10 §4 は正直に「**この枠に限りRLSの防波堤は存在せず、権限付与の誤りが直接漏えいになる**」と認めている。**P0土台に恒久的な穴を開けた**わけである。
  4. **その穴の唯一の住人が、オプショナルなコンポーネントである**：v8 §3.2 は **(iv) 索引を持たない構成を正式なオプションとして併置**した。つまり索引は必須ではない。**(iv) を選んだ配備では、ADR-0001 の枠は対象テーブルが0件**になる——**恒久的な例外を、任意コンポーネント1つのために設けている**という不均衡。
  5. **(iv) の定式化も誤っている**：v8 は (iv) を「**ログインの所属解決をIdPに委ねる**（ADR-0009）」と書いたが、**メンバーシップの真実源は各シャードのDB（ADR-0005）**である。IdP に所属情報を持たせると**真実源が二重化**し、A2f-M2 で解決したはずの整合問題が IdP 側へ移る（IdP が古いとログインで欠落）。正しい (iv) は「**各シャードのメンバーシップを特権経路で直接問い合わせて和を取る**」であり、そうすれば真実源は二重化しない。**(iv) を実際より不利に書いたために、(iii) が選ばれた**という疑いがある（選択肢の記述バイアス）。
- **影響**：**ADR-0001（全設計の土台）に、避けられたはずの恒久的な例外が残る**。ADR-0017 の脅威モデルは以後ずっと「RLSが覆わない枠」を評価対象として抱え、実装は `priv` スキーマ・網羅検査・関数棚卸しという運用を恒久的に負う。**しかもその対価として得られるのは、性能最適化（ログイン時のN回クエリの回避）でしかない**。
- **要修正（案）**：**索引を「新しいテーブル」から「導出＋キャッシュ」へ格下げする**。
  1. **既定を「導出」にする**：`user → シャード集合` は**各シャードの①メンバーシップテーブルから特権経路で導出する**（A1e-M1 の既存の扱い。**ADR-0001 に新しい分類は不要**）。ログイン頻度は低く、キャッシュで足る。
  2. **§3.2 の (iv) を「IdPに委ねる」から「各シャードのメンバーシップから導出する」に書き換え、これを既定案として再評価**する。4案の比較表も、この正しい (iv) を含めて作り直す（現在の表は (iv) を不利に書いている）。
  3. **物理的な索引テーブルは、実測で必要性が示された場合の性能最適化として後付け**する（マテリアライズドキャッシュ）。**その場合に限り ADR-0001 の例外の枠が必要になる**と位置づけ直す——**枠の存在理由を「必要になったら作る」に変える**。
  4. **ADR-0001 への波及**：上記が確定したら、ADR-0001 の「分類外＝特権経路専用テーブル」の枠は **(a) 削除して A1e-M1 に統合**、または **(b) 「将来マテリアライズドキャッシュを導入する場合の枠」として条件付きで残す**、のいずれかへ改める。**ADR-0001 v10 の3版ぶんの記述を無駄にしないためにも、(b) が現実的**（枠の設計自体は良質で、必要になったときにそのまま使える）。ただし **ADR-0001 §4 の「P0土台に恒久的な穴がある」という帰結は「現時点では対象0件」へ緩められる**——これは土台の健全性として大きな改善である。
  5. **S4/S7 で「ログイン時のN回メンバーシップ問い合わせ」のコストを実測**し、索引の物理化が必要かを判断する（判断基準＝ログインのSLO）。

## 🟡 Medium（第7回）

### A2h-M1. 「締め済み期間」の判定責任と、テナント間の締めズレが未定義
- **該当箇所**：v8 §3.1.1「complete ＝**対象期間（締め済み）**について、対象シャード集合のすべてから応答を得たこと」「根拠：権威確定の対象は**締め後の確定済み期間**であり、その期間のデータは**もう変わらない**（ADR-0003 の①-a不変性）」「各シャードの応答には**どの期間までが締め済みか**を付す」
- **指摘（攻撃・要求軸）**：この設計は「締め済み＝以後不変」に全体重を預けているが、**その締めを誰がいつ行うのかが定義されていない**。
  1. **締めは業務プロセスであり、テナントごとに時期が違う**（各法人の経理・給与サイクル）。シャードは**複数テナントを含む**ため、「そのシャードの締め済み期間」は**シャード内の全テナントの締め状況の最小値**にならざるを得ない。**1テナントが締め遅延すると、そのシャード全体が権威確定に使えない**。
  2. **受託者の労務横断は複数テナント（複数シャード）にまたがる**——つまり**関係するどれか1テナントの締め遅延で、その受託者の給与確定が全体でブロックされる**。受託者は無関係な複数グループから請け負う（A2e-M1 が述べた受託の本質）ため、**締めのタイミングを揃える統制が効かない相手**である。これは現場で普通に起こる。
  3. **「締め済み＝不変」の根拠が弱い**：v8 は ADR-0003 の①-a（法定・追記型・不変）を根拠にしたが、**労務サマリ（LaborSummary）が①-a とは限らない**——打刻の訂正が入れば変わる②可逆確定の性質を持つ可能性がある。**「締め」は"それ以降訂正しない"という業務的合意であって、技術的な不変性とは別物**。両者を同一視したまま「別時点で取得しても合算は正しい」と結論づけている。
- **影響**：(a) 大国配備で給与確定が恒常的にブロックされ、§3.1 の (a) 案が実運用で成立しない（第6回 A2g-M2 で解いたはずの問題が別の形で戻る）。(b) 締め後の訂正が入ったとき、**既に確定した権威値と食い違う**が、その扱いが未定義（訂正イベント？再確定？）。
- **要修正（案）**：
  - **「締め」をデータ上の状態として定義**する：**テナント×期間の締めフラグ**（誰がいつ締めたか）を持ち、**シャードの応答は「テナントごとの締め済み期間」を返す**（シャード単位の最小値に丸めない）。権威確定は**関係するテナントすべてが当該期間を締めていること**を条件とする。
  - **締め遅延の可視化とエスカレーション**：確定ジョブは「**どのテナントの締めを待っているか**」を提示する（黙ってブロックしない）。ADR-0020 の可観測性へ。
  - **締め後の訂正の扱いを決める**：労務サマリが②可逆確定なら、**締め後の訂正は「訂正イベント＋再確定」**として扱い（ADR-0003 の③調整イベントと同型）、**既に出した権威値は差分で訂正する**（黙って上書きしない）。**労務サマリが①-a か②かをデータモデルで確定**させる（現状の根拠づけは不正確）。
  - この整理は **ADR-0003／データモデル設計書／ADR-0008 §2.6.2** へ波及する。

### A2h-M2. 認証層が全シャードDBへ接続できるという「接続面の集中」が未評価
- **該当箇所**：v8 §2 A2e-M1【v8】「ログインはキャッシュ→ミス時は**健全な全コピーから和を取る**」（＝認証層が複数シャードDBへ接続する）／A2f-M3（索引**データ**の機密性は論じた）／ADR-0001 A1e-M1（認証・所属解決は**RLS対象外の特権経路**）／ADR-0008 §2.6「入口層が担うのはtenant解決とルーティングまで、**実データの権限はDB層のRLSが最終防波堤**」
- **指摘（攻撃・権限軸）**：v8（および A2h-H1 の導出案でも同様）では、**認証層／認証専用ロールが法域内の全シャードDBへ接続できる必要がある**。これは A2f-M3 が論じた「索引データが N 箇所に複製される」という**データの面**とは別の、**接続の面の集中**である。
  - **多層防御が効かない**：ADR-0008 §2.6 は「入口層が侵害されてもRLSが最終防波堤」と設計したが、**認証専用ロールは A1e-M1 により RLS の外**にある。したがって**認証層の侵害＝法域内の全シャードへ、RLSを迂回して到達できる**——ADR-0002/0001 が積み上げた分離が、この1点で無効になる。
  - v8 は「索引はルーティングメタデータのみ・権限は各シャードで解決」（R6-M1）としたが、それは**索引の中身**の話であり、**認証層が持つ接続権限の範囲**は制限していない。
  - シャード数が増えるほど、この集中の価値（攻撃者にとって）は上がる。
- **要修正（案）**：
  - **認証専用ロールの権限を「メンバーシップ／索引の読取のみ」に厳格限定**する（業務テーブルへの権限を一切持たない）。**接続できることと、業務データを読めることを分離**する（ADR-0001 の `priv` スキーマ／`GRANT` 設計と接合し、**認証専用ロールが業務テーブルに権限を持たないことを網羅検査**に含める）。
  - **認証層の侵害を脅威モデルの独立項目**として ADR-0017 に立てる（「入口層の侵害」とは別に「**認証層＝RLSの外側の侵害**」を評価する）。
  - **接続の分離**：認証用の接続と業務用の接続をロール・接続プールの両面で分け、認証接続からは `SET LOCAL app.tenant_id` 後の業務クエリを実行できないようにする（経路の混在を防ぐ）。
  - ADR-0009（認証）／ADR-0017（脅威モデル）／ADR-0019（配備）へ申し送る。

### A2h-M3. 2構成の併置に既定と切替条件がなく、下流ADRの負担が未明示
- **該当箇所**：v8 §3.2「**(iv) を正式な構成オプションとして併置**し、**機密性を最優先する運用者・シャード数が少ない配備は索引を持たない構成を選べる**」（採用は (iii)）／§5「【v8】索引を持たない構成オプション（§3.2(iv)）の運用条件…**ADR-0009／ADR-0019 で確定**」
- **指摘（攻撃・配備軸）**：OSSとして運用者に選択させる判断自体は妥当（#13 の責務分界に合う）が、**併置のコストが評価されていない**。
  - **下流が2構成を設計・テスト・文書化しなければならない**：ADR-0009（ログインの所属解決が2通り）、ADR-0019（配備手順が2通り）、ADR-0001（枠の対象が有る配備と無い配備）。**どちらを必須実装とし、どちらを将来対応とするか**が明示されていない。
  - **切替条件が定量的でない**：「シャード数が少ない配備／機密性を最優先する運用者」は判断基準になっていない（何シャードから？ 機密性の何を測って？）。§5 に「運用条件を ADR-0009/0019 で確定」と丸投げしているが、**選択肢を併置した側が判断軸を示さないと、下流は両方を等価に扱うしかない**。
  - **移行の可否も未定義**：(iv) で始めた配備が (iii) へ移れるか（＝索引の後付け）、逆は可能か。A2g-L2 でシャード分割の移行は挙げたが、**構成間の移行**は挙がっていない。
- **要修正（案）**：**A2h-H1 の裁定とセットで整理する**（H1 が通れば「導出＝既定、索引の物理化＝性能最適化としての後付け」となり、**2構成の併置ではなく1つの既定＋後付けの最適化**という単純な形になる）。そのうえで：
  - **既定を明示**（導出）、**物理化の判断基準を定量化**（ログインSLO・シャード数・S4/S7の実測値）。
  - **構成間の移行**（後付け・撤去）を §5 に追加。
  - 下流ADRには「**既定のみ必須、物理化は将来対応**」と明示して負担を1系統に絞る。

## 🟢 Low（第7回）

### A2h-L1. §1背景にシャード化と横断の緊張が未追記（教訓6①）
- **該当箇所**：v8 §1 背景（「1つの稼働単位が何を境界とするか」「リージョン案からの収束」まで）／§2 A2-M2・A2b-M2・A2e-M1（**大国配備で本人起点横断がシャードを跨ぐ**という中核の緊張は §2 の中にしかない）
- **指摘**：本ADRが v6 以降ずっと格闘しているのは「**1DB=1国（配備）× 横断（権限）× 規模上限（シャード化）**」の三者の緊張であり、**これが §1 の課題設定に書かれていない**。背景だけを読むと「レジデンシーの境界を決めるADR」に見え、なぜ索引や complete の議論があるのかが分からない。教訓6①（追記のたびに§1背景の前提を確認する）の対象。
- **要修正（案）**：§1 に「**規模上限（#5）を超える法域は1国=複数DBになり、そこで『横断は単一DB内で完結する』（ADR-0001）という前提が崩れる**——本ADRはこの緊張の扱いまでを含めて定める」と前提を追記。

### A2h-L2. A2c-M1 本文に④集約層の役割限定が届いていない（教訓6⑥）
- **該当箇所**：v8 §2 A2c-M1「**そのグループ内の横断・集計は読取専用の国内集約層（集約DB/DWH、国内配置）で扱う**」（権威/非権威の区別なし）／A2d-M1 注記【v8】と構成要素表④には「**非権威・ダッシュボード専用**」と明記済み
- **指摘**：A2g-L1 の処置で注記と表には役割限定を書いたが、**A2c-M1 本文そのものは未修正**。A2c-M1 だけを読んだ設計者は「グループ内の横断・集計は集約層で扱う」と理解し、**権威用途にも使ってよいと読む**（§3.1 で否定したはずのこと）。教訓6⑥（関連する既存決定の射程）の対象。
- **要修正（案）**：A2c-M1 本文に「**ただし集約層は非権威（参考表示・ダッシュボード）専用**であり、**個人に紐づく権威値（給与・法定集計）は §3.1(a) の complete ファンアウトで確定する**（集約層へ個人データを複製しない）」を追記。

### A2h-L3. §3.1.2 が配備モデルADRの範疇を超えている
- **該当箇所**：v8 §3.1.2「**partial の非昇格は運用規律ではなく技術で強制する**：確定APIは完全性を必須入力として受け取り…**集約結果の型に完全性を埋め込み、partial の値を確定関数に渡せないようにする**」
- **指摘**：判断自体は正しい（A2g-L3 の処置として妥当）が、**これは実装設計の規定**である。ADR-0002 は「1システム＝1DB＝原則1国」という**配備境界**を決めるADRであり、**確定APIの型設計は ADR-0008（API方式）／実装設計の範疇**。配備ADRに実装詳細が入ると**ADRの粒度が崩れ、どこを見れば何が決まっているかが分からなくなる**（本プロジェクトは既に9本のADRが相互参照しており、粒度の混在はコストが高い）。
- **要修正（案）**：§3.1.2 は**要件の宣言だけを残す**（「partial を権威値に昇格させないことは、運用規律ではなく技術的に強制されなければならない」）。**実現方法（型で表現する等）は ADR-0008 §2.6.2 と実装設計へ委譲**し、本ADRからは参照のみとする（ADR-0008 側には既に反映済み）。

## 処置方針（第7回・推奨）

1. **A2h-H1 を最優先**：**索引を「新テーブル」から「導出＋キャッシュ」へ格下げ**し、§3.2 の (iv) を正しく定式化（IdP委譲ではなく**各シャードのメンバーシップから導出**）して4案を再評価する。既定を導出とし、**物理化は実測で必要が示された場合の後付け**とする。**ADR-0001 の枠は「将来物理化する場合の条件付きの枠」へ緩め、§4 の「恒久的な穴」を「現時点では対象0件」に改める**——P0土台の健全性として大きな改善になる。**A2h-M3 も同時に解ける**（2構成の併置が「1つの既定＋後付けの最適化」に単純化される）。
2. **A2h-M1**：「締め」をデータ上の状態（テナント×期間の締めフラグ）として定義し、**テナント間の締めズレの可視化とエスカレーション**、**締め後の訂正の扱い**を決める。**労務サマリが ADR-0003 の①-a か②かをデータモデルで確定**させる（現状の根拠づけは不正確）。
3. **A2h-M2**：認証専用ロールの権限を厳格限定し、**業務テーブルに権限を持たないことを網羅検査に含める**（ADR-0001 の `GRANT` 設計と接合）。**「認証層＝RLSの外側の侵害」を ADR-0017 の独立項目**に。
4. **A2h-L1/L2/L3**：§1背景に三者の緊張、A2c-M1 本文に非権威の限定、§3.1.2 は要件宣言だけ残して実装は ADR-0008 へ委譲。
5. 反映後、**第8回で再確認**。

> **所見（メタ・第7回）**：今回の High は **教訓5（処置で何を失ったか）の裏返し＝「処置で何を余計に背負わせたか」**という型である。v8 は「索引は性能最適化」という良い整理に到達したが、**その整理が示す当然の帰結（＝ならばテーブルとして持つ必要すらない）を辿らず、既にADR-0001に作らせた枠を前提として残した**。**上流ADRの処置が下流ADRに恒久的な負債を作ったとき、上流が整理を進めたら下流の負債も見直す**——この逆流の検査が抜けていた。**「波及は下流へ流すだけでなく、上流の改善時に下流へ戻して回収する」**を検査項目に加えるべきである（教訓7）。
>
> あわせて、**選択肢の記述バイアス**を自ら検出した点は記録に値する：§3.2 の (iv) を「IdPに所属を持たせる」と**実際より不利に定式化**したために (iii) が選ばれた。**却下する選択肢を、その選択肢の最良の形で書く**——これも検査項目にすべき（教訓7に含める）。

## 処置結果（v9・2026-07-26）

第7回の7件を**全件処置**した。中核は **A2h-H1＝§3.2 を作り直し、(iv') 「各シャードの①メンバーシップから導出＋キャッシュ」を既定に改めた**ことである。**あわせて ADR-0001 へ逆流させ、v8 の波及で作らせた「RLSの防波堤の外」の枠を条件付きに格下げして対象0件にした**（ADR-0001 v11）——**上流の整理が進んだことで下流の負債を回収した最初の例**（教訓7）。

| ID | 反映先 | 処置の要点 |
|---|---|---|
| A2h-H1 | §2 A2e-M1【v9】／**§3.2（作り直し）**／§4／§5／**ADR-0001 v11** | **既定＝導出**：`user→シャード集合` は各シャードの①メンバーシップから特権経路で引いて和を取る（**ADR-0001 A1e-M1 の既存の扱いで足り、新しいテーブル分類は不要**）。**派生を持たないので整合・鮮度・侵害面の問題群がまとめて消えた**（上位集合保証という仕組み自体が不要になる）。物理索引は**実測でSLOを満たさない場合の後付けの性能最適化**へ降格。**(iv) の誤った定式化（IdPに所属を持たせる＝真実源の二重化）を (iv'') として明示し、正しい (iv') と分けて記録**（教訓8）。**ADR-0001 の枠は条件付き＝現時点で対象0件** |
| A2h-M1 | §3.1.1【v9】／§5 | **締めを「テナント×期間」の状態として定義**（誰がいつ締めたか）。**シャード単位の最小値に丸めない**（1テナントの遅延でシャード全体が使えなくなるのを避ける）。**確定ジョブは「どのテナントの締めを待っているか」を提示**（黙ってブロックしない）。**「締め済み＝不変」の根拠を正し、労務サマリが①-a か②かをデータモデルで確定させる**申し送り。②なら**締め後の訂正は訂正イベント＋再確定**で差分訂正 |
| A2h-M2 | §2 A2e-M1【v9】／§4／§5／**ADR-0001 v11** | **認証層の「接続面の集中」**を明示——認証専用ロールはRLSの外（A1e-M1）で全シャードへ接続でき、**RLSが最終防波堤として効かない唯一の主体**。**権限を所属解決に必要な最小限の読取に限定し、業務テーブルに権限を持たないことを網羅検査**（ADR-0001 v11 に検査項目3bを追加）。**認証接続と業務接続を分離**。**「認証層＝RLSの外側の侵害」を ADR-0017 の独立項目**へ |
| A2h-M3 | §3.2／§4／§5 | **A2h-H1 と同時に解けた**：2構成の併置ではなく**「1つの既定（導出）＋後付けの最適化」**に単純化。**下流ADR（0009/0019/0001）が必須で設計するのは1系統**。物理化の判断基準（許容レイテンシ・Nの閾値）と**後付け・撤去の双方向の移行**を §5 へ |
| A2h-L1 | §1 背景 | **三者の緊張（1DB=1国 × 横断 × 規模上限）**を前提として明示（v6以降の改訂がほぼこれに由来することも記載）＝教訓6① |
| A2h-L2 | A2c-M1 本文 | **集約層は非権威（参考表示）専用**であり個人の権威値は §3.1(a) で確定する、を**本文にも**追記（注記と表だけでなく）＝教訓6⑥ |
| A2h-L3 | §3.1.2 | **要件宣言だけを残し、実現方法（型で強制）は ADR-0008 §2.6.2 と実装設計へ委譲**（配備ADRに実装詳細を置かない＝ADRの粒度を保つ） |

**教訓6（6箇所）の適用結果**：①§1背景＝A2h-L1で追記／②§2決定文＝**「グループ管理者の横断も本人起点横断も単一DB内で完結する」という総論の射程を明示**（シャード化法域では成立しない旨を追記。**第7回では指摘されていなかったが、6箇所チェックで自ら発見**）／③§3選択肢＝§3.2を作り直し／④§4帰結＝3項目追加／⑤§5未解決事項＝4項目追加／⑥関連既存決定＝A2c-M1（L2）・A2e-M1・ADR-0001（逆流）・ADR-0008（既反映）。

> **次**：v9 に対する**第8回レビュー**。v9 で新しく書いた判断——**導出方式のログイン時 N 回クエリが本当に許容できるか**（キャッシュミス時の挙動・シャード数の上限）、**「締めをテナント×期間で持つ」ことがデータモデルとADR-0003にどう波及するか**、**認証専用ロールの「最小限の読取」の範囲が本当に絞れるか**（所属解決にはメンバーシップ全体が要るのでは）——を重点的に攻撃する。

---

# 第8回レビュー（ADR-0002 v9 に対する敵対的再レビュー・3軸＋教訓3〜8の適用）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002 v9](ADR-0002-配備モデル-1DB-1国.md)（第7回7件を反映後） |
| レビュー日 | 2026-07-26 |
| レビュアー | Claude (Opus 5)／設計者＝査読者（自己レビュー） |
| 攻撃方針 | 3軸（要求／配備／権限）＋確立した検査項目を全適用：**教訓3（処置どうしの合成）／4（「〜と同じ扱い」の射程）／5（処置で何を失ったか）／6（追記の行き渡り先6箇所）／7（下流の負債の回収）／8（選択肢の記述バイアス）**。v9 の新規記述は §1背景・§2 line23の射程・§2 A2e-M1【v9】（導出への格下げ・認証層の接続集中）・A2c-M1・§3.1.1【v9】（締め）・§3.1.2の縮小・§3.2の作り直し・§4×3・§5×4・ADR-0001 v11 への逆流。 |
| 指摘件数（第8回） | 計7件（**High 1**／Medium 3／Low 3）。**全件対応済**（v10反映） |
| 総括 | v9 の方向（**物理的な索引を持たず導出＋キャッシュにする**）は正しく、**派生オブジェクトの整合・鮮度・侵害面という問題群がまとめて消え、ADR-0001 の例外も回収できた**——ここまでは良い。**しかし教訓5（処置で何を失ったか）の適用が甘かった**：物理索引（複製）は「**他シャードの分も自分のコピーに載っている**」という性質を持ち、それが **「対象シャード集合を知る手段」＝complete 判定の前提**を支えていた。導出に切り替えると、**停止中のシャードは応答しないため対象集合に現れず、「取得できたシャードすべてから応答を得た」＝complete と誤判定される**——**partial が構造的に発生しなくなり、A2g-M2／A2h-L3 で積み上げた完全性フラグの仕組みが根から無効化する**（給与の過少計上が `complete` として確定する）。**High 1 につきクローズ不可。** |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

## サマリ（第8回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A2i-H1 | 🟠 High | **【要求×配備軸／教訓5】「対象シャード集合」の定義が曖昧で、導出結果から作ると complete が常に真になる**。停止シャードは応答しないため対象集合に現れず、**そのシャードのメンバーシップを見落としたまま `complete` として権威確定**される（給与の過少計上）。**partial が構造的に発生せず、完全性フラグが無意味化**する。物理索引の複製が暗黙に支えていた「対象集合を知る手段」を失っている | 対応済（v10反映） |
| A2i-M1 | 🟡 Medium | **【権限軸】認証専用ロールの権限記述が自己矛盾**。「業務テーブルに権限を持たない」と「メンバーシップの読取のみ持つ」が同じ文にある——**メンバーシップは①テナント固有の業務テーブル**であり、両立しない。かつ A1e-M1 は**SECURITY DEFINER でRLSを回避して読む**方式なので、テーブル権限を与えるのは方式違い。**検査が書けない** | 対応済（v10反映） |
| A2i-M2 | 🟡 Medium | **「締め」の定義は ADR-0002（配備モデル）の範疇外**（A2h-L3 と同じ型の再発が同じ版で起きている）。締めは業務要件であり、要求仕様／データモデルの領域。かつ**「労務サマリが①-aか②か」の確定より先に締めフラグを決めており順序が逆**（①-aなら締めフラグは不要） | 対応済（v10反映） |
| A2i-M3 | 🟡 Medium | **【教訓7の不徹底】ADR-0008 への逆流が漏れている**。v9 で「索引テーブルを持たない・導出が既定」に改めたが、**ADR-0008 §2.6.1 は依然「法域ローカル user→shard ルーティング索引」を前提**に書かれている。ADR-0001 へは逆流させたのに ADR-0008 へは適用していない | 対応済（v10反映） |
| A2i-L1 | 🟢 Low | **構成要素表⑤が v9 前提のまま**（教訓6⑥の見落ち）。⑤の「必要になる条件＝法域がシャード化されている場合」は v9 では誤り（既定では索引を持たない）。**この表は「ADR-0019 のHA設計対象の根拠」と自ら位置づけた表**であり、既定で存在しないコンポーネントのHAを設計させてしまう | 対応済（v10反映） |
| A2i-L2 | 🟢 Low | **§3.2 の (i) の機密性評価が (iv\') 追加後の相対評価に更新されていない**。「◎ 守る箇所は1つ」だが、**(iv\') は「そもそも持たない」ので厳密に上位**。表内の相対評価が不整合 | 対応済（v10反映） |
| A2i-L3 | 🟢 Low | **導出結果のキャッシュの失効・無効化が未定義**。IND-L3（キャッシュの両方向即時無効化）は索引前提の議論だったが、**導出でもキャッシュするなら同じ論点が必要**（メンバーシップ変更時にキャッシュを無効化しないと、新規加入が見えない／脱退後も対象に残る） | 対応済（v10反映） |

## 🟠 High（第8回）

### A2i-H1. 「対象シャード集合」の定義が曖昧で complete が常に真になる
- **該当箇所**：v9 §2 A2e-M1【v9】「ログイン時に**認証専用ロールが法域内の各シャードのメンバーシップテーブルを特権経路で問い合わせ、和を取る**」／用途別表「権威確定＝**索引に頼らず法域内の全シャードへファンアウトして complete を判定**」／§3.1.1「complete ＝**対象シャード集合のすべてから応答を得たこと**」／§3.1「**partial は参考表示**」／§3.1.2「partial を権威値に昇格させない（技術で強制）」／v8【v8】縮退スコープ「**停止シャードの分は縮退（そのシャードは今読めないので実害なし）**」
- **指摘（攻撃・要求×配備軸／教訓5＝処置で何を失ったか）**：v9 は物理索引を捨てたが、**物理索引（全シャードへの複製）が暗黙に支えていた性質**を評価していない——**「他シャードの分も自分のコピーに載っている」ため、1台でも健全なコピーが読めれば "そのユーザーがどのシャードに所属しているか" の全体像が分かった**。これは単なる性能最適化ではなく、**「対象シャード集合を知る手段」**だった。
  - 導出では、**シャード C が停止していると C は応答しない**。すると「ユーザー U は C にメンバーシップを持たない」と「**C が停止しているので分からない**」を**区別できない**。
  - §3.1.1 の complete は「**対象シャード集合のすべてから応答を得たこと**」と定義されているが、**その対象集合を導出結果（＝応答のあったシャード）から作ると、定義上つねに「すべてから応答を得た」ことになる**——**partial が構造的に発生しない**。
  - 結果：**停止シャードに労務データを持つ受託者の権威確定が、そのシャードを見落としたまま `complete` として確定する**。**給与の過少計上**であり、しかも `complete` なので §3.1.2 の型による強制も**素通りする**（型は「partial を渡すな」を強制するだけで、誤って complete になった値は止められない）。
  - **A2g-M2（期間の完全性）→ A2g-L3／A2h-L3（partial の非昇格を型で強制）と積み上げてきた完全性フラグの仕組みが、根から無効化される。**
  - v8 の縮退スコープ「停止シャードの分は縮退（実害なし）」は**日常の参考表示については正しい**が、**権威確定については誤り**——ここを区別せずに導出へ切り替えた。
- **影響**：**給与・法定集計が静かに過少計上される**（最も重い種類の誤り。しかも `complete` の印が付くので気づけない）。ADR-0008 §2.6.2 の完全性フラグ、§2.7 の partial no-store、§3.1.2 の型強制——**完全性に関する設計のすべてが空文化する**。
- **要修正（案）**：**「対象シャード集合」を導出結果から作らず、静的な配備情報から与える**。
  1. **法域内のシャード構成一覧（どのシャードが存在するか）を、メンバーシップとは独立した"配備情報"として明示**する。これは **A2d-M1 の `組織→シャード` マップ／インフラ構成（ADR-0019）から既知**であり、**メンバーシップの派生ではないので停止の影響を受けない**（構成情報は静的）。
  2. **complete の判定基準を「法域内の全シャードから応答を得たか」に固定**する（対象集合＝**構成一覧の全シャード**）。**応答が無いシャードが1つでもあれば partial**。§3.1.1 の「対象シャード集合」という曖昧な語を「**構成一覧に載る法域内の全シャード**」に置き換える。
  3. **ログイン時の解決も同様に「不完全」を検出できるようにする**：応答しないシャードがある場合、**解決結果に「不完全」を付す**（そのユーザーが見えていないシャードに所属している可能性を明示）。日常の横断read は縮退表示（現状どおり）でよいが、**権威確定に使う経路では不完全な解決を使わない**。
  4. **構成一覧を §2 の構成要素表に位置づける**（A2i-L1 と一体で整理）。これは**新しいコンポーネントではなく、既に必要だった配備情報を明示するだけ**である（A2d-M1 のマップと同じもの）。
  5. **S4/S7 の検証項目に「1シャード停止時に権威確定が partial になること」を追加**する（complete と誤判定しないことをテストで担保）。

## 🟡 Medium（第8回）

### A2i-M1. 認証専用ロールの権限記述が自己矛盾しており検査が書けない
- **該当箇所**：v9 §2 A2e-M1【v9】「**認証専用ロールの権限は「メンバーシップ／索引の読取のみ」に厳格限定**し、**業務テーブルへの権限を一切持たせない**」／ADR-0001 v11 検査項目3b「**認証専用ロールが業務テーブル（①〜④）に権限を持たないこと**（メンバーシップ等、所属解決に必要な最小限の読取のみ）」／ADR-0001 A1e-M1「認証・所属解決を**SECURITY DEFINER関数**…で実行し（RLS対象外の特権経路）」
- **指摘（攻撃・権限軸）**：
  1. **自己矛盾**：**メンバーシップテーブルは①テナント固有＝業務テーブル**である。「業務テーブルに権限を一切持たせない」と「メンバーシップの読取は持つ」は**両立しない**。ADR-0001 v11 の検査項目3b は括弧で例外を入れているが、**「持たない」と「最小限は持つ」が同じ文にあるため、検査を実装できない**（何を許し何を禁じるかが判定不能）。A2h-M2 の処置が**検査可能な形になっていない**。
  2. **方式違い**：そもそも A1e-M1 は「**SECURITY DEFINER 関数でRLSを回避して読む**」方式である。認証専用ロールに**メンバーシップテーブルの `SELECT` を直接与えると、RLS がフェイルクローズで0行を返す**（テナント文脈未設定）ため機能しないか、あるいは RLS を回避するための追加のポリシー/設定が必要になり**A1f-H1 で解いた自己ブロックの議論に逆戻り**する。
  3. つまり正しい設計は「**認証専用ロールにテーブル権限を一切与えず、SECURITY DEFINER 関数の `EXECUTE` 権限だけを与える**」である——v9／v11 はそこを「テーブルの読取権限」と書いてしまっている。
- **要修正（案）**：
  - **認証専用ロールは、いかなるテーブルにも直接権限を持たない**。**所属解決は SECURITY DEFINER 関数の `EXECUTE` のみ**で行う（関数の所有者権限で①メンバーシップを読み、RLSを回避する＝A1e-M1 の方式そのまま）。
  - **これなら検査が書ける**：「**認証専用ロールがいかなるテーブルにも直接権限を持たないこと**」（カタログ走査で網羅可能）＋「**認証専用ロールに与えられた `EXECUTE` が、一覧化された SECURITY DEFINER 関数に限ること**」（A1g-L2 の `prosecdef` 走査と接合）。**例外の括弧が消え、判定可能になる**。
  - ADR-0001 v11 の検査項目3b をこの形に書き換える（**逆流**）。
  - 「接続できること」と「業務データを読めること」の分離は、**関数の中身（何を返すか）で制御**する——関数は `user_id → シャード集合` だけを返し、業務データを返さない。

### A2i-M2. 「締め」の定義が ADR-0002 の範疇を超えている（A2h-L3 と同型の即時再発）
- **該当箇所**：v9 §3.1.1【v9】「**締めは「テナント×期間」の状態とする（誰がいつ締めたかを保持）**」「シャードの応答は**テナントごとの締め済み期間**を返す」／§5【v9】「**締めの状態管理**：テナント×期間の締めフラグ（誰がいつ締めたか）のデータ設計」／§3.1.2 で自ら「**実現方法は本ADRの範疇外**」と認めた（A2h-L3）
- **指摘（攻撃・要求軸）**：**同じ版の中で、片方（§3.1.2）は範疇外として実装規定を削り、もう片方（§3.1.1）は新しいドメイン概念とそのデータ設計を決めている**——一貫していない。
  - **「締め」は業務要件**（給与計算・法定集計の締め処理）であり、**要求仕様（F-70〜72 労務）／データモデル設計書の領域**である。配備モデルADRが「テナント×期間の締めフラグを持つ」というデータ設計を決めるのは範疇外（A2h-L3 で学んだのと同じ型）。
  - **要求仕様に「締め」の定義があるかを確認していない**。無いなら**要求仕様への差し戻し**が必要（ADRが勝手に業務概念を導入すると、要求とADRが乖離する）。
  - **順序も逆**：v9 は「労務サマリが①-a か②かをデータモデルで確定させる（申し送り）」と書きながら、**締めフラグの設計を先に決めている**。**①-a（不変）なら締めフラグは不要**（不変なので締める意味がない）——**層の確定が先で、締めの必要性はその結果**である。
- **要修正（案）**：
  - §3.1.1 は **要件の提示に留める**：「**権威確定には『対象期間がもう変わらない』という状態が必要である。それをどう表現するか（不変性で保証するか／業務的な締めで保証するか）は、労務サマリが ADR-0003 のどの層かに依存する**」。
  - **層の確定（①-a か②か）を先行タスクとして §5 に明記**し、**締めの要件は要求仕様／データモデルへ差し戻す**（ADR-0002 は「配備が跨るときに何が必要か」を示すだけ）。
  - **テナント間の締めズレの可視化**（A2h-M1 で加えた良い指摘）は要件として残す——ただし「誰が締めたかを保持する」というデータ設計は書かない。

### A2i-M3. ADR-0008 への逆流が漏れている（教訓7の不徹底）
- **該当箇所**：v9 §2 A2e-M1【v9】「**既定＝導出**」「物理的な索引テーブルは持たない」／**ADR-0008 §2.6.1**（v7/v8 で粒度統一・上位集合保証を反映済みだが、依然「**法域ローカルの user→shard ルーティング索引**」を前提に記述）／ADR-0001 v11（**逆流済み**）
- **指摘（攻撃・文書整合／教訓7）**：v9 は教訓7（上流の整理が進んだら下流の負債を回収する）を **ADR-0001 に対しては実践した**（枠を条件付きへ格下げ）が、**ADR-0008 に対しては適用していない**。ADR-0008 §2.6.1 は「索引で解決する」前提のまま残っており、**2文書が食い違っている**（ADR-0002＝索引を持たない／ADR-0008＝索引で解決）。
  - **A2f-L2（粒度の揺れ）と同型の再発**：あのときも「両文書のいずれかを更新する際は他方との粒度整合を確認する」と §5 に書いたのに、**今回それを実行していない**。
  - ADR-0008 は未クローズ（第9回の Medium 5 が残る）なので、**v10 の作業に合流させれば手戻りは小さい**が、**放置すると実装が索引テーブルを作ってしまう**。
- **要修正（案）**：**ADR-0008 §2.6.1 を「導出が既定・物理索引は後付けの最適化」に合わせる**（ADR-0002 v9 §3.2 を参照）。あわせて **§2.6.2 の complete 判定に A2i-H1 の「対象集合＝構成一覧の全シャード」を反映**する（こちらは A2i-H1 の処置と一体）。**ADR-0008 v10 の作業項目に加える**。

## 🟢 Low（第8回）

### A2i-L1. 構成要素表⑤が v9 前提のまま（教訓6⑥の見落ち）
- **該当箇所**：v9 §2 構成要素表 ⑤「**法域ローカル user→shard 索引**…必要になる条件＝**法域がシャード化されている場合**（1国=複数DB。単一シャードなら不要）。**§3.2(iv)＝索引を持たない構成も選択可**」／同表の前文「**この表が ADR-0019 のHA設計対象の根拠になる**ため、要素の追加漏れは可用性設計の抜けに直結する」
- **指摘**：v9 で**既定は索引を持たない**に変わったのに、⑤の記述は v8 のまま（「シャード化されていれば必要」「(iv) も選択可」）。**自ら「ADR-0019 のHA設計対象の根拠」と位置づけた表**なので、**ADR-0019 が既定では存在しないコンポーネントのHAを設計する**ことになる。**教訓6⑥（関連する既存決定の射程）を v9 の6箇所チェックで見落とした**（①②③④⑤は更新したのに、⑥のうち構成要素表だけが漏れた）。
- **要修正（案）**：⑤を「**（後付けの最適化として物理化した場合のみ）user→shard マテリアライズドキャッシュ**」に改め、**必要条件を「導出のコストが実測でSLOを満たさず、物理化を裁定した場合」**に変更。**あわせて A2i-H1 の「法域内シャード構成一覧」を表に位置づける**（新規コンポーネントではなく既知の配備情報である旨を明記）。

### A2i-L2. §3.2 の (i) の機密性評価が (iv') 追加後に更新されていない
- **該当箇所**：v9 §3.2 表の (i) 独立サービス「機密性（侵害面）＝**◎ 守る箇所は1つ**」／(iv') 「◎ **関係グラフを別オブジェクトとして持たない**（侵害面が増えない）」
- **指摘**：**(iv') は「そもそも持たない」ので、(i) の「守る箇所は1つ」より厳密に上位**である。両方が「◎」だと相対評価が読み取れない（教訓8 で「却下する選択肢をその最良の形で書く」と決めたが、**採用案が明確に優位な軸では、その差を表に出す**べき）。
- **要修正（案）**：(i) の機密性を「○（守る箇所は1つだが、関係グラフというオブジェクト自体は存在する）」に改め、(iv') を「◎（オブジェクトが存在しない）」として差を明示する。

### A2i-L3. 導出結果のキャッシュの失効・無効化が未定義
- **該当箇所**：v9 §2 A2e-M1【v9】「結果は**短TTLでキャッシュする**（ログインは頻度が低い）」／ADR-0008 §2.6.1（IND-L3：**キャッシュの両方向即時無効化**——**縮小も拡大もメンバーシップ変更イベントで即時無効化**）
- **指摘**：IND-L3 で決めた「キャッシュの両方向即時無効化」は**索引を前提とした議論**だったが、**導出でもキャッシュするなら同じ論点がそのまま必要**である（メンバーシップ変更時にキャッシュを無効化しないと、**新規加入が TTL 満了まで見えない／脱退後も対象に残る**）。v9 は「短TTLでキャッシュする」と書いただけで、**無効化の要件を引き継いでいない**——**教訓4（「〜と同じ扱い」の射程確認）の裏側**で、**方式を変えたときに既存の要件を引き継ぎ損ねた**形。
- **要修正（案）**：「**導出結果のキャッシュは、メンバーシップ変更イベントで両方向（加入・脱退）に即時無効化する**（ADR-0008 §2.6.1 IND-L3 と同じ扱い）。**権威確定はキャッシュを使わない**（A2i-H1 のとおり全シャードへ都度問い合わせる）」を明記。

## 処置方針（第8回・推奨）

1. **A2i-H1 を最優先**：**「対象シャード集合＝法域内シャード構成一覧の全シャード（静的な配備情報）」と固定**し、**応答が無いシャードが1つでもあれば partial** とする。§3.1.1 の「対象シャード集合」という曖昧語を置き換え、**S4/S7 に「1シャード停止時に権威確定が partial になること」を追加**。**A2i-L1 と一体**で構成要素表も整理する。
2. **A2i-M1**：認証専用ロールは**テーブル権限ゼロ＋SECURITY DEFINER 関数の `EXECUTE` のみ**に改め、**検査可能な形**にする（ADR-0001 v11 の検査項目3b へ**逆流**）。
3. **A2i-M2**：§3.1.1 を**要件提示に留め**、締めのデータ設計と「労務サマリの層の確定」を**要求仕様／データモデルへ差し戻す**（順序も正す）。
4. **A2i-M3**：**ADR-0008 §2.6.1／§2.6.2 へ逆流**（導出が既定・complete の対象集合）。**ADR-0008 v10 の作業に合流**させる。
5. **A2i-L1/L2/L3**：構成要素表⑤の改訂と構成一覧の位置づけ、(i) の機密性評価、キャッシュの両方向無効化。
6. 反映後、**第9回で再確認**。

> **所見（メタ・第8回）**：今回の High も **教訓5（処置で何を失ったか）**の型だが、**前回よりも見つけにくい形**だった——v8→v9 で失ったのは「機能」ではなく「**判定の根拠（対象集合を知れること）**」という*暗黙の前提*である。**「何を失ったか」は機能一覧を見ても分からず、"その仕組みが何を保証していたか" を辿らないと出てこない**。教訓5 を「**処置で失った機能**」ではなく「**処置で失った保証**」を検査する、と精度を上げるべきである。
>
> あわせて **A2i-M2（範疇を超えた規定）と A2i-M3（逆流の漏れ）は、いずれも同じ版で自ら学んだ教訓（A2h-L3・教訓7）を守れていない**。**教訓は書いただけでは守られない**——v9 は教訓6（6箇所）を明示的に適用してチェックリスト化したから⑤以外は守れた。**教訓も同様にチェックリスト化して、版ごとに適用結果を記録する**運用にすべきである（v9 の処置結果に「教訓6の適用結果」を書いたのは良い形なので、**全教訓について書く**）。

## 処置結果（v10・2026-07-26）

第8回の7件を**全件処置**した。中核は **A2i-H1＝「法域内シャード構成一覧」を明示し、`complete` の対象集合を静的な配備情報に固定**したことである。あわせて **ADR-0008 §2.6.1/§2.6.2 と ADR-0001 v12 へ逆流**させた（教訓7）。

| ID | 反映先 | 処置の要点 |
|---|---|---|
| A2i-H1 | §2 A2e-M1【v10】／§3.1.1／**構成要素表⑥（新設）**／§4／§5 | **対象集合を導出結果から作ることを禁じ、「構成一覧に載る法域内の全シャード」に固定**。**応答が無いシャードが1つでもあれば `partial`**。構成一覧は **A2d-M1 のマップ／インフラ構成から既知の静的情報**であり新規コンポーネントではない（構成要素表⑥として位置づけ）。ログイン/日常横断の解決にも**完全/不完全**を付し、**権威確定に不完全な解決を使わない**。**S4/S7 に「1シャード停止時に権威確定が `partial` になること」を追加** |
| A2i-M1 | §2 A2e-M1【v10改訂】／§4／**ADR-0001 v12** | 認証専用ロールを **「テーブル権限ゼロ＋SECURITY DEFINER 関数の `EXECUTE` のみ」**に改めた。v9 の「メンバーシップの読取のみ」は**①業務テーブルなので自己矛盾で検査不能**、かつ A1e-M1 の方式（SECURITY DEFINER でRLS回避）と食い違っていた。**例外なしの検査が2つ書ける**（テーブル権限ゼロ／`EXECUTE` は一覧の関数に限る）。ADR-0001 の検査項目3bを改訂（逆流） |
| A2i-M2 | §3.1.1【v10改訂】／§5 | 締めを**要件提示に留め**、**データ設計・業務フロー・要求としての定義を要求仕様（F-70〜72）／データモデル／ADR-0003 へ差し戻し**。**順序も正した**——「労務サマリが①-a か②か」の層の確定が先行し、**①-a なら不変性そのものが保証で締めは不要**、②なら締めが必要。テナント単位で持つ必要性・締め待ちの提示・訂正時の差分再確定は**要件として残した** |
| A2i-M3 | **ADR-0008 §2.6.1／§2.6.2** | §2.6.1＝**既定は物理索引を持たず各シャードのメンバーシップから導出**（ADR-0001 A1e-M1 の既存扱いで足る／物理化は後付け）。§2.6.2＝**complete の対象集合は静的なシャード構成一覧から与える**（索引/導出の結果から作ってはならない）。ステータス欄の波及記録も更新 |
| A2i-L1 | 構成要素表⑤⑥／前文 | ⑤を「**user→shard マテリアライズドキャッシュ・既定では存在しない**」に改訂（必要条件＝物理化を裁定した場合のみ）。**⑥シャード構成一覧を追加**。前文の「ADR-0019 のHA設計対象」を**①〜④＋⑥**と明示 |
| A2i-L2 | §3.2 表 | (i) の機密性を **○（守る箇所は1つだが関係グラフというオブジェクト自体は存在する）**に改め、(iv') の ◎（オブジェクトが存在しない）との差を明示 |
| A2i-L3 | §2 A2e-M1【v10】 | **導出結果のキャッシュも加入・脱退の両方向でメンバーシップ変更イベントにより即時無効化**（IND-L3 の要件は方式を変えても引き継がれる）。**権威確定はキャッシュを使わず都度全シャードへ問い合わせる** |

### 各教訓の適用結果（教訓10 に従い記録）

| 教訓 | 適用結果 |
|---|---|
| 3（処置どうしの合成） | ✅ A2i-H1（構成一覧で complete を判定）と A2i-L3（キャッシュ無効化）を突き合わせ、**「権威確定はキャッシュを使わない」**と明記して矛盾を防いだ |
| 4（「〜と同じ扱い」の射程） | ✅ A2i-L3 で「IND-L3 と同じ扱い」と書く際、**その要件が何を防ぐためのものか（加入の欠落・脱退後の残存）を明記**して引き継いだ |
| 5／9（処置で失った**保証**） | ✅ 今回の処置で失う保証を点検——**構成一覧を静的情報にしたことで「一覧の管理が甘いとシャードを数え落とす」という新しい依存が生まれた**ため、§4帰結に明記し ADR-0019 のシャード追加/削除手順へ申し送った |
| 6（追記の行き渡り先6箇所） | ✅ ①§1背景＝変更なし（v9で三者の緊張を追記済み・今回の追記は射程内）／②§2決定文＝変更なし（v9で射程明示済み）／③§3選択肢＝§3.2 表の (i) を改訂（L2）／④§4帰結＝2項目追加／⑤§5未解決事項＝2項目追加＋順序を正した／⑥関連既存決定＝**構成要素表⑤⑥（L1）**・A2d-M1（構成一覧の出所）・ADR-0008（逆流）・ADR-0001（逆流） |
| 7（下流の負債の回収／逆流） | ✅ **ADR-0008（A2i-M3）と ADR-0001 v12（A2i-M1）の両方へ逆流**させた（第8回で「ADR-0001 にしか適用していない」と指摘された点を是正） |
| 8（却下する選択肢を最良の形で） | ✅ §3.2 の表を再点検。(i) の評価を**過大だった「◎」から「○」へ下げた**（採用案を有利に見せる方向の偏りも是正の対象とした） |

> **次**：v10 に対する**第9回レビュー**。v10 で新しく書いた判断——**構成一覧という新しい依存**（一覧の権威的な保持場所・シャード追加時の更新漏れがどう検出されるか）、**認証専用ロールが「テーブル権限ゼロ」で本当に所属解決できるか**（SECURITY DEFINER 関数が必要な情報をすべて返せるか）、**締めを差し戻したことで ADR-0002 側に残った要件が実装可能な粒度か**——を重点的に攻撃する。**なお ADR-0002 は第5〜8回で4回連続 High が出ており、第9回でも High が出る場合は自己レビューの限界と判断して独立レビューを入れる**（教訓1）。

---

# 第9回（独立レビュー・第三者敵対的レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0002 v10](ADR-0002-配備モデル-1DB-1国.md) |
| レビュー日 | 2026-07-26 |
| レビュアー | **独立エージェント**（設計者の意図・**既存レビュー記録・開発工程（教訓）を一切与えず**、ADR-0002 v10 ＋ 前提ADR〔0001/0003/0004/0005/0008〕＋ データモデル設計書 ＋ 要求仕様のみで敵対的レビュー） |
| このレビューの経緯 | **第5〜8回で High が4回連続**したため、教訓1（自己レビューの限界）に従って独立レビューを投入した。第7回で「毎回一段メタへ移動しているので収束に向かっている」と自己評価していたが、**それ自体が収束バイアスであった可能性を検証する**ため。 |
| 指摘件数（独立） | 計15件（**High 4／Medium 7／Low 4**）。**High 4件＋IND2-M6 は処置済**（v11/v12＋ADR-0001 v13／データモデル設計書 v5/v6／ADR-0003 v6/v7／ADR-0004 v5／ADR-0005 v5／ADR-0008 へ波及）。**Medium 6（M1〜M5・M7）／Low 4 は未処置** |
| 重大な結論 | **8巡の自己レビューが見落とした本物の High を4件検出**。うち2件は**実装不能／土台未成立**の指摘であり、**ADR-0002 だけでなく ADR-0001（クローズ済み前提）と データモデル設計書（M3・クローズ済み）の再オープンを要する**。**設計者による検証の結果、中核2件（IND2-H1／IND2-H3）はいずれも正しいことを確認した**（下記「設計者による検証」）。**ADR-0002 はクローズ不可。M3 のクローズも取り消しを要する。** |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

## サマリ（独立レビュー）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| IND2-H1 | 🟠 High | **既定方式（導出）の中核前提「SECURITY DEFINER でRLSを回避して①メンバーシップを読む」は、自プロジェクトの FORCE RLS 規約の下で成立しない**（フェイルクローズで0行＝ログイン不能）。かつ A2i-M1 の検査「認証専用ロールがテーブル権限を持たない」は、回避策（BYPASSRLS 所有者等）を採っても**合格したままで偽の安心を与える** | **対応済（v11＋波及）** |
| IND2-H2 | 🟠 High | **v10 で complete の対象集合を「法域内の全シャード」に固定した結果、権威確定の可用性が法域内全シャードの AND になった**。無関係なシャードの停止で**法域内全員**の給与確定が partial になる。v7→v8 で自ら撤回した「読取SPOFを消して書込ANDを作る」誤りを、**権威確定の読取側でやり直している**。§3.2 の裁定表（(iii) vs (iv')）が v10 の制約下で再評価されていない | **対応済（v11＋波及）** |
| IND2-H3 | 🟠 High | **本ADRの土台「横断は単一DB内で完結する」が、唯一の写像仕様（データモデル設計書 §4 の restrictive AND 合成）の下で成立しない**。LaborSummary は①＝restrictive `tenant_id = current` の対象なので、permissive `user_id = current` を足しても他テナント分は可視にならない。**シャード化以前に base case が未成立**。④には同じ論理で除外規定があるのに、横断経路には無い | **対応済（v11＋波及）** |
| IND2-H4 | 🟠 High | **「必ず一度起きる」シャード分割の移行が、索引の話だけで扱われており、①-a法定記録・監査ハッシュチェーンの移送が未検討**。データモデル設計書は監査チェーンを**パーティション単位**と確定しており、テナント単位の移送で**鎖が壊れアンカ署名と不一致＝改ざん耐性（要求#17）が失われる**。v9 で索引を撤回した結果、§5 のこの項目は**中身が空**になっている | **対応済（v11＋波及）** |
| IND2-M1 | 🟡 Medium | **【v8】「認証前の全シャード探索は行わない」と、既定 (iv')「各シャードのメンバーシップを問い合わせる」が正面から矛盾**（A2i-M1 自身がメンバーシップを①業務テーブルと認定）。かつ**未認証ログイン試行が N 倍に増幅**され、認証由来の負荷が ADR-0008 §2.7.1 の優先度クラスの外にある | 未対応 |
| IND2-M2 | 🟡 Medium | **中央ID障害時の縮退スコープ「単一国ユーザーは継続可」が実装不能**。「単一国ユーザーである」の判定情報が中央にしかなく、A2-M1 が「中央で解決してから」と順序を固定しているため、素直に実装すると**中央ID障害で全ログインが止まる**（任意コンポーネントの根拠が失われる） | 未対応 |
| IND2-M3 | 🟡 Medium | **既定方式が必須要素として要求する「導出結果キャッシュ」と「無効化イベント経路」が構成要素表（①〜⑥）に無く、HA・脅威モデルの対象外**。§3.2(iv') の「関係グラフを別オブジェクトとして持たない＝侵害面が増えない」は不正確（**認証層キャッシュに実在**）。**問題を認証層へ移動させただけ**という評価になり得る | 未対応 |
| IND2-M4 | 🟡 Medium | **上限超グループ（>5万人）では、グループ管理者の横断に権威用途の経路が存在しない**。④は非権威に固定され、§3.1 の裁定は本人起点横断のみが対象。要求3.2「原価/経営データ 全組織○」が黙って参考値に劣化。**(c) 案には「要求への差し戻しが要る」と書いたのに、同じ型のこちらには差し戻し項目が無い**（非対称） | 未対応 |
| IND2-M5 | 🟡 Medium | **「1国=複数DB」が要求仕様の用語定義（2章「1システム＝1DB＝原則1国」）・#5（拡張＝システム増設）・3.2・F-74注記と矛盾するのに、ADR-0002 §5 に要求仕様への差し戻し項目が無い**。ADR-0008 §5 は同種の申し送りをしている。**配備モデルの所有者が自分の用語破りを申し送っていない** | 未対応 |
| IND2-M6 | 🟡 Medium | **労務サマリの層について、クローズ済みのデータモデル設計書が既に「導出/②相当」と確定しており §5 の「確定させる」が空振り**。かつ「導出＝値として保持しない」（ADR-0003）なら**元データ（打刻＝①-b・削除可能）が変われば変わる**ので、期間完全性の前提が崩れる。3文書が3つの異なる答えを出している | **対応済（v12＋ADR-0003 v7）** |
| IND2-M7 | 🟡 Medium | **A2i-M1 の「関数の返り値で分離する」という安全性の根拠が、ブートストラップ全体には成立しない**。A1e-M1 は**許可tenant_id集合**まで特権経路の責務としており、A1b-M1 は集合検証も `SET LOCAL` 前に置く。「シャード集合のみ返す」基準では**必要な他の関数を一覧に載せられない**（載せれば基準違反・載せなければ検査で落ちる） | 未対応 |
| IND2-L1 | 🟢 Low | **§2【v8】表と §4 の記述が v9/v10 で stale**（4箇所）：§4 の「HA設計は①〜⑤」（§2 は①〜④＋⑥）／撤回済みの「加入時の同期更新」が運用項目に残存／却下された (iv) を「正式なオプションとして併置」と運用者へ提示／用途別表の「日常の横断read」行が索引前提のまま（既定での絞り込み方が読めない） | 未対応 |
| IND2-L2 | 🟢 Low | **A2-M3「エクスポート/集約機能の既定をPIIフリー」の射程が無限定**で、F-32/F-80/F-81（**作業者を必須項目に含む法定記録CSV**）・GDPRポータビリティ（PIIを出すのが目的）と衝突して読める | 未対応 |
| IND2-L3 | 🟢 Low | **A2-L2「データモデルに法域属性を持たせる」が「1システム内は原則1法域」と重複し、粒度（システム設定か行列か）が判定不能**。結果データモデル設計書には国コードも法域も入っていないが、ADR-0003 A3b-L1・ADR-0015 が法域を参照している | 未対応 |
| IND2-L4 | 🟢 Low | **(iii) 物理化への切替基準が「ログインSLO」だが、要求仕様 5.2.1 にログインのSLO行が存在しない**。かつシャード化の判定がユーザー数のみで、**データ量（圃場ポリゴン・時系列・写真）起因の限界に発火条件が無い**（要求#5 は「圃場枚数は極小〜巨大」） | 未対応 |

## 設計者による検証（中核2件）

独立レビューの指摘を鵜呑みにせず、**最も影響の大きい2件を設計者側でファイルを読んで検証した**。結果、**いずれも正しい**。

### IND2-H1 の検証：✅ 正しい

- PostgreSQL の実挙動：SECURITY DEFINER 関数内の RLS 判定は `current_user`＝**関数所有者**で行われ、**`FORCE ROW LEVEL SECURITY` は所有者のバイパスも止める**。RLS を無条件に迂回できるのは superuser と `BYPASSRLS` 属性ロールのみ。
- **本プロジェクトはこれを既に正しく認識している**：[データモデル設計書](../データモデル設計書.md) の `audit_log` 定義に「**FORCE RLS が効くため、書込は専用ロール audit_writer（BYPASSRLS）で行う**」と明記されている。ADR-0001 A1f-H1 も同じ論理で `priv` テーブルへの RLS 適用を却下した。
- 一方 [データモデル設計書 §4](../データモデル設計書.md) は「**全テーブル `FORCE ROW LEVEL SECURITY`**」「①テナント固有＝restrictive `tenant_id = current`（未設定/不正は全拒否）」と定めており、**メンバーシップは①なので FORCE RLS ＋ フェイルクローズの対象**。ログイン時点で `app.tenant_id` は未設定なので、**SECURITY DEFINER 関数から読んでも0行**。
- したがって ADR-0002 v10 A2i-M1 の「**A1e-M1 は SECURITY DEFINER でRLSを回避して読む方式**」という記述は**事実として誤り**であり、**既定方式 (iv')＝導出はこのADRに書かれたままでは実装できない**。
- **同じ知識がリポジトリ内（データモデル設計書 §6・ADR-0001 A1f-H1）にあるのに、メンバーシップには適用されていなかった**——レビューの死角そのものの形。
- **追加で発見**：データモデル設計書 §4 の④確定時の越境FK検証も「**特権関数（SECURITY DEFINER or BYPASSRLS）**」と書いており、**SECURITY DEFINER 単独では FORCE RLS 下で機能しない**（同一文書内で §6 は正しく BYPASSRLS を要求している）。**データモデル設計書自体に同型の不整合がある。**

### IND2-H3 の検証：✅ 正しい

- [データモデル設計書 §4](../データモデル設計書.md) の記述を確認：
  - 「PostgreSQL の RLS は、**restrictive を AND、permissive を OR** で結合する」
  - 「**restrictive**（AND・必ず絞る）｜テナント分離｜`tenant_id = current`（未設定/NULLは全拒否）｜**①/③テナント帰属テーブルのみ**」
  - 「| LaborSummary | **①テナント固有**（本人起点RLS例外） |」
  - 「| 本人起点（LaborSummaryのみ） | **permissive** | `user_id = current` |」
  - 「| グループ管理者横断 | **permissive** | `tenant_id = ANY(current_setting('app.allowed_tenants'))` |」
- PostgreSQL の実挙動：行が可視になるのは「**すべての restrictive を満たし、かつ少なくとも1つの permissive を満たす**」場合。したがって **LaborSummary は restrictive `tenant_id = current` で切られ、permissive `user_id = current` を足しても他テナント分は可視にならない**。**グループ管理者横断も同様**（permissive `tenant_id = ANY(allowed_tenants)` が restrictive `tenant_id = current` と AND され、実効は現テナントのみ）。
- **著者はこの意味論を正しく理解している**：④について「**④ユーザー所有はテナント帰属しない**ため、restrictive テナント分離の**対象外**（対象にすると tenant_id NULL の下書きが本人でも全拒否される）」と、まさに同じ論理で除外規定を置いている。**それなのに横断経路（LaborSummary・グループ管理者）には同じ除外規定が無い。**
- したがって **ADR-0002 §2／§4 が断定する「グループ管理者の横断も本人起点横断も、この単一DB内で完結する」は、下流の唯一の写像仕様の下では成立しない**。**シャード化以前に base case が未成立。**
- なお、テナントごとに `app.tenant_id` を設定してループする実装なら動くが、それは A1-M1 が「行単位サブクエリの性能劣化を避けるため集合注入」と決めた設計と別物であり、文書のどこにも書かれていない。
- **データモデル設計書 §7 の S1 結果は「横断」を PASS としている**が、§4 のポリシー構成では通らない——**PoC で通ったポリシーと文書化されたポリシーが同一である保証がない**。

## 波及範囲（クローズ済み文書の再オープンを要する）

| 文書 | 現状 | 波及 |
|---|---|---|
| [ADR-0002](ADR-0002-配備モデル-1DB-1国.md) | 提案中（再オープン） | **High 4 未処置。クローズ不可。** §3.2 の裁定（(iii) vs (iv')）の再評価を含む |
| [ADR-0001](ADR-0001-マルチテナント分離-行レベル-RLS.md) | 提案中（v12） | **A1e-M1（v6でクローズ済みの決定）の前提が崩れた**——「特権経路で①を読む」が A1-H2（全業務テーブルに FORCE RLS）と**当初から矛盾していた**。A1e-M1 の機構を選び直し（BYPASSRLS 所有者／ブートストラップ専用ポリシー）、**「RLSを迂回できる主体の全列挙」を網羅検査へ**（IND2-H1／M7） |
| [データモデル設計書](../データモデル設計書.md) | **クローズ（M3）** | **§4 に横断経路の restrictive 除外規定を追加**（IND2-H3）、**§4 の「SECURITY DEFINER or BYPASSRLS」を訂正**（IND2-H1）、**監査チェーンの単位をテナント境界で分割可能な形へ**（IND2-H4）、**労務サマリの層の再確定**（IND2-M6）、**§7 S1 の「横断」PASS の根拠となったポリシー本文の固定**。**→ M3 のクローズ取り消しが必要** |
| [ADR-0003](ADR-0003-データライフサイクル-追記型-論理削除-版履歴-監査.md) | クローズ | **「移送」という操作の位置づけ**（①-a の非削除原則との関係／IND2-H4）、**労務サマリの層と A3b-L2 の整合**（IND2-M6） |
| [ADR-0004](ADR-0004-DB-PostgreSQL-PostGIS.md) | クローズ | **監査ハッシュチェーンの単位**（A4b-M2 の選択肢のうち、シャード化＝テナント移送が起きる前提ではパーティション単位が不適）（IND2-H4） |
| [ADR-0008](ADR-0008-API方式.md) | 提案中（v9） | **移行中の同期push の宛先**（旧シャード宛の受理・切替の原子性／IND2-H4）、**認証由来負荷の優先度クラス**（IND2-M1） |
| 要求仕様 v1.0 | 確定 | **2章用語定義／#5 規模モデル／3.2／F-74注記の「1システム＝1DB＝1国」の改訂**（IND2-M5）、**5.2.1 にログインSLO 追加**（IND2-L4）、**上限超グループの横断の権威性**（IND2-M4） |

## 所見（メタ・第9回＝独立レビューの意義）

- **独立レビューは2度目も機能した**。ADR-0008 第8回に続き、**自己レビューが到達できない角度の High を検出**した。今回の4件は **PostgreSQL の FORCE RLS 実挙動／restrictive の AND 合成／監査チェーンの移送／要求仕様の用語定義の破り**——**いずれも過去8巡で一度も触れられていない角度**である。
- **「未処置ゼロ＝収束」という判定基準の限界が明確になった**。ADR-0002 は v10 時点で「未処置指摘ゼロ」だったが、**同じ角度からの反復レビューでは検出できない種類の穴**が4つ開いていた。**未処置ゼロは「これまでに当てた角度では見つからない」ことしか意味しない。**
- **第7回の自己評価「Highが毎回一段メタへ移動しているので収束に向かっている」は、収束バイアスだった**。メタへ移動していたのは**攻撃の角度が固定されていたから**であり、角度を変えれば土台側（base case・実装可否）に未検査の穴が残っていた。**「指摘が周縁化している」ことを収束の証拠にしてはならない。**
- **最も重い教訓は IND2-H1 の構造**：**必要な知識はリポジトリ内に既にあった**（データモデル設計書 §6 の `audit_writer（BYPASSRLS）`、ADR-0001 A1f-H1）。`priv` テーブルと `audit_log` については見抜けたのに、**メンバーシップには適用されなかった**。**同じ判断規則を、対象を変えて総当たりで適用する**という検査が抜けていた（＝「FORCE RLS 下で認証前に読む必要があるテーブルを全列挙したか」）。
- **修正順序は独立レビュアーの推奨に従う**：**IND2-H1（認証経路が物理的に動くか）→ IND2-H3（横断が単一DBで動くか）→ IND2-H2（complete の対象集合の再裁定）→ IND2-H4（分割移行）**。H2 の裁定は H1 の解決策（BYPASSRLS 所有者を認めるか等）でコストが変わるため、H1 を先に確定する。
- **ADR-0002 は9巡してなお High が出ている**。ただし今回の High は**自己レビューの角度の外**から来たものであり、**「反復を増やす」ことでは解決しない**。処置後は**もう一度独立レビュー**を入れ、**さらに「PostgreSQL の実挙動と文書の整合」を専門に検査する回**を設けるべきである（今回の4件のうち2件がこの類）。

## 処置結果（High 4件・v11＋波及・2026-07-26）

独立レビューの **High 4件を、推奨された順序（H1→H3→H2→H4）で処置**した。**6文書に波及**している。

### IND2-H1（認証経路が物理的に動くか）→ **ADR-0001 v13 §3.2 が本体**

**「SECURITY DEFINER なら RLS を回避できる」という前提を撤回**し、3案を比較して裁定した（ADR-0001 §3.2 新設）：

| 方式 | RLSを迂回する主体 | 侵害時の影響範囲 | 判定 |
|---|---|---|---|
| (γ) メンバーシップを FORCE RLS 対象外に | 増える（所有者バイパス復活） | 当該テーブル全件 | ✕ A1-H2 と正面衝突 |
| (α) 所属解決関数の所有者を `BYPASSRLS` に | **増える**（`audit_writer` に続き2つ目） | **メンバーシップ全件** | △ 動くが迂回主体を増やす |
| **(β) ロール限定ポリシー（採用）** | **増えない**（`auth_role` は非BYPASSRLS・非所有者） | **主張された1ユーザー分の行のみ** | ◎ RLSの枠内に留まる |

採用した (β) の構成：restrictive テナント分離を **`TO app_role` に限定**し、メンバーシップに **`auth_role` 限定の permissive ポリシー（`user_id = app.auth_subject`）を1本**置く。`auth_role` はメンバーシップの `SELECT` のみ。**(β) の安全性は「(テーブル, ロール) 全組合せの網羅検査」に依存する**ため、**検査が無いなら (β) を採ってはならない**と明記した。あわせて **「RLSを迂回できる主体の全列挙」**（superuser／`rolbypassrls`／`FORCE RLS` 未設定テーブルの所有者／restrictive 適用外ロール）をカタログ走査の検査項目に追加した。

### IND2-H3（横断が単一DBで動くか）→ **データモデル設計書 v5 §4 が本体**

**「横断は permissive を足す」では成立しない**（restrictive の AND で切られる）ことを明記し、写像を改めた：

- **restrictive テナント分離の述語を `tenant_id = ANY(app.allowed_tenants)` に統一**（通常ユーザーは自テナント1要素／グループ管理者は配下組織集合）→ **グループ管理者の横断が restrictive を通る**。permissive の横断ポリシーは廃止。
- **本人起点横断は真の例外**（`allowed_tenants` の外を読む）なので、**`labor_summary` に限り restrictive を `... OR user_id = app.user_id` とする**。**この形を持つテーブルを一覧化**し、一覧外に存在しないことを検査。
- **restrictive の述語形を4種（標準／本人起点例外／スコープ／④対象外）に限定**し、**①③に restrictive が無いものが存在しないこと**も検査。
- **検査は「遮断」だけでなく「横断が通ること」も見る**——遮断だけを検査すると**横断が動かない状態でも全部緑になる**（これが4巡＋αで見逃された理由）。
- **ADR-0002 §2 の「単一DB内で完結する」を断定から要件提示へ**書き換えた（依存している性質を「既に成立している」と書かない＝A2i-M2 と同じ型の是正）。
- **失ったもの（教訓5/9）**：restrictive の床が `tenant_id = current`（絶対）から `allowed_tenants`（アプリ計算）へ上がったため、**集合計算のバグの影響範囲が全テーブルへ広がった**。**A1b-M1 の注入時DB側検証がこの床を守る唯一の機構**になるので、**通常ユーザーでは `allowed_tenants` が1要素であることも注入時に検証**する、と明記した。
- **§7 S1 の「横断」PASS は根拠ポリシーを再確認・再実行**が必要（v4 の構成では通らないため、PASS したのは別構成だった可能性が高い）。

### IND2-H2（complete の対象集合の再裁定）

**対象集合＝構成一覧の全シャード（正しさの根拠）は維持しつつ、「長期離脱」マークによる例外**を設けた：

- **一時的な未応答（メンテ・障害）は `partial` のまま**、部分取得の蓄積とリトライで解決（時点同時性は不要なので1台ずつ順に取れる）。
- **構成一覧上で「長期離脱」と明示的にマークされたシャードのみ対象外にできる**。**マークは運用者の明示判断＋監査記録を要し、自動では絶対にしない**——シャードを外すことは「そのデータを権威値に数えない」＝**給与の過少計上を承知で受け入れる業務・法的判断**であり、設計が黙って決めてよいことではない。復旧後の**遡及再確定**の経路も要る。
- これで **AND が効くのは「リトライで解ける範囲」だけ**になり、長期離脱は人の判断で解ける。
- **§3.2 の (iv') 可用性評価を ◎ → ○ に是正**した（教訓8：採用案を有利に見せる偏りも是正の対象）。

### IND2-H4（分割移行）→ **ADR-0003 v6・ADR-0004 v5 へ波及**

v9 で索引を撤回した結果**中身が空になっていた** §5 の移行手順を実質化した：

- **ADR-0004 v5：監査ハッシュチェーンの単位を `(tenant_id, 期間)` に確定**（A4b-M2 の再裁定）。パーティション単位では**テナント移送の瞬間に鎖が壊れ、外部保存のアンカ署名が実データと一致しなくなる＝要求#17 の改ざん耐性が失われる**。テナント単位に分割すれば**チェーン丸ごとの移送になり鎖もアンカも無傷**。副次的に**並行性も改善**（テナント境界で直列化が分かれる＝A4b-M2 の当初の懸念にも有利）。代償は署名数の増加（ADR-0017）。
- **ADR-0003 v6 §2.9：第4の操作「移送」を定義**。削除でも改変でもないため不変性3層のどれにも当てはまらなかった。不変条件：内容と `row_hash` は不変／**チェーンは鎖ごと移送**／**移送そのものを監査に記録**（改ざんと区別が付かなければならない）／移送中は書込凍結／**移出元に「移送済み」の証跡を残す**（痕跡なく消えてはならない）。GDPR削除の対象・根拠には影響しない。
- **凍結→移送→切替の原子性**：切替点は A2d-M1 のルーティングマップ更新。古いキャッシュで旧シャードへ届いたら**「移送済み」応答→マップ再取得→新シャードへ再送**、**`event_uuid` の冪等で二重受理にならない**。
- **オフライン端末**：最長14日のアウトボックスがあるため**切替後に旧シャード宛 push が来る前提**。凍結中は**リトライ可能な一時エラー**（アウトボックスに残す＝0007 の不変条件を守る）。**ADR-0008 §2.2 に反映**。
- **移行中は凍結テナントの権威確定を行わない**（長期離脱マークは使わない——移行は一時的）。
- **移送後にチェーン検証・行数・冪等キーの一致を確認するまで切替完了としない**。手順はリハーサル可能にする。**S7 に「移行中の push が失われないこと」「移送後にチェーン検証が通ること」を追加**。

### 波及した文書（すべて再レビューが必要）

| 文書 | 版 | 波及内容 |
|---|---|---|
| [ADR-0001](ADR-0001-マルチテナント分離-行レベル-RLS.md) | **v13** | A1e-M1 の機構是正（§3.2 新設）・restrictive 述語の統一・網羅検査2種の追加 |
| [データモデル設計書](../データモデル設計書.md) | **v5** | §4 の restrictive/permissive 役割の是正・SECURITY DEFINER の訂正・チェーン単位・§7 S1 の再実行要 |
| [ADR-0003](ADR-0003-データライフサイクル-追記型-論理削除-版履歴-監査.md) | **v6** | §2.9「移送」の定義 |
| [ADR-0004](ADR-0004-DB-PostgreSQL-PostGIS.md) | **v5** | A4b-M2 の再裁定＝チェーン単位を `(tenant_id, 期間)` へ |
| [ADR-0008](ADR-0008-API方式.md) | v9＋波及 | §2.2 移送中の push／§2.6.2 長期離脱マーク |

### 残（未処置）

**Medium 7件（IND2-M1〜M7）・Low 4件（IND2-L1〜L4）は未処置。** うち **IND2-M6（労務サマリの層）は H2 の前提に関わる**——「対象期間のデータがもう変わらない」が成立するかは層の確定に依存するため、**H2 の裁定は M6 の解決を前提とする**。M6 はデータモデル設計書 v5 §8 に差し戻し項目として記載済み。

> **次**：Medium/Low の処置 → **再度の独立レビュー**＋**「PostgreSQL の実挙動と文書の整合」を専門に検査する回**（今回の High 4件のうち2件がこの類だったため）。あわせて**波及した5文書それぞれの再レビュー**が必要（運用ルール②）。

## 処置結果（IND2-M6・v12＋ADR-0003 v7・2026-07-26）

**H2 の裁定（期間の完全性）の前提だったため、Medium のうち M6 を先に解いた。**

### 結論：労務サマリは **①-a（法定・追記型・不変）として実体化**する

3文書で割れていた答え（データモデル設計書「導出/②相当」／ADR-0003 層①横断「導出値は保持しない」／ADR-0003 A3b-L2「法定保持される集計は保持」）を確定した。**A3b-L2 が正しく、データモデル設計書 v4 の「導出/②相当」が誤り**だった。

**判断の根拠として、まず「導出でよい」条件を3つに明文化した**（ADR-0003 v7 §2.10）。導出（値を保持しない）が成立するのは次のすべてを満たす集計に限る：

1. 元データが保持され続け、再計算が常に可能
2. その集計自体に法定保持義務がない
3. **集計値そのものを、元データを見せられない相手に渡す必要がない**

**労務サマリは3条件すべてに反する**：

1. 元データの**打刻（Punch）は①-b＝GDPR削除・保持期間経過で物理削除/匿名化が可能**——消えたら再計算できない。
2. **賃金台帳・勤怠記録は法定保持義務**があり、元データの保持では代替できない（年数は法域別＝E2）。
3. **これが決定的**：要求#18／3.2 の本人起点横断は「**参照先を労務サマリに限定し、作業日誌等を他テナント分まで露出させない**」ことが目的で、要求仕様7章も「労務サマリは打刻・作業日誌から**当該テナント内で集約して生成**し、本人横断ビューはそのサマリのみを束ねる」と定めている。**導出（都度計算）にすると、横断参照のたびに他テナントの打刻・作業日誌を読む経路が必要になり、要求#18 が防ごうとした越境露出そのものが発生する**。**実体化された最小化投影であることが要求の前提**だった。

### 確定した設計

| 項目 | 内容 |
|---|---|
| 層 | **①-a（法定・追記型・不変）**。確定後は UPDATE/DELETE しない |
| 「確定」の意味 | **①-a への追記**。これが **ADR-0002 §3.1.1 の「対象期間がもう変わらない」の技術的実体** |
| 訂正 | **訂正レコードの追記**（`corrects` で連鎖・ADR-0003 A3-M2 と同型）。**既出の権威値は差分訂正**（黙って上書きしない） |
| 未確定期間 | **テナント内で打刻から導出した参考値**。横断では**各シャードが自テナント内で導出した最小化サマリを返す**＝**PII（打刻）は越境しない**。**権威確定には使わない** |
| 最小化 | `user_id ＋ 期間 ＋ 稼働`のみ。**業務内容（圃場・作業種別等）は含まない**（要求#18／3.2） |
| GDPR | **打刻（①-b）は削除できるが、確定された労務サマリ（①-a）は法定保持**（A3b-L2 の意図／A3c-M1「法令保持が優先」）。削除要求時の線引きは **E3**。再識別可能にならないよう粒度・仮名化を確認（ADR-0017） |
| RLS | restrictive は**本人起点例外形** `tenant_id = ANY(app.allowed_tenants) OR user_id = app.user_id`（IND2-H3 の処置と整合）。WITH CHECK は自テナントのみ追記 |
| 業務としての「締め」 | **要求仕様（F-70〜72）／業務設計へ差し戻し**。本ADR群が定めるのは技術的意味だけ（A2i-M2 の是正を維持） |

### 波及した文書

| 文書 | 版 | 内容 |
|---|---|---|
| [ADR-0003](ADR-0003-データライフサイクル-追記型-論理削除-版履歴-監査.md) | **v7** | **§2.10 新設（本体）**＝層の確定＋「導出でよい」3条件の明文化。A3b-L2 に確定を反映。§5 に訂正の物理設計・E2/E3 を追加 |
| [データモデル設計書](../データモデル設計書.md) | **v6** | §1 設計原則に3条件、§2 分類表を①-a へ、**§6 に `labor_summary` の定義を新規追加**（`period`／`confirmed_at`／`corrects`／`retention_basis`・不変条件・未確定期間の扱い・RLS・GDPR）、§8 の差し戻し項目を解決済みへ |
| [ADR-0002](ADR-0002-配備モデル-1DB-1国.md) | **v12** | §3.1.1 の前提が成立（「確定＝①-aへの追記」）。§5 の差し戻し項目を解決済みへ |
| [ADR-0005](ADR-0005-権限モデル-RBAC-メンバーシップ.md) | **v5** | 本人起点横断の参照先は**確定済みの①-a レコード**であることを明記（未確定は各テナント内の導出＝参考値・PIIは越境しない） |

### 残（この件から派生）

- **訂正チェーンの有効値ビュー**と**既出権威値との差分訂正の経路**（データモデル設計書 §8）
- **未確定期間の導出ビュー**（テナント内・参考値）
- **業務としての「締め」の定義・主体・時期** → **要求仕様 F-70〜72 へ差し戻し**
- **法定保持年数（E2）／GDPR削除の線引き（E3）** → エスカレーション
- **本人起点 restrictive 例外のテスト**（`allowed_tenants` 外の自分の行が読めること／この形の restrictive が `labor_summary` 以外に無いこと）

> **所見**：M6 は「3文書で答えが割れている」という**文書間の不整合**として報告されたが、解いてみると**要求#18 の趣旨（越境露出を防ぐために最小化投影を経由させる）が、実体化を要求していた**——つまり**答えは要求仕様に既に書かれていた**（7章「当該テナント内で集約して生成し」）。**教訓12（同じ規則を対象を変えて総当たりで適用する）と同型で、今回は「要求の趣旨を設計判断に当てはめる」ことが抜けていた**。3つの文書が独立に「導出か保持か」を判断した結果、要求の趣旨を参照した A3b-L2 だけが正解に達していた。


---

# 【再レビュー記録】波及6文書の再レビュー（ADR-0001 第8回）で本文書に指摘があった項目

ADR-0002 第9回（独立レビュー）の処置により本文書も変更されたため、**運用ルール②に従って再レビュー**した。**結果は [レビュー記録_ADR-0001.md 第8回](レビュー記録_ADR-0001.md) に一括で記録**している（6文書を横断する指摘のため分散させない）。本文書に関わる指摘は次のとおり。

| ID | 重大度 | 内容 |
|---|---|---|
| A1h-M4 | 🟡 Medium | **`allowed_tenants` の意味（リクエスト単位の許可集合か、ユーザーの所属集合か）が未定義**。誤読すると**受託者の restrictive の床が全所属テナントに広がり、業務データ全体への越境になる**。ADR-0008 §2.6.2 の per-tenant 注入と接合して明記すべき（ADR-0001 v13／ADR-0005 v5 と共通） |

> 未処置。詳細・要修正案は ADR-0001 第8回の記録票を参照。
