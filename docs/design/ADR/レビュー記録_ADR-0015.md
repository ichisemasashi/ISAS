# ADR-0015 敵対的レビュー記録票

| 項目 | 内容 |
|---|---|
| 対象 | [ADR-0015：国際化基盤＝ICU/CLDR・UTC・通貨/単位](ADR-0015-国際化基盤-ICU-CLDR-UTC.md) |
| レビュー種別 | 敵対的レビュー（この決定のまま実装すると破綻・矛盾する箇所の摘出） |
| レビュー日 | 2026-07-17 |
| レビュアー | Claude (Opus 4.8) |
| 指摘件数（累計） | 第1回6件・第2回4件（計10件）を全件処置。以降も再レビューを継続。 |
| ステータス凡例 | 未対応 / 対応中 / 対応済 / 却下（リスク受容） |

> 所見：ICU/CLDR・UTC・テナント通貨の骨格は妥当。ただし**農業ドメイン固有の「日付の意味」（散布月日・収穫前日数・作業適期は"暦日"であり瞬間ではない）とUTC瞬間保存が衝突**する点が最大の穴——UTC保存＋TZ変換は"日付"を日跨ぎでずらし、農薬の収穫前日数（法定）計算を誤らせうる。加えてI18nLabelと共有マスタ/RLSの接合、通貨換算の欠落がある。

---

## サマリ

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A15-H1 | 🟠 High | 農業の「暦日」（散布月日・収穫前日数・作業適期・作付年度）は瞬間でなく圃場ローカルの日付。全時刻UTC保存＋TZ変換だと日跨ぎで日付がずれ、収穫前日数（法定）等の日数計算を誤る恐れ。瞬間(timestamptz)と暦日(date)を区別していない | **対応済**（v2：時点=timestamptz(UTC)/暦日=date(ローカル)を型区別、日数計算は圃場ローカル暦日） |
| A15-M1 | 🟡 Medium | I18nLabel（マスタ多言語）とADR-0001の共有マスタ（②/③）・RLSの接合が未定義。共有マスタの多言語ラベルは全テナント共有か、テナント別上書き翻訳を許すか | **対応済**（v2：共有翻訳＋テナント上書き翻訳の2層、ADR-0001③ポリシー適用） |
| A15-M2 | 🟡 Medium | 「金額集計は同一通貨内に限定」だが、グループ管理者/経営ダッシュボード（F-62）がテナント間で通貨が異なる場合、横断集計が出せない（要求仕様のダッシュボードが成立しない）。参考換算の扱いが未定義 | **対応済**（v2：基準通貨＋レート基準日で参考換算を明示表示） |
| A15-M3 | 🟡 Medium | 未翻訳/未レビューのラベルのフォールバック（表示言語が無い場合に何を出すか）と、法令・安全に関わる表示（農薬警告等）を機械翻訳のまま出す危険が未整理 | **対応済**（v2：フォールバック順定義、安全/法令表示はレビュー済みのみ） |
| A15-L1 | 🟢 Low | CLDRデータの更新（新通貨・書式変更）とオフライン端末への配布（PWAキャッシュ）の鮮度が未定義 | **対応済**（v2：バージョン管理＋PWA配布更新） |
| A15-L2 | 🟢 Low | 単位系の内部正規化（面積=㎡/ha/坪/エーカー等を内部で正規化保持し表示のみ変換）か、入力単位のまま保持かが未定義。計算・集計の一貫性に影響 | **対応済**（v2：内部は正規化単位保持、入力/表示で変換） |

---

## 🟠 High

### A15-H1. 「暦日」と「瞬間（UTC）」の混同
- **該当箇所**：決定「全時刻をUTC保存＋表示時ローカル変換」＋ 農業ドメイン（散布月日・収穫前日数・作業適期・作付年度）
- **指摘（攻撃）**：本ADRは「全時刻をUTC保存」とするが、農業ドメインの多くの日付は**瞬間（timestamp）ではなく圃場ローカルの「暦日（date）」**である：
  - **散布月日**（F-32必須項目）・**収穫前日数**（F-41、収穫日−最終散布日）・**作業適期ウィンドウ**（F-2E：4/20〜5/10）・**作付年度/シーズン**。
  これらを **UTCの瞬間で保存し表示時にTZ変換すると、日付が日跨ぎでずれる**（例：日本時間5/1 07:00の散布をUTC保存＝4/30 22:00 UTC→別TZ表示で4/30になり、収穫前日数の計算が1日ずれる）。**収穫前日数は法定（出荷可否）**なので、1日ずれは**出荷停止/違反**に直結する。瞬間（timestamptz）と暦日（date、TZなし）を区別していない。
- **影響**：法定の日数計算（収穫前日数）・適期判定・年度集計が日跨ぎで誤り、食品安全・法令に直結。
- **要修正（案）**：**「瞬間」と「暦日」を型で区別**する：ログ/監査/打刻など**時点は `timestamptz`（UTC保存）**、**散布月日・収穫前日数・適期・作付年度など"日付"は `date`（圃場ローカルの暦日、TZ変換しない）**で保持する。日数計算は圃場ローカル暦日で行う。UTC一律保存の方針を「時点はUTC／暦日はローカルdate」に改める。

## 🟡 Medium

### A15-M1. I18nLabelと共有マスタ/RLSの接合
- **該当箇所**：決定「マスタ多言語＝DBのI18nLabel」＋ ADR-0001（②システム共有／③共有＋テナント上書き）
- **指摘（攻撃）**：作業種別・作物等のマスタは、ADR-0001で「②共有」や「③共有＋テナント上書き」になり得る。その**多言語ラベル**は、**全テナント共有の翻訳**か、**テナント別に上書き翻訳を許す**かが未定義。共有マスタの翻訳をテナントが勝手に変えると共有性が崩れ、逆に共有固定だとテナント固有の呼称に対応できない。RLS（誰が翻訳を編集/参照できるか）も未接合。
- **要修正（案）**：I18nLabelを「**共有マスタの共有翻訳（特権編集）＋テナント上書き翻訳（自テナントのみ）**」の2層にし、ADR-0001の③（共有＋上書き）と同じポリシーを適用する、と接合を明記。

### A15-M2. 同一通貨限定と横断ダッシュボードの衝突
- **該当箇所**：決定「金額集計は同一通貨内に限定」＋ 経営ダッシュボード（F-62）・グループ管理者の横断
- **指摘（攻撃）**：グループ管理者や経営ダッシュボード（F-62）は**複数組織を横断**する。テナント間で**通貨が異なる**と、「同一通貨内に限定」では**横断の金額集計が出せない**（原価・売上の合算ができず、ダッシュボードが成立しない）。要求仕様のダッシュボード要件と衝突する。
- **要修正（案）**：**横断集計時は「基準通貨＋為替レート基準日」で参考換算した値を、換算である旨を明示して表示**する（原本は各通貨、換算は参考）。1システム=1国（ADR-0002）なら通常は同一通貨だが、法域内に複数通貨があり得る場合の参考換算方針を定める。

### A15-M3. 未翻訳フォールバックと安全表示
- **該当箇所**：決定「機械翻訳＋未レビューフラグ」＋ 農薬警告等の安全表示
- **指摘（攻撃）**：表示言語のラベルが**無い/未レビュー**のとき何を出すか（フォールバック順：ユーザー言語→英語→原文？）が未定義。さらに、**農薬使用基準の警告（F-41）・法令に関わる表示**を**未レビューの機械翻訳のまま出す**と、誤訳が食品安全・法令リスクになる。安全に関わる表示は機械翻訳のまま出してよいか未整理。
- **要修正（案）**：フォールバック順を定義（ユーザー言語→地域共通語→原文、未レビューは印付き）。**安全・法令に関わる表示は、ネイティブレビュー済みのみ表示（未レビュー機械訳は使わない）／または原文併記＋警告アイコン**とする。

## 🟢 Low

### A15-L1. CLDR更新とオフライン配布
- **該当箇所**：決定「CLDRのロケールデータ」
- **指摘**：CLDRは定期更新（新通貨・書式変更）される。**オフライン端末（PWA）へのロケールデータ配布・更新の鮮度**が未定義。
- **要修正（案）**：ロケールデータのバージョン管理とPWAキャッシュ更新方針を明記（ADR-0006/0007と連携）。

### A15-L2. 単位系の内部正規化
- **該当箇所**：決定「単位系はテナント/圃場設定」
- **指摘**：面積（㎡/ha/坪/エーカー）等を、**内部で正規化保持（例：㎡）し表示のみ変換**するか、**入力単位のまま保持**するかが未定義。後者だと集計・計算で単位混在の危険。
- **要修正（案）**：**内部は正規化単位（SI等）で保持し、表示/入力でロケール単位に変換**する、と方針を明記（F-04面積計算・集計の一貫性）。

---

## 処置方針（推奨）

1. **A15-H1** は決定に反映必須（瞬間＝UTC timestamptz／暦日＝ローカルdate の型区別）。法定日数計算の正しさの核。
2. **A15-M1〜M3** はI18nLabelの共有/上書き・横断参考換算・安全表示のフォールバックを明記。
3. **A15-L1・L2** はCLDR配布・単位正規化。

> 総括：i18n基盤の選定（ICU/CLDR・UTC）は妥当だが、**「全時刻UTC」を農業の"暦日"にも一律適用した点（A15-H1）が最大の穴**——収穫前日数（法定）等は圃場ローカル暦日で扱わないと1日ずれ、食品安全/法令に直結する。瞬間と暦日を型で分ければ解決。ADR-0001〜0005同様、2〜3回で収束の見込み。**これでP0土台6本のレビューが出そろう。**

---

# 第2回レビュー（ADR-0015 v2 に対する再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | ADR-0015 v2（第1回6件を反映した版） |
| レビュー日 | 2026-07-17 |
| 指摘件数 | High 0／Medium 1／Low 3（計4件） |

> 所見：A15-H1（型区別）は正しいが、**その一段深く「暦日をどのTZで確定するか」＝圃場TZ基準**が抜けている（A15b-M1）。入力端末のローカル日付をそのまま使うと、越境する受託者・深夜作業で散布月日がずれ、収穫前日数（法定）を再び誤る。High相当なし、収束方向。

## サマリ（第2回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A15b-M1 | 🟡 Medium | 暦日(date)の確定は「圃場TZでの発生時点→暦日」で行うべき。入力端末のローカル日付をそのまま使うと、圃場TZと異なる作業者/端末（越境受託者・深夜作業）で散布月日がずれ収穫前日数(法定)を誤る | **対応済**（v3：暦日は圃場TZ基準で確定、端末ローカル日付をそのまま使わない） |
| A15b-L1 | 🟢 Low | I18nLabel2層で、テナントが安全に関わるラベルを未レビュー上書き翻訳で置くと、A15-M3「安全表示はレビュー済みのみ」が上書きで破られうる | **対応済**（v3：安全ラベルはテナント上書きにもレビュー要/上書き不可） |
| A15b-L2 | 🟢 Low | 単位内部正規化(SI保持)は坪/反等の変換で往復丸め誤差が生じ、面積ベースの計算(補助金・資材費)や表示でずれうる。入力原単位の保持や丸め方針が未定義 | **対応済**（v3：原単位・原値も保持、表示は原値優先・丸め方針） |
| A15b-L3 | 🟢 Low | 参考換算(A15-M2)のFXレート出所・鮮度が未定義。1システム=1国では通常不要で、システム外集約に寄せる方が素直（in-systemに換算機構を持つ必要性の再検討） | **対応済**（v3：通貨またぎ主経路はシステム外集約、in-system参考換算は同一法域複数通貨に限る任意機能） |

## 🟡 Medium（第2回）

### A15b-M1. 暦日を確定するタイムゾーン（圃場TZ基準）
- **該当箇所**：A15-H1「暦日＝date（圃場ローカル）」＋ 散布月日の入力（オフライン・作業者の端末）
- **指摘（攻撃）**：A15-H1で「暦日はdate型・圃場ローカル」としたが、**その暦日を"どのTZで"確定するか**が抜けている。散布月日は**圃場のタイムゾーンにおける暦日**であるべき。ところが入力は作業者の端末（デバイスローカル時刻）で行われ、**作業者の端末TZが圃場TZと異なる**（越境する受託者、TZ跨ぎ地域、深夜作業で日付境界付近）場合、**端末ローカル日付をそのまま散布月日にすると圃場TZの暦日とずれる**。結果、収穫前日数（法定）が再び1日ずれうる（A15-H1が狙った是正が、TZ確定の欠落で貫徹しない）。
- **影響**：法定日数計算の日跨ぎずれが、TZ確定の欠落で残る。
- **要修正（案）**：**暦日は「圃場のTZでの発生時点→その暦日」で確定**する、と明記する。入力は端末の瞬間（timestamptz）を取り、**圃場所在地のTZに変換した暦日を散布月日として保存**する（端末ローカル日付をそのまま使わない）。圃場TZは圃場所在地から決定（PostGISの座標→TZ解決等）。

## 🟢 Low（第2回）

### A15b-L1. 安全ラベルのテナント上書きとレビュー
- **該当箇所**：A15-M1（テナント上書き翻訳）＋ A15-M3（安全表示はレビュー済みのみ）
- **指摘**：テナントが**安全に関わるラベル**を**未レビューの上書き翻訳**で置くと、A15-M3の「安全表示はレビュー済みのみ」が上書き経路で破られうる。
- **要修正（案）**：安全に関わるラベルは**テナント上書きにもレビューを要する／または上書き不可（共有のみ）**とし、A15-M3をテナント上書きにも適用する。

### A15b-L2. 単位正規化の往復丸め
- **該当箇所**：A15-L2（内部SI正規化）
- **指摘**：坪/反等をSI（㎡）で保持すると、**往復変換で丸め誤差**が生じ、面積課金・補助金・資材費（F-11）の計算や表示でずれうる。
- **要修正（案）**：**入力の原単位・原値も保持**し、表示は原値優先、計算は正規化値、丸め方針（有効桁）を明記。

### A15b-L3. 参考換算の出所・要否
- **該当箇所**：A15-M2（基準通貨＋レート基準日で参考換算）
- **指摘**：**FXレートの出所・鮮度**が未定義。かつ1システム=1国では通常同一通貨で換算は不要。in-system（F-62）に換算機構を持つより、**通貨またぎ集計はシステム外集約（ADR-0002/0014）に寄せる**方が素直。
- **要修正（案）**：in-systemの参考換算は「同一法域内に複数通貨がある場合に限る任意機能」とし、通貨またぎの主経路はシステム外集約とする。FXレートの出所・基準日・鮮度は導入時に定める。

## 処置方針（第2回・推奨）

1. **A15b-M1** は決定に反映（暦日は圃場TZ基準で確定）。A15-H1の是正を貫徹させる核。
2. **A15b-L1〜L3** は安全ラベル上書き・単位往復・参考換算の整理。

> 総括：2回のレビューで High は 1→0。第2回は「A15-H1の型区別を、TZ確定（圃場TZ基準）まで貫徹させる」（A15b-M1）が中心。これを反映すれば法定日数計算が正しくなる。**A15b反映後はADR-0015をクローズし、P0土台6本が完成**。

---

# 第3回レビュー（ADR-0015 v3 に対する再レビュー）

| 項目 | 内容 |
|---|---|
| 対象 | ADR-0015 v3（第2回4件を反映した版） |
| レビュー日 | 2026-07-17 |
| 指摘件数 | High 0／Medium 0／Low 3（計3件） |

> 所見：**High/Medium ともにゼロ**。骨格（型区別・圃場TZ確定・I18nLabel2層・単位正規化・システム外換算）は固まった。残るは実装アプローチの明確化（圃場TZの保持方法・安全ラベルの判定・正規化値の権威）のみで、いずれも設計フェーズ（M3）で吸収可能。ADR-0015は収束、クローズ相当。

## サマリ（第3回）

| ID | 重大度 | 表題 | ステータス |
|---|---|---|---|
| A15c-L1 | 🟢 Low | 圃場TZは「座標→TZ解決」を毎回するのでなく、圃場属性として保持（作成時に解決・編集可）すべき。座標→TZデータセットの依存・オフライン・TZルール更新の考慮が未定義 | 未対応 |
| A15c-L2 | 🟢 Low | 「安全に関わるラベル」の判定（どのラベルが安全か）のフラグ/指定方法が未定義（A5c-L1の機微分類と同型） | 未対応 |
| A15c-L3 | 🟢 Low | 単位の原値と正規化値の二重保持で、どちらが権威か・整合（正規化は導出/生成列にする）が未定義。二重保持はドリフトの温床 | 未対応 |

## 🟢 Low（第3回）

### A15c-L1. 圃場TZの保持方法とデータセット依存
- **該当箇所**：A15b-M1「圃場TZは圃場所在地（座標→TZ解決）から決定」
- **指摘**：座標→TZ解決は**TZ境界データセット**（timezone-boundary等）に依存し、**オフライン端末での解決**や**TZルール変更（政府のDST/TZ変更）**の考慮が要る。毎イベントで解決するのは重く、オフラインで解決できない恐れ。
- **要修正（案）**：**圃場TZは圃場の属性として保持**（作成時に座標→TZで解決、以後は属性を使用・手修正可）。端末は圃場と共にTZをキャッシュし、オフラインでも暦日を確定できる。TZデータ更新時の再解決方針も定める。

### A15c-L2. 「安全に関わるラベル」の判定
- **該当箇所**：A15b-L1「安全に関わるラベルはテナント上書きにもレビュー要」
- **指摘**：どのラベルが「安全に関わる」かの**判定（フラグ/指定）**が未定義（ADR-0005 A5c-L1の機微操作分類と同型の穴）。
- **要修正（案）**：ラベル/マスタに**「安全・法令関連」フラグ**を持たせ、そのラベルは上書きにもレビュー必須/上書き不可とする。対象（農薬関連・警告文言等）を指定。

### A15c-L3. 原値/正規化値の二重保持の権威
- **該当箇所**：A15b-L2「原単位・原値も保持、計算は正規化値」
- **指摘**：原値と正規化値を**両方保存**すると、片方だけ更新されて**ドリフト**する温床になる。どちらが権威かが未定義。
- **要修正（案）**：**原値（入力単位・入力値）を権威**とし、**正規化値は導出（生成列/計算）**で持つ（保存するなら生成列で常に原値と整合）。表示は原値優先、計算は導出正規化値。

## 処置方針（第3回・推奨）

1. **A15c-L1〜L3** は実装アプローチの明確化（圃場TZ属性化・安全ラベルフラグ・正規化は導出）。いずれもM3（データモデル設計）で吸収可能。

> 総括：3回のレビューで High は 1→0→0、第3回は**High/Medium ゼロ**で実装詳細のLowのみ。ADR-0015は収束。**A15c反映後はADR-0015をクローズし、P0土台6本（0001/0002/0003/0004/0005/0015）が完成**する。以降の細部はM3（データモデル設計）へ。
