# ISAS 運用文書 v1.0 敵対的レビュー記録

| 項目 | 内容 |
|---|---|
| 対象 | [管理者guide v1.1](システム管理者運用ガイド.md)、[利用者guide](システム利用者ガイド.md)、[既存runbook](../operations/README.md)、[配備別サービス操作手順](../operations/配備別サービス操作手順.md) |
| 実施日 | 2026-08-15 |
| 方法 | 実装画面／capability、要求RBAC、ADR-0017/0019/0020/0021、CSV guide、品質記録との横断照合 |
| 指摘 | High 5、Medium 6、Low 1。文書版1.1までに全件処置 |
| 結論 | 文書範囲の残存High 0／Medium 0。本番環境の未実装gateは「文書で解決」とせずrelease blockerとして明示 |

## 指摘と処置

| ID | 重大度 | 攻撃／欠落 | 処置 |
|---|---|---|---|
| OM-H1 | High | `v1.0.0`表記だけを見ると、本番IdP／KMS／DR未完でもproduction readyと誤解できる | 両guide、index、release noteに「baselineであり本番承認ではない」を固定し、未完gateを列挙 |
| OM-H2 | High | 利用者へlogoutを要求しても、現行Webのaccount menuはproduction logoutへ未接続で、共有端末からsessionを終了できない | 利用者guideの既知制限と管理者受入条件にproduction logout/back-channel失効を追加。未接続なら共有端末運用を禁止 |
| OM-H3 | High | cloud／backup製品未選定なのに具体commandを作ると、誤った法域やbackupを破壊できる | guideは検証点と停止条件に限定し、provider固有commandは二人review済みadapter runbookへ分離 |
| OM-H4 | High | 管理者guideに起動・終了の実command、正常判定、終了確認がなく、UT／spike／productionを誤認できる | §5へ対象matrix、UT PWAと検証DBの実command・期待結果・停止・再開・破棄警告を追加。実際にPWAの起動、HTTP疎通、`Ctrl+C`終了を確認 |
| OM-H5 | High | production runtimeが未実装なのに一般的なproduction初期化順だけがあり、運用者が`pnpm dev`やspike DBを本番起動へ流用し得る | production起動不能を明記し、配備別の実command、期待結果、影響、abort、staging証跡を必須にする操作手順templateを追加。未記入は`BLOCKED` |
| OM-M1 | Medium | 「端末保存」を「同期済み」と誤認し、browser data消去で法定記録を失う | 全画面状態表、offline章、update、終業checklist、troubleshootingで未同期0確認を反復 |
| OM-M2 | Medium | 農薬警告のoverrideが単なる確認boxとして使われる | 現物label／法令を優先し、根拠・責任者・監査なしoverride禁止を明記 |
| OM-M3 | Medium | 複数tenant利用者が組織を誤って記録し、後で他tenantへ移せない | login、常時状態、作業手順、終業checklistへ選択組織確認を追加 |
| OM-M4 | Medium | 日本語／英語切替を「全画面翻訳済み」と誤表示する | baselineは未翻訳日本語が残ると明記し、推測せず問い合わせる運用にした |
| OM-M5 | Medium | 緊急／農薬／privacy窓口が空のまま利用開始される | `未設定`台帳と「未設定のまま開始しない」を両guideへ設置 |
| OM-M6 | Medium | 権限、指示、review、競合、在庫、CSV、点検、backup、release、incidentが方針中心で、操作場所・button・期待結果が不足 | 管理者guide §§6〜14へ画面遷移、入力、button、成功message、停止条件、実command参照を追加。未実装画面／adapterはDB直接操作で代替せず`BLOCKED`とした |
| OM-L1 | Low | 管理者guide、利用者guide、ADR、runbookのどれが正本か曖昧 | manual indexで操作／緊急実行／設計理由／進捗／CSV列の正本を分離 |

## 網羅性確認

| 領域 | 管理者 | 利用者 |
|---|---|---|
| 対象／責任／既知制限 | 対応 | 対応 |
| 導入／login／MFA／端末 | 対応 | 対応 |
| tenant／role／scope／失効 | 対応 | 対応 |
| 今日／打刻／指示／予定 | 管理・調査 | 操作手順 |
| 日誌／写真／訂正／review | 管理・監査 | 操作手順 |
| 農薬／在庫／圃場 | master・裁定 | 現場操作・安全行動 |
| offline／同期／競合／PWA更新 | 監視・回復 | 状態・禁止操作 |
| CSV／移行／export | 権限・保護 | 操作手順 |
| SLO／backup／DR／release | 対応 | 影響時の行動 |
| security／privacy／incident | 対応 | 利用者規則・緊急報告 |
| troubleshooting／問い合わせ／廃止 | 対応 | 対応 |

文書で未実装を覆い隠していないこと、利用者にRLS無効化やoutbox削除等の危険な回避を案内していないことを確認した。

## 文書版1.1の実行確認

| 対象 | 実施結果 |
|---|---|
| UT／デモPWA起動 | `apps/web`で`pnpm dev --host 127.0.0.1`を実行し、Vite 8.2.1が`127.0.0.1:4173`でreadyになることを確認 |
| HTTP疎通 | `curl --fail --silent --show-error 'http://127.0.0.1:4173/?ut=1'`が0終了 |
| 終了 | 起動terminalで`Ctrl+C`後、同じ`curl`が接続不能になることを確認 |
| Compose定義 | `docker compose -f spikes/docker-compose.yml config --quiet`が0終了 |
| Production操作 | runtime／provider未実装のため未実施。配備別手順の全`未設定`解消とstaging証跡をrelease blockerとして維持 |
