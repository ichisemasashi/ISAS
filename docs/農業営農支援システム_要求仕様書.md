# 営農支援Webアプリケーション 要求仕様書（ドラフト v0.8）

> 更新履歴：
> - v0.8 = オフライン競合解決の個別ルールを確定（5.1.1）：LWW適用は**オフライン編集可能なデータに限定**（日誌・写真・作業ステータス・資材入出庫イベント）／作業ステータス＝**前進優先マージ**／在庫＝**残高直接編集禁止・入出庫イベント追記方式**（オフライン出庫記録に対応）／原価等の導出値＝保存せず再計算／計画系・マスタ・権限＝**オンライン専用＋楽観ロック**（作業の再割当を含む）／時刻＝端末時刻とサーバー受理時刻の二本立て。データモデルに入出庫イベント（StockEvent）を追加。
> - v0.7 = **対象地域を日本国内＋海外圃場に拡大**（1.3）。GAP・環境保全型農業直接支払の方針を確定（基準＝国際水準GAPガイドラインの記録項目／環境直払は全国共通様式のテンプレート化＋都道府県特認取組はマスタ対応／優先対象自治体＝**山形県**（グループ圃場所在地））。多言語対応は**全世界の言語を扱えるi18n基盤**（RTL・ロケール対応）とし、初期リリースは日英・以降は現場構成に応じ順次追加。翻訳運用はハイブリッド方式を確定。海外対応に伴う新規検討事項を9.1へ追加。
> - v0.6 = WAGRI調査結果を確定（契約＝お試し会員で検証後にデータ利用・提供会員へ移行・農業法人割引を申請／実証パートナー第一候補＝クボタ・第二候補＝井関、ヤンマーは直接契約ルートで第2アダプタ以降／データ保持12ヶ月制限を踏まえ日次バッチ取込＋自前永続化）。MITデータ救出＝グループ内に利用者がいるため実施（アグリノート経由CSV回収＋三菱へ清算前に直接照会）。
> - v0.5 = 三菱マヒンドラ農機の解散発表（2026年3月、農機事業撤退・IoT基盤「MITサービス」の外部連携も2026年3月末終了）を受け、初回農機アダプタを三菱から**農機OpenAPI（WAGRI）準拠アダプタ**に変更（F-56）。マルチテナント分離方式を**行レベル分離（共有スキーマ＋tenant_id＋PostgreSQL RLS）**に確定。技術構成（7章）・確定事項（9章）・残検討事項（9.1）へ反映。
> - v0.4 = 9.1の残検討事項を確定（テナント＝グループ/連合利用によりマルチテナント／JGAP以外は将来対応（都道府県GAP・環境保全型農業直接支払）／オフライン競合＝最終更新優先／農機アダプタ初回対応＝三菱／多言語対応＝必要）。技術構成・機能・非機能へ反映。
> - v0.3 = 9章の未決事項を確定（対象作物＝全種別／農機連携＝ベンダー非依存・API＋ファイル両対応／GAP＝JGAP準拠・農薬記録必須項目確定・CSV出力／オフライン必須／規模＝極小〜最大5万ユーザー／自社利用／既存データ移行あり）。関連する機能・非機能要求へ反映。
> - v0.2 = 作業指示に「スケジュール・工程管理（ガントチャート）」（4.3.1）を追加し、データモデル・フェーズ案を整合。

> KSAS（クボタスマートアグリシステム）を参考にした、大規模・法人経営向け営農支援システムの要求整理。
> 本書は「何を作るか（What）」を合意するための要求仕様であり、設計仕様（How）は別途とする。

---

## 1. 背景・目的

### 1.1 背景
- 圃場の集約・大規模化が進み、紙・Excelでの管理が限界に達している。
- 複数の従業員・作業受託先が広範囲の圃場で作業するため、「誰が・いつ・どこで・何を」の共有が困難。
- 収量・品質・コストのデータが分散し、経営判断（PDCA）に活かせていない。

### 1.2 目的
- 圃場・作付・作業・資材・収穫データを一元管理し「見える化」する。
- 現場（スマホ）と管理（PC）をつなぎ、作業指示と記録を効率化する。
- 蓄積データを分析し、収量・品質の向上とコスト最適化を支援する。
- 外部システム（農機・会計・気象・センシング等）と連携できる基盤とする。

### 1.3 スコープ
- **対象**：大規模・法人経営（従業員複数・作業受託あり）を主想定。ただし極小規模から最大規模まで同一システムで対応する。
- **対象作物**：水稲・畑作・野菜・果樹のすべてを対象とする（作物種別に依存しない汎用データモデルとする）。
- **対象地域**：日本国内および**海外圃場**を対象とする。国内制度に依存する機能（筆ポリゴン取込 F-06／WAGRI農機連携 F-56／JGAP・環境保全型農業直接支払 F-81〜83 等）は国内圃場向けとし、海外圃場は各国の規制・認証（GLOBALG.A.P.等）への対応を段階的に行う（**国内先行**）。データモデル・i18n・単位/通貨/タイムゾーンは海外圃場を前提に国・地域非依存で設計する。
- **利用形態**：自社利用（外販SaaSは当面想定しない）。ただし複数法人による**グループ/連合利用**を含むため、マルチテナント構成とする。
- **想定規模**：圃場枚数は極小〜巨大、ユーザー数は最大5万人を上限として設計する。複数組織（グループ/連合）にまたがる。
- **今回の整理範囲**：ソフト機能全体＋農機/IoT/API連携までの全体像。
- **重視点**：①スマホ現場入力のしやすさ ②地図・GIS ③外部連携・API ④データ分析・可視化。

> 補足：利用形態は自社利用だが、複数法人のグループ/連合利用（最大5万ユーザー）を前提とするため、組織（テナント）単位のデータ分離・権限管理を設計の基本に置く。

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| 圃場（ほじょう） | 田・畑・果樹園など作物を育てる区画。枚数で数える。 |
| 作付（さくづけ） | ある圃場・シーズンに、どの作物・品種を栽培するかの割当。 |
| 作業日誌 | 「いつ・どこで・誰が・何の作業をしたか」の記録単位。 |
| メッシュ | 圃場を格子（例：5〜20m四方）で区切った単位。可変施肥や収量マップに使う。 |
| 可変施肥 | 圃場内の生育ムラに応じて施肥量を場所ごとに変えること。 |
| トレーサビリティ | 農薬・肥料の使用履歴を追跡・証明できる状態。GAP等で必要。 |

---

## 3. 想定利用者とロール

### 3.1 ペルソナ
- **経営者／管理者**：全体の作付計画・原価・収量を把握し意思決定する。PC中心。
- **現場責任者（班長）**：作業指示を割り振り、進捗を管理する。PC＋スマホ。
- **作業員**：割り当てられた作業をスマホで確認し、現場で記録する。スマホ中心。
- **受託作業者（担い手）**：受託圃場の作業を記録し、実施エビデンスとして提出する。

### 3.2 ロールと権限（RBAC）
| ロール | 圃場/計画編集 | 作業指示 | 作業記録 | 原価/経営データ | 従業員/権限管理 | マスタ設定 |
|---|---|---|---|---|---|---|
| グループ管理者 | 全組織○ | 全組織○ | 全組織○ | 全組織○ | 全組織○ | 全組織○ |
| 管理者（組織単位） | 自組織○ | 自組織○ | 自組織○ | 自組織○ | 自組織○ | 自組織○ |
| 現場責任者 | 一部○ | ○ | ○ | 閲覧のみ | × | × |
| 作業員 | × | × | 自分の分○ | × | × | × |
| 受託作業者 | × | 受託分のみ | 受託分○ | × | × | × |

> 権限は**テナント（組織）単位＋ロール単位＋圃場グループ単位（担当エリア）**で制御する。グループ/連合利用のため、各組織のデータは既定で分離し、グループ管理者のみ横断アクセスできる。

---

## 4. 機能要求

各機能に優先度を付す（**M**=Must / **S**=Should / **C**=Could）。

### 4.1 圃場管理・GIS（重点）
- **F-01（M）** 電子地図上で圃場をポリゴンとして登録・編集・削除できる。
- **F-02（M）** 圃場に属性（圃場名／住所／面積／所有者／地目／メモ）を登録できる。
- **F-03（M）** 作物・所有者・状態などで圃場を色分け表示できる。
- **F-04（S）** 面積を地図から自動計算できる（手入力も可）。
- **F-05（S）** 圃場をグループ・エリア・団地単位で束ねて管理できる。
- **F-06（S）** 筆ポリゴン（農水省公開データ）等の外部地図データを取り込める。
- **F-07（C）** メッシュ（格子）単位で圃場内を区切って管理できる。
- **F-08（M）** スマホのGPSで現在地から近い圃場を特定できる。

### 4.2 作付計画
- **F-10（M）** 年度・シーズン単位で作付計画（圃場×作物×品種）を作成できる。作物は水稲・畑作・野菜・果樹すべてに対応し、作物種別を設定で追加できる。
- **F-11（S）** 農薬・肥料の使用量・単価を設定し、栽培面積と掛け合わせて資材費をシミュレーションできる。
- **F-12（S）** 作物ごとの標準作業暦（テンプレート）から、開始日・工期・依存関係付きの作業タスク群（4.3.1のガント）を自動生成できる。
- **F-13（C）** 輪作・連作の履歴を参照して警告できる。

### 4.3 作業指示・タスク管理（大規模法人で重要）
- **F-20（M）** 管理者・責任者が作業（圃場・作業種別・開始予定日・終了予定日／工期・担当者）を指示できる。
- **F-21（M）** 作業員はスマホで自分に割り当てられた作業リストを確認できる。
- **F-22（M）** 他メンバーの作業状況（未着手／進行中／完了）も共有・可視化できる。
- **F-23（S）** 天候・優先度による作業の並べ替え・再割当ができる。
- **F-24（C）** 作業指示に施肥量・散布量などのパラメータを付与できる。

### 4.3.1 スケジュール・工程管理（ガントチャート）（重点）
> 開始日〜終了日を持つタスクを時間軸で管理し、ガントチャート形式で全体工程を計画・追跡する。

- **F-25（M）** 各作業タスクに「開始予定日・終了予定日（または工期日数）」を設定できる。
- **F-26（M）** タスク群を横軸＝日付のバーで表示するガントチャート（タイムライン）を提供する。
- **F-27（S）** タスク間の依存関係（先行→後続、例：代掻き→田植え）を設定し、チャート上に表示できる。
- **F-28（S）** 依存関係のある先行タスクの日程を変更すると、後続タスクの予定日を自動でずらせる（連動）。
- **F-29（S）** マイルストーン（田植え開始日・出荷締切など重要期日）を設定・表示できる。
- **F-2A（S）** バー上に進捗率（%）を表示し、計画（ベースライン）と実績を対比できる。
- **F-2B（S）** 圃場グループ・作物・作業種別・担当者でガントチャートを絞り込み／並べ替えできる。
- **F-2C（S）** 担当者・農機ごとの割当を時間軸で表示し、負荷の重なり（オーバーアロケーション）を検知・警告できる（リソースビュー）。
- **F-2D（C）** チャート上でバーのドラッグ操作により日程・工期を直接変更できる。
- **F-2E（C）** 作物ごとの作業適期ウィンドウ（例：田植えは4/20〜5/10）を背景帯として表示し、逸脱を警告できる。
- **F-2F（C）** 天候予報（4.10 F-91）と連動し、雨天予定日に屋外作業が重なる場合に警告できる。
- **F-2G（C）** クリティカルパス（遅延が全体遅延に直結する経路）を強調表示できる。
- **F-2H（S）** ガント上の予定を、作業員のスマホ作業リスト（F-21）および作業日誌（F-30）と同一データで連動させる。

### 4.4 作業記録（日誌）（現場入力の要）
- **F-30（M）** スマホ・PCから数タップで作業日誌を作成できる（圃場・作業種別・日時・作業者）。
- **F-31（M）** 写真・メモを日誌に添付できる。
- **F-32（M）** 使用した農薬・肥料を記録できる。**農薬記録の必須項目**は、散布月日・圃場・作物・薬剤名・希釈倍率・散布量・対象病害虫・作業者・使用器具とする（JGAP／農薬取締法省令の記帳事項に準拠）。
- **F-33（S）** 作業時間・使用機械・投入資材を記録し、原価計算に連携できる。
- **F-34（M）** オフラインで入力し、通信回復時に同期できる（圏外圃場が多いため必須）。入力・写真添付・作業リスト参照・資材の出庫記録（F-42）がオフラインで動作すること。競合解決は5.1.1のデータ分類別ルールに従う。
- **F-35（S）** よく使う入力パターンを定型化して入力を省略できる。
- **F-36（S）** 農機連携により作業を自動記録できる（4.6参照）。
- **F-37（M）** 既存のExcel/CSV（圃場台帳・作業記録等）を取り込み、初期データとして移行できる（マッピング指定・重複チェックを含む）。

### 4.5 資材・在庫・トレーサビリティ
- **F-40（M）** 農薬・肥料マスタ（登録番号・成分・希釈倍率・使用回数制限等）を管理できる。
- **F-41（S）** 農薬の使用基準（適用作物・回数・収穫前日数）をチェックし、違反を警告できる。
- **F-42（S）** 資材の入出庫・在庫を管理できる。在庫残高は直接編集せず、**入出庫イベントの追記**から導出する（オフライン同期時の消失防止：5.1.1）。**出庫記録は現場からオフラインで入力できる**。同期後のマイナス在庫は警告表示する。
- **F-43（S）** 圃場・作物単位で農薬/肥料使用履歴を出力し、GAP・出荷証明に使える。

### 4.6 農機・IoT・センシング連携（重点：外部連携）
> 方針：特定メーカー/機種に固定せず、ベンダー非依存で連携する。連携方式はAPIとファイル取込（CSV/SDカード等）の両方に対応する。

- **F-50（S）** 対応農機の稼働情報（位置・稼働時間・エラー）を収集・表示できる。
- **F-51（S）** 食味・収量センサ付き収穫機のデータ（水分・タンパク・収量）を圃場/メッシュ単位で取り込める。
- **F-52（C）** ドローン/衛星のセンシングから生育マップを生成できる。
- **F-53（C）** 生育マップを基に可変施肥マップを作成し、対応農機へ送信できる。
- **F-54（M）** メーカー・機種を問わず連携できるよう、標準化した取込フォーマット（共通データモデル）と変換層（アダプタ）を設ける。API連携・ファイル取込のいずれにも対応する。
- **F-55（S）** 新しいメーカー/機種の追加を、アダプタ追加のみで対応でき、本体改修を最小化できる。
- **F-56（M）** 初回対応の農機アダプタは**農機OpenAPI（WAGRI）準拠アダプタ**とし、実証パートナーは**第一候補＝クボタ**（API数最多、植付・収穫等の状態データが充実）、**第二候補＝井関農機**（収量・散布レートを明示提供）とする。ヤンマーはWAGRI外の「営農API」直接契約ルートとして第2アダプタ以降で対応する。ファイル取込（CSV/SDカード等）のアダプタも併走して対応する。以降、他メーカー・他方式をアダプタ追加で順次拡張する。
  > 注：当初想定していた三菱マヒンドラ農機は2026年3月に解散（農機事業撤退）を発表し、IoT基盤「MITサービス」の外部連携も2026年3月末で終了したため、初回対象から除外した。共通データモデル（F-54）は農機OpenAPIのスキーマを参考に設計する。
- **F-57（M）** 農機OpenAPIのデータ保持期間（クボタで12ヶ月等）を踏まえ、**日次バッチで取込み自前DBへ永続化**する。長期の分析（F-51、F-60〜63）はISAS側の永続データを基礎とする。

### 4.7 データ分析・可視化（重点）
- **F-60（M）** 収量・食味・コストを圃場/作物/年度で集計・グラフ表示できる。
- **F-61（M）** 収量メッシュマップ・食味メッシュマップを地図上に可視化できる。
- **F-62（S）** 経営ダッシュボード（面積・進捗・原価・収量）を提供する。
- **F-63（S）** 年度間・圃場間で比較分析できる。
- **F-64（C）** 蓄積データを基に改善提案（AIによる施策推奨・病害虫診断等）を行う。

### 4.8 労務・従業員管理（大規模法人向け）
- **F-70（S）** 従業員の作業時間・作業実績を集計できる。
- **F-71（C）** 作業実績を勤怠・給与計算の基礎データとして出力できる。
- **F-72（S）** 受託作業の実績を受託先別に集計し、請求根拠にできる。

### 4.9 帳票・出力
> 方針：出力は**CSV形式を基本**とする（汎用性・他システム移行性を優先）。GAPはJGAP準拠を基本とし、公式様式への完全準拠はスコープ外。必要な様式のみ個別にテンプレート化する。

- **F-80（M）** 作業日誌・圃場台帳・農薬使用記録をCSVで出力できる。
- **F-81（S）** JGAPに準拠した農薬使用記録（F-32の必須項目）をCSVで出力し、GAP対応・出荷証明に使える。
- **F-82（C）** 補助金・行政提出用の帳票は、対象が確定した様式のみテンプレート化して出力する（PDF/Excelは必要に応じ追加）。
- **F-83（C）** JGAP以外への将来対応は、**国際水準GAPガイドライン（農水省2022年策定）の記録項目**を保持基準とする。都道府県GAPは同ガイドライン準拠への収斂が進んでいるため、**県版GAPの個別様式対応は行わない**（記録項目の包含で対応）。環境保全型農業直接支払は**全国共通様式（農水省実施要領）をテンプレート化**し、都道府県特認取組は取組名マスタの設定追加で対応する（コード改修なし）。**優先対象自治体は山形県（グループ圃場所在地）およびその市町村**。記録データは引き続き様式非依存で保持する。
- **F-84（C）** 海外圃場向けには**GLOBALG.A.P.等の国際認証**への対応を将来検討する。記録項目は国際水準GAPガイドライン（GLOBALG.A.P.を参照して策定）を基準とするため、大部分を流用できる想定。

### 4.10 外部連携・API（重点）
- **F-90（S）** REST APIを公開し、外部システムからデータ取得・登録ができる。
- **F-91（S）** 会計システム・販売管理・市況情報・気象データと連携できる。
- **F-92（C）** 農水省のオープンAPI（農業データ連携基盤 WAGRI 等）に準拠する。
- **F-93（S）** Webhook等でイベント通知（作業完了・エラー等）ができる。

---

## 5. 非機能要求

### 5.1 使いやすさ（UX）
- 現場作業員が手袋・屋外・電波不安定な環境でも数タップで入力できること。
- スマホ・タブレット・PCに対応（レスポンシブ／PWAまたはネイティブアプリ）。
- 高齢の作業者でも迷わない、文字大きめ・操作の少ないUI。

### 5.1.1 オフライン対応（必須要件）
- 圏外圃場が多いことを前提に、オフラインでの作業リスト参照・作業日誌入力・写真添付・**資材の出庫記録**を**必須**とする。
- 通信回復時に自動同期し、競合（同一データの複数端末編集）を検知・解決できること。
- オフライン中のデータは端末側に安全に保持し、消失しないこと。

#### 競合解決ルール（データ分類別・v0.8確定）

| 分類 | 対象データ | ルール |
|---|---|---|
| 追記型（イベント） | 作業日誌・写真・収穫データ・**資材入出庫イベント** | 新規作成は別レコードのため競合しない。同一レコードの編集は**最終更新優先（LWW、フィールド単位）** |
| 状態遷移型 | 作業指示のステータス・進捗率 | **前進優先マージ**（完了＞進行中＞未着手。進んだ状態を採用）。巻き戻しは明示操作（理由入力必須）のみ |
| 残高・集計型 | 資材在庫 | **残高の直接編集を禁止**し、入出庫イベントの追記から残高を導出する。同期後にマイナス在庫が生じた場合は警告表示 |
| 導出型 | 原価・集計値・ダッシュボード数値 | 編集可能な形で保存せず、元データ（作業時間・資材使用・単価マスタ）から**常に再計算** |
| 計画・マスタ型 | 圃場ポリゴン・作付計画・ガント日程・作業の**再割当**・各種マスタ | **オフライン編集対象外（オンライン専用）**。オンライン同時編集は**楽観ロック**（バージョン番号）で検知し、上書きせず差分提示・マージ操作 |
| 権限・組織型 | ユーザー・ロール・テナント設定 | **オフライン編集禁止＋オンライン専用＋楽観ロック**（権限の巻き戻りはセキュリティ事故のため） |

- LWWの判定時刻は**端末申告時刻を無条件に信用しない**。編集時の端末時刻とサーバー受理時刻を両方記録し、競合判定・監査に用いる（詳細は基本設計）。
- いずれの分類でも、上書き・マージが発生した場合は上書き前の値を監査ログ（5.4）に残し、追跡可能とする。

### 5.2 性能・可用性
- 想定規模は圃場枚数が極小〜巨大、**ユーザー数は最大5万人**。この規模で地図表示・一覧・ガント表示が実用速度で動作すること。
- 大量圃場（数万枚規模）でも地図・空間検索が破綻しないアーキテクチャ（タイル化・空間インデックス等）とする。
- クラウド提供・可用性目標（例：稼働率99.5%以上）を設定する。
- 農繁期のアクセス集中に耐える水平スケーラビリティを確保する。

### 5.3 信頼性・データ保全
- 入力データのバックアップと、**他システムへ移行可能なエクスポート**を保証する（囲い込み回避）。
- オフライン入力データの同期整合性を担保する。

### 5.4 セキュリティ
- ロールベースのアクセス制御、通信の暗号化（TLS）、認証（多要素認証を選択可）。
- 個人情報・経営情報の適切な取り扱い。
- 監査ログ（誰がいつ何を変更したか）。

### 5.5 保守・拡張性
- 作物・作業種別・帳票様式を設定で追加できる（コード改修なしを目指す）。
- **多言語対応（必須）**：海外圃場を対象に含むため、**全世界の言語を扱えるi18n基盤**とする。具体的には：
  - **任意の言語をデータ追加のみで追加**できる（コード改修なし）。Unicode全面対応、RTL（右書き：アラビア語等）レイアウト、言語ごとの複数形規則、ロケール別の日付・数値・単位表示に対応する。
  - **初期リリース言語は日本語＋英語**。以降、現場の作業員構成に応じて順次追加する（国内はベトナム語・インドネシア語等を想定。農業分野の外国人材の主要国籍に基づく）。
  - 作業員が自分の言語を選択でき、マスタ（作業種別等）も多言語表示できる。
  - **翻訳運用（ハイブリッド方式）**：UI固定文言＝機械翻訳一次訳＋ネイティブレビュー（農業用語グロッサリを先に整備）／マスタ＝登録時に機械翻訳を自動付与し「未レビュー」フラグで後日確定／自由入力（日誌メモ等）＝原文保持＋オンデマンド機械翻訳／**農薬名・圃場名等の固有名詞は翻訳しない**（誤訳の法令リスク回避）。
  - 翻訳に依存しない補助策として、アイコン・ピクトグラム中心のUI、作業指示への写真・定型文活用（F-31/F-35）、日本語UIの「やさしい日本語」基準を併用する。
- マルチテナント構成として、組織（テナント）の追加・分離を運用で行えること。

---

## 6. データモデル（概念レベル）

主要エンティティと関係の概略：

```
テナント/グループ(Tenant) 1--* 組織(Organization)  … グループ/連合の親
組織(Organization) 1--* ユーザー(User)
組織 1--* 圃場(Field)         … ポリゴン, 面積, 所有者
圃場 1--* 作付(Cropping)       … 年度, 作物, 品種
作付 1--* 作業指示(WorkOrder)   … 作業種別, 開始予定日, 終了予定日/工期,
                                 担当, 進捗率, 状態, ベースライン日程
作業指示 *--* 作業指示(依存関係) … 先行/後続, 種別(FS等), マイルストーンフラグ
作業指示 1--* 作業日誌(WorkLog) … 実施日, 作業者, 写真, 資材使用, 更新時刻
作業日誌 *--* 資材(Material)    … 農薬/肥料, 使用量
資材 1--* 入出庫イベント(StockEvent) … 種別(入庫/出庫), 数量, 日時, 作業者
                                 （在庫残高はイベントから導出、直接編集不可）
圃場 1--* 収穫データ(Harvest)   … 収量, 食味(水分/タンパク), メッシュ値
農機(Machine) 1--* 稼働ログ(MachineLog)
作物マスタ 1--* 作業適期(CropWindow) … 作業種別, 適期開始日, 適期終了日
マスタ各種(作物/作業種別等) 1--* 多言語ラベル(I18nLabel) … 言語コード, 表示名, レビュー状態
```

- 全レコードはテナント（組織）IDを持ち、既定でテナント単位に分離する（グループ管理者のみ横断）。
- 同期の競合解決（5.1.1）のため、更新のあるエンティティは更新時刻（端末編集時刻・サーバー受理時刻）と更新者、楽観ロック用のバージョン番号を保持する。在庫・原価は残高/導出値を直接保持せず、イベント（StockEvent）や元データから導出する。

- 位置情報は圃場ポリゴン・メッシュ・GPSログを地理空間データ（GeoJSON / PostGIS等）で保持。
- ガントチャートは **WorkOrder（日程・進捗・ベースライン）＋依存関係＋作業適期（CropWindow）** を基礎データとし、専用テーブルを追加せずこれらから描画する。

---

## 7. 技術構成（たたき台・要検討）

> 要求段階のため確定ではなく選択肢の提示。ただし確定した前提（オフライン必須／ベンダー非依存連携／最大5万ユーザー／自社利用）を反映する。

- **フロント**：Web（React等）＋モバイル。オフライン必須のため**PWA（Service Worker＋ローカルストレージ）またはオフライン対応ネイティブ**を採用し、同期・競合解決機構を持つ。
- **地図/GIS**：地図ライブラリ（MapLibre / Leaflet 等）＋タイル配信、PostGISで空間検索。数万圃場規模を前提にタイル化・空間インデックスを設計。
- **バックエンド**：REST/GraphQL API、認証基盤、RBAC。最大5万ユーザーを見据えた水平スケール構成。
- **DB**：RDB（PostgreSQL＋PostGIS）を中核、時系列・稼働ログは別途検討。
- **連携**：農機/センサはベンダー非依存の**共通データモデル＋アダプタ層**（F-54〜57）で、API・ファイル取込の双方に対応。初回アダプタは**農機OpenAPI（WAGRI）準拠**（実証パートナー第一候補＝クボタ、第二候補＝井関。ヤンマーは営農API直接契約で第2以降）。共通データモデルは農機OpenAPIのスキーマを参考に設計し、WAGRI準拠（F-92）と整合させる。取込は日次バッチ＋自前永続化（F-57）。
  - **WAGRI契約方針**：設計・検証段階は**お試し会員（無料・1年・100回/月）**でAPI仕様とデータ品質を検証し、Phase 1 実装時に**データ利用・提供会員（40,000円/月・クライアント、転送20GB/月込み）**へ移行。**農業法人割引の適用可否を申請時に確認**する。食味データの提供可否・認証/同意フロー・更新頻度は利用申請時に照会する。
- **インフラ**：クラウド（可用性・スケール・バックアップ前提）。グループ/連合利用のため**マルチテナント**を基本とし、分離方式は**行レベル分離（共有スキーマ＋tenant_id＋PostgreSQL RLS）**とする。
  - RLS設計の留意点：接続プール維持のためテナント別DBロールではなく**セッション変数（例：`SET app.tenant_id`）＋ポリシー参照方式**とする／グループ管理者の横断アクセスはRLSポリシーで宣言的に許可する／`tenant_id`を複合インデックスの先頭に置く／横断ダッシュボード等の集計系は集計テーブル・マテリアライズドビューを別途設計する／オフライン同期APIの同期対象絞り込みにもtenant_idを必ず通す。
- **国際化**：**全世界の言語を扱えるi18n基盤**を初期から組み込む（初期リリースは日英）。RTLレイアウト・複数形規則・ロケール別の日付/数値/単位に対応するi18nフレームワーク（ICU/CLDR準拠）を採用。**タイムゾーン**は全時刻をUTC保存＋表示時にローカル変換、**通貨・単位系**（原価・面積等）はテナント/圃場ごとに設定可能とする（海外圃場対応）。
- **海外対応の考慮**：海外圃場向けの地図データソース（筆ポリゴンは国内のみ）、各国の個人情報・データ所在規制（GDPR等）、海外からのアクセスレイテンシ（リージョン配置）を設計時に考慮する。国内制度依存の連携（WAGRI等）は国内圃場に限定して適用する。
- **データ移行**：既存Excel/CSVの取込パイプライン（F-37）を用意する。

---

## 8. 優先度とフェーズ案

| フェーズ | 目標 | 主な機能 |
|---|---|---|
| **Phase 1（MVP）** | 現場入力と見える化の核 | 圃場管理・GIS(F-01〜03,08)、作業指示(F-20〜22)、作業日誌(F-30〜32)、**オフライン対応(F-34/5.1.1)**、**データ移行(F-37)**、CSV出力(F-80)、ガント基本表示(F-25,F-26,F-2H) |
| **Phase 2** | 計画・工程・資材・分析 | 作付計画(F-10〜12)、ガント高度化（依存・進捗・リソース：F-27〜2C）、資材/トレーサビリティ(F-40〜43)、JGAP対応CSV(F-81)、分析・ダッシュボード(F-60〜62) |
| **Phase 3** | 連携・高度化 | 農機/IoT連携（ベンダー非依存：F-50〜55）、外部API(F-90〜93)、労務(F-70〜72)、ガント最適化（適期/天候/クリティカルパス：F-2E〜2G）、AI提案(F-64)、環境直払テンプレート(F-83) |
| **Phase 4（海外展開）** | 海外圃場対応 | 各国農薬規制・登録農薬データ対応、GLOBALG.A.P.等(F-84)、海外地図データ、海外農機連携（ISOBUS/ADAPT等）、対象国言語の追加。※i18n基盤・UTC・単位/通貨設定はPhase 1から組み込み済みの前提 |

---

## 9. 前提・制約（確定事項）

v0.3〜v0.8で以下を確定した。

| # | 項目 | 決定 |
|---|---|---|
| 1 | 対象作物 | 水稲・畑作・野菜・果樹すべて。作物種別非依存の汎用データモデルとする。 |
| 2 | 農機・センサ連携 | 特定メーカー/機種に固定しない（ベンダー非依存）。連携方式はAPI・ファイル取込の両対応。共通データモデル＋アダプタ層で拡張。**初回アダプタは農機OpenAPI（WAGRI）準拠アダプタ**（実証パートナー第一候補＝クボタ、第二候補＝井関。ヤンマーは営農API直接契約で第2以降。ファイル取込を併走）。WAGRI契約はお試し会員で検証→データ利用・提供会員へ移行（農業法人割引を申請）。取込は日次バッチ＋自前永続化（F-57）。※当初想定の三菱マヒンドラ農機は解散発表（2026年3月）のため対象外（v0.5）。 |
| 3 | GAP・帳票 | 国内標準の**JGAP**準拠を基本。農薬使用記録の必須項目＝散布月日・圃場・作物・薬剤名・希釈倍率・散布量・対象病害虫・作業者・使用器具。出力は**CSV**を基本。公式様式の完全準拠はスコープ外。将来対応の基準は**国際水準GAPガイドラインの記録項目**とし、県版GAPの個別様式対応は行わない。環境保全型農業直接支払は**全国共通様式のテンプレート化＋県特認取組はマスタ対応**。**優先対象自治体＝山形県**（グループ圃場所在地）。海外圃場はGLOBALG.A.P.等を将来検討（F-84）（v0.7）。 |
| 4 | オフライン対応 | **必須**。作業リスト参照・日誌入力・写真添付・**資材出庫記録**をオフラインで可能とし、回復時に同期。競合解決は**データ分類別ルール**（5.1.1）：LWWは追記型のみ／ステータス＝前進優先マージ／在庫＝イベント追記方式／導出値＝再計算／計画・マスタ・権限＝オンライン専用＋楽観ロック（**作業の再割当もオンライン専用**）。上書き前値は監査ログに保持（v0.8）。 |
| 5 | 想定規模 | 圃場枚数は極小〜巨大、**ユーザー数上限5万人**を前提に性能設計。 |
| 6 | 利用形態／テナント | **自社利用**だが**複数法人のグループ/連合利用**を含むため**マルチテナント**構成。テナント（組織）単位でデータ分離、グループ管理者のみ横断。分離方式は**行レベル分離（共有スキーマ＋tenant_id＋PostgreSQL RLS）**とする（v0.5）。 |
| 7 | データ移行 | 既存Excel/CSVからの移行**あり**。取込機能（F-37）で対応。旧MITサービスの過去データ（アグリノート経由CSV・三菱への直接照会で回収）も移行対象に含める（v0.6）。 |
| 8 | 多言語対応 | **必要**。海外圃場対応のため**全世界の言語を扱えるi18n基盤**（任意言語のデータ追加・RTL・ロケール対応）。初期リリースは日英、以降は現場構成に応じ順次追加（国内はベトナム語・インドネシア語等を想定）。翻訳運用は**ハイブリッド方式**（UI＝機械翻訳＋ネイティブレビュー／マスタ＝自動機械翻訳＋レビューフラグ／固有名詞は翻訳しない）（v0.7）。 |
| 9 | 対象地域 | **日本国内＋海外圃場**。国内制度依存機能（筆ポリゴン・WAGRI・JGAP・環境直払等）は国内圃場向け、海外は各国規制・認証へ段階対応（**国内先行**）。単位・通貨・タイムゾーンは国・地域非依存で設計（v0.7）。 |

## 9.1 残る検討事項（設計フェーズで詰める）

- [x] マルチテナントの分離方式 → **行レベル分離（共有スキーマ＋tenant_id＋PostgreSQL RLS）に確定**（v0.5、詳細は7章）。RLSポリシーの具体設計（3.2章ロール表のDB表現）は基本設計で行う。
- [x] 初回農機アダプタ → **農機OpenAPI（WAGRI）準拠に確定**（v0.5）。※三菱マヒンドラ農機は解散のため対象外。
- [x] WAGRI／農機OpenAPIの詳細調査 → **確定（v0.6）**：契約＝お試し会員（無料）で検証後にデータ利用・提供会員（40,000円/月）へ移行、農業法人割引を申請。実証パートナー第一候補＝クボタ（18API）、第二候補＝井関（11API、収量・散布レートあり）。ヤンマーは営農API直接契約で第2アダプタ以降。データ保持12ヶ月制限のため日次バッチ取込＋自前永続化（F-57）。※食味データ提供可否・認証/同意フロー・更新頻度の細目は利用申請時に照会する。
- [x] MITサービスの過去データ救出 → **実施する（v0.6、グループ内に利用者あり）**。手順：①アグリノート連携済みの作業・収穫記録は**アグリノートのCSVエクスポートで回収**し、F-37の移行機能で取込む（走行軌跡・位置・メンテ情報は連携終了により消失済み）。②アグリノート未連携のデータは**三菱マヒンドラ農機へ直接照会**（清算が2026年9月末目処のため**至急**。窓口：mam.co.jp/contact/）。
- [x] 都道府県GAP・環境保全型農業直接支払の優先順位 → **確定（v0.7）**：基準＝国際水準GAPガイドラインの記録項目（県版GAPの個別様式対応はしない）。環境直払＝全国共通様式のテンプレート化＋県特認取組はマスタ対応。優先対象自治体＝**山形県およびその市町村**。海外はGLOBALG.A.P.等を将来検討（F-84）。
- [x] 多言語対応の初期対象言語・翻訳運用 → **確定（v0.7）**：全世界の言語を扱えるi18n基盤（詳細は5.5）。初期リリース＝日英、以降順次追加。翻訳運用＝ハイブリッド方式（機械翻訳一次訳＋ネイティブレビュー、固有名詞は翻訳しない）。
- [x] 最終更新優先で問題が起きやすいデータの個別ルール → **確定（v0.8、詳細は5.1.1）**：LWWは追記型（日誌・写真・入出庫イベント）に限定／作業ステータス＝前進優先マージ／在庫＝残高直接編集禁止・入出庫イベント追記方式（オフライン出庫対応）／原価等＝再計算／計画・マスタ・権限・作業の再割当＝オンライン専用＋楽観ロック／時刻＝端末・サーバー二本立て。
- [ ] **（v0.7新規）海外圃場の対象国の確定**と、各国の農薬規制・登録農薬データの入手方法、GAP/認証要件（GLOBALG.A.P.等）の調査。
- [ ] **（v0.7新規）海外圃場の地図データソースの選定**（筆ポリゴンは国内のみ。衛星画像・OpenStreetMap・現地公的データ等）。
- [ ] **（v0.7新規）データ所在・プライバシー規制への対応方針**（GDPR等。海外リージョン配置の要否、国外移転の制約）。
- [ ] **（v0.7新規）海外農機連携の方式調査**（WAGRIは国内のみ。ISOBUS/ISO-XML、AgGateway/ADAPT、各社グローバルAPI（John Deere Operations Center等））。

---

## 10. 参考

- KSAS（クボタスマートアグリシステム）：圃場管理／作付計画／作業指示／作業記録（日誌）、対応農機との食味・収量・可変施肥連携、Excel出力、GAP推奨システム認定、他社農機連携（簡易接続）、AI病害虫診断等を提供。本仕様はこれらの機能群を参考に整理した。
