{
  "name": "@isas/bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.mjs && node --check test/*.test.mjs"
  }
}
