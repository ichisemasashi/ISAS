import type { OutboxRecord } from "./storage";

export type TodayTask = { id: string; time: string; field: string; crop: string; work: string; status: "next" | "today" | "safety_check" | "completed" | "cancelled" };
export type FieldFeature = { type: "Feature"; id: string; geometry: { type: "MultiPolygon"; coordinates: number[][][][] }; properties: { id: string; fieldGroupId: string; name: string; cropName: string | null; status: "active" | "fallow" | "archived"; areaSqm: number; version: number } };
export type FieldCollection = { type: "FeatureCollection"; features: FieldFeature[]; nextCursor: string | null };
export type OfflineMapPackManifest = { packId: string; fieldGroupId: string; assignmentVersion: string; tilesetVersion: string; archiveSha256: string; archiveUrl: string; archiveUrlExpiresAt: string; bbox: [number, number, number, number]; minZoom: number; maxZoom: number; maxBytes: number; expiresAt: string; attribution: string; licenseUrl: string };
export type PushBundle = { bundleId: string; events: OutboxRecord[] };
export type PushResult = { bundleId: string; status: "accepted" | "duplicate" | "rejected" | "conflict"; events?: Array<{ eventUuid: string; eventTs: string }>; rejection?: { reason: string; recoveryAction: string } };
export type PullChange = { serverSeq: string; type: string; operation: "upsert" | "delete" | "revoke"; entityId?: string | null; eventUuid?: string | null; data: Record<string, unknown> };
export type PullResult = { changes: PullChange[]; nextCursor: string; snapshotUpper?: string; hasMore: boolean };
export type SyncRejection = { id: string; bundleId: string; eventUuids: string[]; reason: string; recoveryAction: string; createdAt: string };
export type SyncConflict = { id: string; documentId: string; eventUuid: string; baseVersion: number; currentVersion: number; currentValue: Record<string, unknown>; proposedValue: Record<string, unknown>; conflictingFields: string[]; status: string; createdAt: string };
export type PesticideAlert = { id: string; usageId: string; reasons: string[]; clientSafety: Record<string, unknown>; serverSafety: Record<string, unknown>; status: string; createdAt: string };
export type StockAlert = { id: string; chemicalId: string; name?: string; triggeringEventId: string; negativeQuantity: number; status: string; createdAt: string };
export type QueueSnapshot = { rejections: SyncRejection[]; conflicts: SyncConflict[]; pesticideAlerts?: PesticideAlert[]; stockAlerts?: StockAlert[] };
export type WorkInstruction = { id: string; fieldId: string; fieldGroupId: string; fieldName: string | null; cropName: string | null; cropPlanId?: string | null; varietyName?: string | null; plannedAreaM2?: number | null; targetYieldKg?: number | null; title: string; workType: string; details: string; scheduledStart: string; scheduledEnd: string; priority: number; status: "issued" | "in_progress" | "completed" | "cancelled"; progressPercent?: number; version: number; assignment: { id: string; assigneeUserId: string; version: number } | null; dependencies?: Array<{ predecessorInstructionId: string; type: string; lagMinutes: number }>; resources?: Array<{ id: string; name: string; quantity: number }>; resourceConflicts?: Array<{ resourceId: string; resourceName: string; conflictStart: string; conflictEnd: string }> };
export type PlanningTemplate = { id: string; name: string; cropName: string | null; description: string; version: number; active: boolean; steps: Array<Record<string, unknown>> };
export type JournalEntry = { id: string; instructionId: string | null; fieldId: string | null; fieldName: string | null; workerUserId: string; body: Record<string, unknown>; status: "submitted" | "approved" | "returned" | "corrected"; version: number; returnReason?: string | null; submittedAt: string; updatedAt: string; attachments: Array<{ id: string; fileName: string; contentType: string }> };
export type JournalBootstrap = { instruction: null | { id: string; fieldId: string; fieldGroupId: string; fieldName: string; workType: string; details: string; scheduledStart: string; scheduledEnd: string }; punchSuggestion: { startedAt: string | null; endedAt: string | null; warning: "missing_start" | "missing_finish" | null }; templates: Array<{ id: string; name: string; workType: string; defaults: Record<string, unknown>; version: number }>; previous: null | { id: string; instructionId?: string | null; fieldId?: string | null; fieldGroupId?: string | null; body: Record<string, unknown>; status?: JournalEntry["status"]; version: number; updatedAt: string } };
export type Agrochemical = { id: string; registrationNumber: string; name: string; activeIngredient: string; applicableCrops: string[]; dilutionMin: number; dilutionMax: number; maxUses: number; preharvestDays: number; revokedOn: string | null };
export type PesticideBootstrap = { field: { id: string; fieldGroupId: string; name: string; cropName: string | null; timezone: string }; release: null | { id: string; version: string; validUntil: string; publishedAt: string; syncedAt: string }; chemicals: Agrochemical[]; usage: Array<{ chemicalId: string; usageCount: number; lastAppliedOn: string | null }>; inventory: Array<{ chemicalId: string; quantity: number; updatedAt: string | null }> };
export type InventorySnapshot = { balances: Array<{ chemicalId: string; name: string; registrationNumber: string; quantity: number; updatedAt: string | null; policy?: null | { reorderPoint: number; targetLevel: number; safetyStock: number; allowNegative: boolean }; belowReorderPoint?: boolean }>; alerts: StockAlert[]; incoming?: Array<{ purchaseOrderLineId?: string; purchaseOrderId: string; orderNumber: string; supplierName: string; chemicalId: string; name?: string; incomingQuantity: number; unit: string; unitCost?: number; currency?: string; expectedOn: string | null }>; lots?: Array<{ id: string; chemicalId: string; name?: string; lotNumber: string; supplierName: string; receivedOn: string; expiresOn: string | null; unit: string; unitCost: number; currency: string; status: string; quantity: number; inventoryValue: number }>; counts?: Array<{ id: string; locationName: string; countedAt: string; status: string; version: number; lineCount?: number; absoluteVariance?: number }> };
export type MigrationDataset = "fields" | "journals" | "pesticide_history";
export type MigrationRow = { lineNumber: number; status: "valid" | "duplicate" | "invalid" | "committed"; duplicateKey: string | null; errors: string[]; normalized: Record<string, unknown>; entityId?: string | null };
export type MigrationJob = { id: string; dataset: MigrationDataset; sourceName: string; sourceSha256: string; mapping: Record<string, string>; status: "needs_review" | "validated" | "committing" | "committed"; rowCount: number; validCount: number; duplicateCount: number; errorCount: number; version: number; createdAt: string; committedAt: string | null; rows?: MigrationRow[] };
export type ExportDataset = "fields" | "journals" | "pesticide-records" | "jgap-inventory";
export type SecurityUser = { userId: string; displayName: string; issuer: string; subject: string; userStatus: string; tenantId: string; roleKey: string; membershipStatus: string; validFrom: string; validUntil: string | null; fieldGroupIds: string[]; authorizationVersion: number };
export type SecurityChangeRequest = { requestId: string; changeType: "user_register" | "user_change" | "user_revoke" | "break_glass"; targetUserId: string; requestedBy: string; requestedAt: string; requestExpiresAt: string; reason: string; ticketRef: string; beforeState: Record<string, unknown> | null; proposedState: Record<string, unknown>; status: string; decidedBy?: string | null; decisionNote?: string | null };
export type PrivacyRequest = { requestId: string; subjectUserId: string | null; requestType: "disclosure" | "correction" | "deletion"; status: string; details: Record<string, unknown>; legalHold: boolean; dueAt: string; requestedBy: string; requestedAt: string; evidenceRef?: string | null; events: Array<{ eventId: number; fromStatus: string | null; toStatus: string; note: string; actorUserId: string; occurredAt: string }> };
export type LocalTestUserCredential = { userId: string; username: string; displayName: string; roleKey: string; fieldGroupIds: string[]; temporaryPassword: string; requiredActions: string[]; status: "ready_for_first_login" };
export type SecuritySnapshot = { users: SecurityUser[]; roles: Array<{ roleKey: string; roleLabel: string; capabilities: string[] }>; changeRequests: SecurityChangeRequest[]; breakGlassGrants: Array<Record<string, unknown>>; privacyRequests: PrivacyRequest[]; localTestUserRegistration?: boolean };
export type PesticideMasterReview = { id: string; proposedRelease: Record<string, unknown>; beforeRelease: Record<string, unknown> | null; status: string; reason: string; ticketRef: string; requestedBy: string; requestedAt: string; decidedBy?: string | null; decisionNote?: string | null; releaseId?: string | null };
export type TenantAnalytics = { source: "operational_db"; dwhRequired: false; generatedAt: string; plans: Array<{ cropPlanId: string; cropName: string | null; varietyName?: string | null; targetYieldKg: number | null; actualYieldKg: number | null; progressPercent?: number; plannedWorkSeconds: number; actualWorkSeconds: number; pesticideAmount: number | null; missingMetrics: string[]; freshestAt?: string | null }>; materials: Array<{ usageType: string; chemicalId: string; materialName: string; quantity: number; unit: string | null; eventCount: number; freshestAt: string | null }>; freshness: Array<{ source: string; freshestAt: string | null; status: "fresh" | "stale" | "missing"; ageSeconds?: number | null }> };
export type LocationBootstrap = { policies: Array<{ policyVersion: string; purpose: string; locale: string; title: string; body: string; contentSha256: string; effectiveFrom?: string; effectiveUntil?: string | null }>; consent: null | { id: string; action: "granted" | "withdrawn"; policyVersion: string; consentTextSha256: string; locale: string; effectiveAt: string; expiresAt: string | null }; preference: { enabled: boolean; punchLinked: boolean; retentionDays: number; locale: string; version: number; updatedAt?: string } };

export class ApiProblem extends Error { constructor(public status: number, public type: string, public body: Record<string, unknown>) { super(`${type} (${status})`); } }
export interface MvpGateway {
  getToday(contextId: string, signal?: AbortSignal): Promise<{ tasks: TodayTask[]; serverTime: string }>;
  getFields(contextId: string, search?: { bbox?: [number, number, number, number]; query?: string; limit?: number; cursor?: string | null }, signal?: AbortSignal): Promise<FieldCollection>;
  getOfflineMapPack(contextId: string, fieldGroupId: string, signal?: AbortSignal): Promise<OfflineMapPackManifest>;
  getWorkInstructions(contextId: string, signal?: AbortSignal): Promise<{ instructions: WorkInstruction[] }>;
  createWorkInstruction(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<WorkInstruction>;
  reassignWorkInstruction(contextId: string, csrfToken: string, instructionId: string, input: { assigneeUserId: string; expectedVersion: number }, signal?: AbortSignal): Promise<{ id: string; assignmentId: string; assigneeUserId: string; version: number }>;
  getPlanningTemplates?(contextId: string, signal?: AbortSignal): Promise<{ templates: PlanningTemplate[] }>;
  expandPlanningTemplate?(contextId: string, csrfToken: string, templateId: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<{ templateId: string; cropPlanId: string; instructions: WorkInstruction[]; conflictCount: number }>;
  updateWorkProgress?(contextId: string, csrfToken: string, instructionId: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<{ id: string; progressPercent: number; status: string; version: number; updatedAt: string }>;
  getJournalBootstrap(contextId: string, search?: { instructionId?: string; fieldId?: string; journalId?: string }, signal?: AbortSignal): Promise<JournalBootstrap>;
  getPesticideBootstrap(contextId: string, fieldId: string, signal?: AbortSignal): Promise<PesticideBootstrap>;
  getInventory(contextId: string, signal?: AbortSignal): Promise<InventorySnapshot>;
  createPurchaseOrder?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  receiveInventoryLot?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  createInventoryCount?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  postInventoryCount?(contextId: string, csrfToken: string, sessionId: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  getJournals(contextId: string, signal?: AbortSignal): Promise<{ journals: JournalEntry[] }>;
  reviewJournal(contextId: string, csrfToken: string, journalId: string, input: { action: "approve" | "return"; expectedVersion: number; reason?: string }, signal?: AbortSignal): Promise<{ id: string; status: string; version: number; updatedAt: string }>;
  uploadJournalAttachment(contextId: string, csrfToken: string, attachment: { id: string; journalId: string; fileName: string; capturedAt: string; blob: Blob }, signal?: AbortSignal): Promise<{ id: string; journalId: string; byteSize: number; sha256: string }>;
  push(contextId: string, csrfToken: string, bundles: PushBundle[], signal?: AbortSignal): Promise<{ results: PushResult[] }>;
  pull(contextId: string, scope: string, priority: "priority" | "normal", cursor: string | null, signal?: AbortSignal): Promise<PullResult>;
  getQueues(contextId: string, signal?: AbortSignal): Promise<QueueSnapshot>;
  resolveConflict(contextId: string, csrfToken: string, conflictId: string, resolution: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  createMigrationJob(contextId: string, csrfToken: string, input: { dataset: MigrationDataset; sourceName: string; csv: string; mapping: Record<string, string> }, idempotencyKey: string, signal?: AbortSignal): Promise<MigrationJob>;
  getMigrationJobs(contextId: string, signal?: AbortSignal): Promise<{ jobs: MigrationJob[] }>;
  commitMigrationJob(contextId: string, csrfToken: string, jobId: string, expectedVersion: number, signal?: AbortSignal): Promise<MigrationJob>;
  exportCsv(contextId: string, dataset: ExportDataset, search?: { from?: string; to?: string }, signal?: AbortSignal): Promise<{ blob: Blob; fileName: string }>;
  getSecurityAdministration(contextId: string, signal?: AbortSignal): Promise<SecuritySnapshot>;
  provisionLocalTestUser?(contextId: string, csrfToken: string, input: { username: string; displayName: string; roleKey: string }, signal?: AbortSignal): Promise<LocalTestUserCredential>;
  reconcileAttachmentStorage(contextId: string, csrfToken: string, signal?: AbortSignal): Promise<{ scanned: number; taggedOrphans: number; finalized: number; quarantined: number }>;
  requestSecurityChange(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<{ requestId: string; status: string }>;
  decideSecurityChange(contextId: string, csrfToken: string, requestId: string, input: { decision: "approve" | "reject"; note: string }, signal?: AbortSignal): Promise<Record<string, unknown>>;
  createPrivacyRequest(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<{ requestId: string; status: string }>;
  transitionPrivacyRequest(contextId: string, csrfToken: string, requestId: string, input: { action: string; note: string; evidenceRef?: string }, signal?: AbortSignal): Promise<Record<string, unknown>>;
  getPesticideMasterReviews(contextId: string, signal?: AbortSignal): Promise<{ reviews: PesticideMasterReview[] }>;
  requestPesticideMasterReview(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<PesticideMasterReview>;
  decidePesticideMasterReview(contextId: string, csrfToken: string, reviewId: string, input: { decision: "approve" | "reject"; note: string }, signal?: AbortSignal): Promise<Record<string, unknown>>;
  getTenantAnalytics?(contextId: string, signal?: AbortSignal): Promise<TenantAnalytics>;
  recordHarvestActual?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  getLocation?(contextId: string, locale: string, signal?: AbortSignal): Promise<LocationBootstrap>;
  recordLocationConsent?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<Record<string, unknown>>;
  saveLocationPreference?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<LocationBootstrap["preference"]>;
  appendLocationPoints?(contextId: string, csrfToken: string, input: Record<string, unknown>, signal?: AbortSignal): Promise<{ accepted: number; eventUuids: string[] }>;
}
type FetchLike = typeof fetch;
async function result<T>(response: Response): Promise<T> { const body = await response.json() as Record<string, unknown>; if (!response.ok) throw new ApiProblem(response.status, typeof body.type === "string" ? body.type : "request_failed", body); return body as T; }
function headers(contextId: string, extra: Record<string, string> = {}) { return { Accept: "application/json", "X-ISAS-Context": contextId, ...extra }; }

export function createMvpGateway(fetcher: FetchLike = fetch): MvpGateway {
  return {
    getToday: (contextId, signal) => fetcher("/api/v1/today", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ tasks: TodayTask[]; serverTime: string }>(response)),
    getFields: (contextId, search = {}, signal) => {
      const query = new URLSearchParams();
      if (search.bbox) query.set("bbox", search.bbox.join(","));
      if (search.query) query.set("q", search.query);
      if (search.limit) query.set("limit", String(search.limit));
      if (search.cursor) query.set("cursor", search.cursor);
      const suffix = query.size ? `?${query}` : "";
      return fetcher(`/api/v1/fields${suffix}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<FieldCollection>(response));
    },
    getOfflineMapPack: (contextId, fieldGroupId, signal) => fetcher(`/api/v1/offline-map-pack?fieldGroupId=${encodeURIComponent(fieldGroupId)}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<OfflineMapPackManifest>(response)),
    getWorkInstructions: (contextId, signal) => fetcher("/api/v1/work-instructions", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ instructions: WorkInstruction[] }>(response)),
    createWorkInstruction: (contextId, csrfToken, input, signal) => fetcher("/api/v1/work-instructions", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<WorkInstruction>(response)),
    reassignWorkInstruction: (contextId, csrfToken, instructionId, input, signal) => fetcher(`/api/v1/work-instructions/${encodeURIComponent(instructionId)}/assignment`, { method: "PATCH", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ id: string; assignmentId: string; assigneeUserId: string; version: number }>(response)),
    getPlanningTemplates: (contextId, signal) => fetcher("/api/v1/planning/templates", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ templates: PlanningTemplate[] }>(response)),
    expandPlanningTemplate: (contextId, csrfToken, templateId, input, signal) => fetcher(`/api/v1/planning/templates/${encodeURIComponent(templateId)}/expand`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ templateId: string; cropPlanId: string; instructions: WorkInstruction[]; conflictCount: number }>(response)),
    updateWorkProgress: (contextId, csrfToken, instructionId, input, signal) => fetcher(`/api/v1/work-instructions/${encodeURIComponent(instructionId)}/progress`, { method: "PATCH", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ id: string; progressPercent: number; status: string; version: number; updatedAt: string }>(response)),
    getJournalBootstrap: (contextId, search = {}, signal) => { const query = new URLSearchParams(); if (search.instructionId) query.set("instructionId", search.instructionId); if (search.fieldId) query.set("fieldId", search.fieldId); if (search.journalId) query.set("journalId", search.journalId); const suffix = query.size ? `?${query}` : ""; return fetcher(`/api/v1/journal-bootstrap${suffix}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<JournalBootstrap>(response)); },
    getPesticideBootstrap: (contextId, fieldId, signal) => fetcher(`/api/v1/pesticide-bootstrap?fieldId=${encodeURIComponent(fieldId)}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<PesticideBootstrap>(response)),
    getInventory: (contextId, signal) => fetcher("/api/v1/inventory", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<InventorySnapshot>(response)),
    createPurchaseOrder: (contextId, csrfToken, input, signal) => fetcher("/api/v1/inventory/purchase-orders", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    receiveInventoryLot: (contextId, csrfToken, input, signal) => fetcher("/api/v1/inventory/receipts", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    createInventoryCount: (contextId, csrfToken, input, signal) => fetcher("/api/v1/inventory/counts", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    postInventoryCount: (contextId, csrfToken, sessionId, input, signal) => fetcher(`/api/v1/inventory/counts/${encodeURIComponent(sessionId)}/post`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    getJournals: (contextId, signal) => fetcher("/api/v1/journals", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ journals: JournalEntry[] }>(response)),
    reviewJournal: (contextId, csrfToken, journalId, input, signal) => fetcher(`/api/v1/journals/${encodeURIComponent(journalId)}/review`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ id: string; status: string; version: number; updatedAt: string }>(response)),
    uploadJournalAttachment: (contextId, csrfToken, attachment, signal) => fetcher("/api/v1/journal-attachments", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": attachment.blob.type, "X-CSRF-Token": csrfToken, "X-Attachment-ID": attachment.id, "X-Journal-ID": attachment.journalId, "X-File-Name": encodeURIComponent(attachment.fileName), "X-Captured-At": attachment.capturedAt }), body: attachment.blob, signal }).then((response) => result<{ id: string; journalId: string; byteSize: number; sha256: string }>(response)),
    push: (contextId, csrfToken, bundles, signal) => fetcher("/api/v1/sync/push", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify({ bundles }), signal }).then((response) => result<{ results: PushResult[] }>(response)),
    pull: (contextId, scope, priority, cursor, signal) => { const query = new URLSearchParams({ scope, priority }); if (cursor) query.set("cursor", cursor); return fetcher(`/api/v1/sync/pull?${query}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<PullResult>(response)); },
    getQueues: (contextId, signal) => fetcher("/api/v1/sync/queues", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<QueueSnapshot>(response)),
    resolveConflict: (contextId, csrfToken, conflictId, resolution, signal) => fetcher(`/api/v1/sync/conflicts/${encodeURIComponent(conflictId)}/resolve`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify({ resolution }), signal }).then((response) => result<Record<string, unknown>>(response)),
    createMigrationJob: (contextId, csrfToken, input, idempotencyKey, signal) => fetcher("/api/v1/migration-jobs", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken, "Idempotency-Key": idempotencyKey }), body: JSON.stringify(input), signal }).then((response) => result<MigrationJob>(response)),
    getMigrationJobs: (contextId, signal) => fetcher("/api/v1/migration-jobs", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ jobs: MigrationJob[] }>(response)),
    commitMigrationJob: (contextId, csrfToken, jobId, expectedVersion, signal) => fetcher(`/api/v1/migration-jobs/${encodeURIComponent(jobId)}/commit`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify({ expectedVersion }), signal }).then((response) => result<MigrationJob>(response)),
    exportCsv: async (contextId, dataset, search = {}, signal) => {
      const query = new URLSearchParams();
      if (search.from) query.set("from", search.from);
      if (search.to) query.set("to", search.to);
      const response = await fetcher(`/api/v1/exports/${dataset}.csv${query.size ? `?${query}` : ""}`, { credentials: "include", cache: "no-store", headers: headers(contextId, { Accept: "text/csv" }), signal });
      if (!response.ok) return result<never>(response);
      const disposition = response.headers.get("Content-Disposition") || "";
      const matched = disposition.match(/filename="([^"]+)"/i);
      return { blob: await response.blob(), fileName: matched?.[1] || `${dataset}.csv` };
    },
    getSecurityAdministration: (contextId, signal) => fetcher("/api/v1/security-admin", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<SecuritySnapshot>(response)),
    provisionLocalTestUser: (contextId, csrfToken, input, signal) => fetcher("/api/v1/security-admin/local-test-users", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<LocalTestUserCredential>(response)),
    reconcileAttachmentStorage: (contextId, csrfToken, signal) => fetcher("/api/v1/security-admin/attachment-storage/reconcile", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "X-CSRF-Token": csrfToken }), signal }).then((response) => result<{ scanned: number; taggedOrphans: number; finalized: number; quarantined: number }>(response)),
    requestSecurityChange: (contextId, csrfToken, input, signal) => fetcher("/api/v1/security-admin/change-requests", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ requestId: string; status: string }>(response)),
    decideSecurityChange: (contextId, csrfToken, requestId, input, signal) => fetcher(`/api/v1/security-admin/change-requests/${encodeURIComponent(requestId)}/decision`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    createPrivacyRequest: (contextId, csrfToken, input, signal) => fetcher("/api/v1/security-admin/privacy-requests", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ requestId: string; status: string }>(response)),
    transitionPrivacyRequest: (contextId, csrfToken, requestId, input, signal) => fetcher(`/api/v1/security-admin/privacy-requests/${encodeURIComponent(requestId)}/transitions`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    getPesticideMasterReviews: (contextId, signal) => fetcher("/api/v1/pesticide-master/reviews", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<{ reviews: PesticideMasterReview[] }>(response)),
    requestPesticideMasterReview: (contextId, csrfToken, input, signal) => fetcher("/api/v1/pesticide-master/reviews", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<PesticideMasterReview>(response)),
    decidePesticideMasterReview: (contextId, csrfToken, reviewId, input, signal) => fetcher(`/api/v1/pesticide-master/reviews/${encodeURIComponent(reviewId)}/decision`, { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    getTenantAnalytics: (contextId, signal) => fetcher("/api/v1/analytics/overview", { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<TenantAnalytics>(response)),
    recordHarvestActual: (contextId, csrfToken, input, signal) => fetcher("/api/v1/analytics/harvests", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    getLocation: (contextId, locale, signal) => fetcher(`/api/v1/location?locale=${encodeURIComponent(locale)}`, { credentials: "include", cache: "no-store", headers: headers(contextId), signal }).then((response) => result<LocationBootstrap>(response)),
    recordLocationConsent: (contextId, csrfToken, input, signal) => fetcher("/api/v1/location/consents", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<Record<string, unknown>>(response)),
    saveLocationPreference: (contextId, csrfToken, input, signal) => fetcher("/api/v1/location/preference", { method: "PUT", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<LocationBootstrap["preference"]>(response)),
    appendLocationPoints: (contextId, csrfToken, input, signal) => fetcher("/api/v1/location/points", { method: "POST", credentials: "include", cache: "no-store", headers: headers(contextId, { "Content-Type": "application/json", "X-CSRF-Token": csrfToken }), body: JSON.stringify(input), signal }).then((response) => result<{ accepted: number; eventUuids: string[] }>(response)),
  };
}
